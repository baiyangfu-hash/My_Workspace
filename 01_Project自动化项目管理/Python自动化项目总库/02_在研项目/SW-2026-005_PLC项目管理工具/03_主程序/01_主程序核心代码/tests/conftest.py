"""测试共享 fixture"""

from __future__ import annotations

import os
import tempfile

import pytest


# 真实参考文件路径
WORKSPACE_ROOT = r"C:\Users\fubai\Desktop\My_Workspace\0100_PLC自动化"
PROJECT_ID = "DJ-2026-005"
PROJECT_PATH = os.path.join(WORKSPACE_ROOT, PROJECT_ID)
PROJ_FILE = os.path.join(
    PROJECT_PATH, "00_项目管理", "01_立项与需求",
    "003_DJ-2026-005_项目立项表_PROJ-V3.0.0.md",
)
CHG_FILE_001 = os.path.join(
    PROJECT_PATH, "00_项目管理", "04_变更管理", "01_变更单",
    "CHG-DOCU", "CHG-DOCU-2026-001.md",
)
LEDGER_FILE = os.path.join(
    PROJECT_PATH, "00_项目管理", "04_变更管理", "04_变更记录",
    "01_版本变更台帐.md",
)


@pytest.fixture
def proj_file() -> str:
    """立项表文件路径"""
    return PROJ_FILE


@pytest.fixture
def chg_file() -> str:
    """变更单文件路径"""
    return CHG_FILE_001


@pytest.fixture
def ledger_file() -> str:
    """台帐文件路径"""
    return LEDGER_FILE


@pytest.fixture
def workspace_root() -> str:
    """工作空间根目录"""
    return WORKSPACE_ROOT


@pytest.fixture
def project_id() -> str:
    """项目编号"""
    return PROJECT_ID


@pytest.fixture
def tmp_dir() -> str:
    """临时目录"""
    with tempfile.TemporaryDirectory() as d:
        yield d


@pytest.fixture
def sample_proj_content() -> str:
    """样例立项表内容"""
    return """# 测试项目立项表

> **项目编号**: TEST-2026-001
> **文档版本**: V1.0.0

## 3. 业务身份（这是什么项目）

| 字段 | 内容 |
|------|------|
| **项目名称** | TEST-2026-001 测试项目 V1.0.0 |
| **业务描述** | 这是一个测试项目 |
| **重要说明** | 仅用于测试 |
| **工艺范围** | 测试 / 验证 |
| **客户/产线** | （待补充） |

## 4. 技术环境（在什么平台上开发）

| 字段 | 内容 |
|------|------|
| **编程平台** | 测试平台 |
| **PLC 型号** | 测试PLC |
| **HMI 型号** | 测试HMI |
| **驱动器** | 测试驱动 |
| **通信方式** | 测试通信 |

### 控制参数

| 参数 | 值 |
|------|-----|
| 运动轴 | X1 单轴 |
| 定位精度 | ±0.5mm |
| 安全保护 | 急停保护 |

## 5. 工程规模（项目大概多大）

| 字段 | 值 | 备注 |
|------|-----|------|
| 工艺模块数 | 2 | test_a / test_b |

### 项目范围

- PLC 程序开发
- HMI 界面设计

## 6. 工程状态（当前健康度）

| 字段 | 值 |
|------|-----|
| **当前阶段** | 开发中 (`developing`) |
| **开始日期** | 2026-01-01 |
| **预计完成日期** | 2026-06-30 |
| **总工期** | 180 天 |

## 7. 变更台账（项目经历了哪些变更）

| 字段 | 值 |
|------|-----|
| **变更总数** | （待从变更管理系统读取） |

## 附录 A：项目资源与风险

### A.1 人力资源

| 角色 | 人员 | 说明 |
|------|------|------|
| 电气工程师 | 张三 | 开发与调试 |

### A.4 风险评估

| 风险项 | 风险等级 | 应对措施 |
|--------|:--------:|----------|
| 测试风险 | 低 | 充分测试 |
"""


@pytest.fixture
def sample_chg_content() -> str:
    """样例变更单内容"""
    return """# 变更单

## 3. 变更基本信息

### 3.0 编号与项目
| 字段 | 内容 |
|------|------|
| 变更编号 | CHG-PLC-2026-001 |
| 项目名称 | TEST-2026-001 测试项目 |
| 项目编号 | TEST-2026-001 |

### 3.1 技术领域（必选）
| 领域 | 选择 | 说明 |
|------|------|------|
| □ **ELEC** 电气设计 | - | Eplan/接线图 |
| □ **PLC**   PLC程序 | **选中** | SCL/ST/LD |
| ☑ **DOCU** 工程文档 | **选中** | 设计说明书 |

### 3.2 业务性质（必选）
| 性质 | 选择 | 典型场景 |
|------|------|----------|
| ☑ **DEF** 缺陷修复 | **选中** | Bug修复 |

### 3.3 影响范围（可多选）
| 范围 | 选择 | 审批要求 |
|------|------|----------|
| ☑ **LOCAL**   局部变更 | **选中** | 项目经理审批 |
| ☑ **MODULE** 模块级变更 | **选中** | 项目负责人审批 |

### 3.4 申请信息
| 字段 | 内容 |
|------|------|
| 变更申请人 | 张三 |
| 申请日期 | 2026-01-15 |
| 预计实施日期 | 2026-01-20 |
| 紧急程度 | ☑一般 □紧急 □非常紧急 |

## 4. 变更原因

**变更背景**：
测试变更背景描述

**变更必要性**：
测试变更必要性描述

**参考依据**：
测试参考依据

## 8. 变更审批

### 8.1 审批流程（按影响范围分级）

| 审批环节 | 审批人 | 审批意见 | 审批日期 | 签字/电子签章 |
|----------|--------|----------|----------|---------------|
| **初审** | 李四 | 同意 | 2026-01-16 | 李四 |

### 8.2 审批结论
| 结论 | ☑ 通过 □ 有条件通过 □ 驳回 □ 拒绝 |
"""
