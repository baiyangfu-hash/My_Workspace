# 产品需求文档（PRD）

## 1. 文档信息

| 项目 | 内容 |
|------|------|
| 文档名称 | 产品需求文档 |
| 文档版本 | V4.0.0 |

> **⚠️ V4.0.0 重要变更**: GUI架构从PyQt5迁移至PyWebView+Chromium（五区IDE布局），PyQt5保留为回退路径。新增V3.1/V3.2/V4.0/V4.1/V4.2里程碑。详见§3.4/§6.2/§6.3。
| 编制日期 | 2026-06-01 |
| 编制人 | AI Assistant (Trae) |
| 审核人 | [待审核] |
| 项目名称 | SW-2026-005 PLC项目管理工具 |
| 项目编号 | SW-2026-005 |

> **⚠️ V2.1.0 重要变更**: 本版本基于 2026-05-22 深度审查结果进行文档同步。
> M04(HMI映射)/M07(测试运行器) 状态修正为V2.0占位符，
> 新增 M11(变更管理Sync) 模块，新增6.4技术债与已知限制章节，反映V2.0-R2实际修复状态。详见 [变更记录](#103-变更记录)。

## 2. 产品概览

### 2.1 产品简介

**SW-2026-005 PLC项目管理工具** 是一款面向IEC 61131-3标准的PLC自动化项目集成化管理桌面应用。本产品旨在解决PLC开发过程中的项目管理分散、代码规范检查困难、文档生成繁琐、诊断分析不足等痛点，提供一站式项目管理解决方案。

**核心价值主张**:
- 🎯 **全生命周期管理**: 从项目创建到交付的完整流程覆盖
- 🔍 **智能规范检查**: 六维自动化代码质量检测
- 📄 **标准化文档生成**: 10种模板一键生成工程文档
- 📊 **深度诊断分析**: 七维度项目健康度评估
- 🔄 **变更管理Sync**: 版本检查/CHG/IFC文档自动生成
- 🔗 **FB接口一致性校验**: OB1调用参数与FB定义自动比对，缺参/多参/类型不匹配一键发现
- 🔧 **智能批量修复**: 中文标点自动替换、命名规范建议、dry_run预览+一键回滚
- 📊 **Excel接口文档**: SCL源码→标准9列变量表，支持批量导出全部FB
- 📚 **公共库全量扫描**: SysLib递归扫描+全局索引+版本一致性检测

### 2.2 产品目标

#### 业务目标
- 提升PLC项目开发效率 **30%+**
- 降低代码规范问题率 **50%+**
- 缩短文档编写时间 **60%+**
- 实现项目管理标准化、规范化

#### 技术目标
- 构建高内聚低耦合的分层架构
- 实现可扩展的插件化检查器体系
- 支持多品牌PLC（Siemens/Beckhoff/Omron/Mitsubishi/Codesys）
- 提供响应速度 < 3秒的用户体验

#### 用户目标
- **项目经理**: 快速创建标准化项目结构，实时监控项目健康度
- **PLC工程师**: 高效编辑ST代码，自动检查命名/语法规范
- **文档工程师**: 一键生成标准文档模板

### 2.3 术语定义

| 术语 | 定义 |
|------|------|
| **PLC (Programmable Logic Controller)** | 可编程逻辑控制器，工业自动化核心设备 |
| **IEC 61131-3** | 国际电工委员会制定的PLC编程语言标准 |
| **SCL (Structured Control Language)** | 结构化控制文本语言，类似Pascal的高级PLC语言 |
| **FB (Function Block)** | 功能块，IEC 61131-3中的可复用程序组织单元 |
| **FC (Function)** | 函数，IEC 61131-3中的无状态程序单元 |
| **POU (Program Organization Unit)** | 程序组织单元，包含Program/FB/FC |
| **GVL (Global Variable List)** | 全局变量列表 |
| **HMI (Human Machine Interface)** | 人机界面，用于监控和操作PLC系统 |
| **LSP (Language Server Protocol)** | 语言服务器协议，提供代码智能提示 |
| **AutoFix** | 自动修复引擎，支持 dry-run 预览和 .bak 备份回滚的安全修复机制 |
| **SysLib** | 共享函数库目录，存放跨项目复用的通用 FB/FC 组件（如气缸控制、输送机、定时器） |
| **FB Interface Signature** | FB 接口签名，包含 VAR_INPUT/VAR_OUTPUT/VAR_IN_OUT/VAR 的完整变量定义集合 |
| **SCLTest** | 基于SCL语言的单元测试框架 |

## 3. 核心功能

### 3.1 功能模块总览

| 模块ID | 模块名称 | 功能描述 | 优先级 | 当前状态 |
|--------|----------|----------|--------|----------|
| M01 | 仪表盘Dashboard | 项目概览、统计卡片、最近项目列表、快捷操作入口 | P0 | ✅ 已完成 |
| M02 | 项目管理 | 新建/打开/关闭项目、M001/S001模板选择、项目树浏览 | P0 | ✅ 已完成 |
| M03 | ST代码编辑器 | SCL语法高亮(QScintilla)、代码片段库(7种)、自动缩进 | P0 | ✅ 已完成 |
| M04 | HMI变量映射 | PLC变量→HMI标签双向映射、IO信号表编辑、Excel导入导出 | P1 | ⏸ V2.0占位符(V2.1+规划) |
| M05 | 规范检查面板 | 六维检查(命名/语法/注释/配置/定时器/变量)、规则注册表 | P0 | ✅ 已完成 |
| M06 | 诊断面板 | 七维度深度诊断、LSP兼容性检查、修复建议生成 | P0 | ✅ 已完成 |
| M07 | 测试运行器 | SCLTest解析执行、测试用例管理(PASS/FAIL/ERROR)、结果报告 | P1 | ⏸ V2.0占位符(V2.1+规划) |
| M08 | 文档生成引擎 | 10种模板(ALM/ARC/CHG/DSN/IFC/IO/REQ/UM/VAR)、变量替换 | P0 | ✅ 已完成 |
| M09 | 变量检查器 | I/O地址合法性、命名规范性、数据类型匹配度校验 | P1 | ✅ 已完成 |
| M10 | 路径解析器 | 策略模式实现相对/绝对路径转换、配置可移植性保证 | P2 | ✅ 已完成 |
| M11 | 变更管理Sync | 版本检查/CHG/IFC文档生成、同步报告 | P1 | ✅ 已完成 |
| M12 | FB接口一致性检查 | OB1调用参数与FB定义自动比对(缺参/多参/类型不匹配/输出未接/IN_OUT缺失/DB结构体不对齐), 6项规则(FB_IFC_001~006), 支持含SysLib联合检查 | P0 | ✅ V3.1完成 |
| M13 | 自动批量修复引擎 | 中文标点→英文半角自动替换(12种映射)、变量命名建议+脚本生成、dry_run预览模式、.bak备份+一键回滚、QThread后台执行 | P0 | ✅ V3.1完成 |
| M14 | Excel接口变量表导出 | SCL源码→标准9列Excel(序号/类别/名称/数据类型/隐藏/初始值/掉电保持/注释)、openpyxl样式引擎(首行冻结/斑马纹/自适应宽)、批量导出全部FB+调用关系矩阵+检查报告Excel | P0 | ✅ V3.1完成 |
| M15 | SysLib公共库扫描 | 自动发现SysLib根目录、递归扫描所有.scl文件、构建全局FB/FC接口索引(带缓存)、版本号提取(Vx.x.x格式)、项目引用库版本一致性检测 | P1 | ✅ V3.1完成 |

### 3.2 V3.1 功能详细需求

#### 3.2.1 M12 FB接口一致性检查 (FB_IFC_CHECKER)

**用户故事**: 作为PLC工程师，我希望工具能自动检查OB1主程序中所有FB调用的参数是否与FB的实际接口定义一致，以便提前发现运行时错误。

**输入**: OB1.scl 源码 + 项目目录下所有 .scl 文件(FB定义)
**输出**: 违规清单(按严重度排序: ERROR/WARNING/INFO)

**检查规则**:

| 规则ID | 名称 | 严重度 | 描述 |
|--------|------|--------|------|
| FB_IFC_001 | 缺少必要输入参数 | ERROR | FB定义了VAR_INPUT但调用时未赋值 |
| FB_IFC_002 | 未知参数名 | WARNING | 调用时传了FB不存在的参数 |
| FB_IFC_003 | 输出未连接 | WARNING | VAR_OUTPUT在调用时未用=>接出到GlobalVars |
| FB_IFC_004 | IN_OUT缺失 | ERROR | VAR_IN_OUT参数未处理 |
| FB_IFC_005 | 类型不匹配 | WARNING | 赋值源类型与参数基础类型不一致 |
| FB_IFC_006 | DB结构体不匹配 | ERROR | GlobalVars.db结构体字段数≠FB实际引脚数 |

**技术实现**:
- `FBCallParser`: 解析OB1中的 `fbName(param := value, out => target);` 调用语句
- `FBSignatureBuilder`: 从.scl构建FB完整接口签名(VAR_INPUT/OUTPUT/IN_OUT/VAR四分类)
- `FBInterfaceChecker`: 继承BaseChecker，6项规则独立可控(enable_rule/disable_rule)

**GUI入口**: 规范检查Dock面板工具栏 → 🔗 **FB接口检查** 按钮(紫色)

---

#### 3.2.2 M13 自动批量修复引擎 (AUTO_FIX)

**用户故事**: 作为PLC工程师，我希望工具能自动修复不符合规范的代码（至少是注释中的中文标点问题），并能在修复前预览效果。

**支持的修复器**:

| 修复器ID | 名称 | 安全级别 | 说明 |
|----------|------|----------|------|
| COMMENT_PUNCTUATION_FIXER | 注释中文标点修复 | 🔒安全(可直接执行) | 12种映射：，→, 、→, ：→:, ；→;, （→(), ）→), 【→[], 】→], ""→"", ''→'' |
| NAMING_FIXER | 变量命名建议 | ⚠️需确认 | 基于NamingChecker 6条规则(NAMING_001~006)，生成重命名建议脚本 |

**工作流程**:
1. **扫描** → AutoFixService.scan_project() → FixReport(统计: 可修复N处/需确认M处/已跳过S处)
2. **预览** → dry_run=True → ProjectFixReport(每项显示原始文本↔建议文本diff)
3. **执行** → 弹出确认对话框 → 实际修改文件 + 自动创建.bak备份
4. **回滚** → 从.bak恢复 → rollback_file()

**安全机制**:
- 修改前强制创建 `.bak` 备份(同目录)
- dry_run 模式只预览不修改
- is_safe 标记区分安全修复器和需确认修复器

**GUI入口**: 右侧面板 Tab **🔧 自动修复**

---

#### 3.2.3 M14 Excel接口变量表导出 (EXCEL_EXPORT)

**用户故事**: 作为PLC工程师，我希望从SCL源码直接导出符合公司标准的FB接口变量表Excel，避免手工录入的繁琐和出错。

**输出格式**(9列，严格对应标准模板):

| 列号 | 列名 | 数据来源 | 示例 |
|------|------|----------|------|
| 1 | 序号 | 自动编号 | 1, 2, 3... |
| 2 | 类别 | VAR_CATEGORY映射 | IN / OUT / VAR / IN_OUT |
| 3 | 名称 | 变量声明名 | i_bExtend |
| 4 | 数据类型 | 类型标注 | BOOL / DINT / TONR / ST_xxx |
| 5 | 隐藏 | 属性(默认OFF) | OFF / ON |
| 6 | 初始值 | :=后面的值 | 5000 / FALSE / (ET:=...) |
| 7 | 掉电保持 | RETAIN/PERSISTENT检测 | 保持 / 不保持 |
| 8 | 注释 | (* *)或//后的文字 | 伸出命令 (推/降/夹紧) |

**Excel样式特性**:
- 首行标题冻结(A3)
- 蓝色表头背景(#1565C0) + 白色文字
- 交替行背景色(白色/#E3F2FD斑马纹)
- 细边框 + 自适应列宽
- 居中/左对齐分区(序号居中、名称左对齐)

**导出模式**:
- 单个FB: export_fb_from_file(scl_path, output_dir) → FB_Name.xlsx
- 批量全部: export_all_fbs(project_path, output_dir, include_syslib) → List[xlsx]
- 检查报告: export_check_report(results, output_path) → Report.xlsx

**GUI入口**: 右侧面板 Tab **📊 Excel导出**

---

#### 3.2.4 M15 SysLib公共库扫描 (SYSLIB_SCANNER)

**用户故事**: 作为项目经理，我希望工具能同时检查项目中使用的公共库(SysLib)代码是否符合规范，确保整个技术栈的一致性。

**扫描范围**:
```
{workspace}/0100_PLC自动化/01_SharedLibraries/SysLib/
├── actuator/     (FB_1011_CylinderControl, FB_1012_ConveyorMotor, ...)
├── timer/        (FB_TON, FB_TONR, ...)
├── convert/       (FC_TIME_TO_INT, ...)
├── alarm/         (FB_2001_CommonAlarm, ...)
└── ...            (更多分类子目录)
```

**核心能力**:
- **自动发现**: discover_syslib_root(workspace) → Path
- **递归扫描**: scan_all_scl_files(root) → List[SCLFileInfo] (20+文件)
- **全局索引**: build_global_fb_index(root) → GlobalFBIndex (15 FBs + 5 FCs, 带缓存)
- **版本提取**: get_library_version(source) → "V9.0.0" (从头注释提取)
- **引用检测**: find_outdated_references(project, index) → List[LibraryRefIssue]

**联合检查模式**:
- check_project_with_syslib(project, syslib) → { project_report, syslib_report, library_issues, summary }
- 结果合并到统一的 CheckReport 中展示

**GUI入口**: 规范检查Dock面板工具栏 → 📚 **含SysLib** 复选框 + ⚙️ 配置按钮

### 3.3 核心流程设计

#### 流程1: 新建项目向导

```
用户点击[新建项目]
    ↓
NewProjectDialog.show()
    ├── Step 1: 选择业务线 (包装/物流/组装/其他)
    ├── Step 2: 填写项目信息 (名称/负责人/描述)
    ├── Step 3: 选择技术选型 (PLC品牌/HMI品牌)
    ├── Step 4: 选择项目规模 (M001完整版 / S001精简版)
    └── Step 5: 确认存储路径
        ↓
ProjectService.create_new_project(project_info)
    ├── 验证必填字段
    ├── 构建项目根目录 {parent}/{id}_{name}
    ├── 加载并应用模板JSON
    ├── 创建12个标准目录 + .gitkeep
    ├── 生成8个占位文件 (README/PRD/VAR_LIST等)
    ├── 写入 .plc_project.json 元数据
    └── 缓存Project对象 → 返回成功
        ↓
EventBus.project_created.emit(path)
    → Dashboard刷新统计
    → ProjectTree刷新节点
    → StatusBar显示成功消息
```

#### 流程2: 规范检查执行

```
用户在SpecCheckPanel选择检查规则 → 点击[开始检查]
    ↓
SpecCheckerService.run_checks(project_path, selected_rule_ids)
    ↓
RuleRegistry.get_checkers(rule_ids)  # 获取检查器实例列表
    ↓
并发扫描 (max_workers=4):
    for each checker:
        ├── FileScanner.scan()  # glob("**/*.st", "**/*.TcPOU")
        │   └── 排除: _test/example/.bak/.tmp
        └── for each file:
            ├── 读取文件内容 (限制1MB)
            └── checker.check(context)
                └── return List[CheckResult]
                    ├── rule_id: "NAMING_001"
                    ├── file: "Main.st"
                    ├── line: 42
                    ├── message: "变量名不符合匈牙利命名法"
                    ├── severity: Severity.MEDIUM
                    └── suggestion: "建议改为 'motor_Start_Cmd'"
    ↓
聚合结果 → CheckReport
    ├── summary: {total: 150, passed: 120, failed: 20, warning: 10}
    └── details: List[CheckResult]
        ↓
UI更新:
    ├── 结果列表展示 (✅通过 / ❌失败 / ⚠️警告)
    ├── 统计图表 (通过率环形图)
    └── 导出报告 (HTML/JSON)
```

#### 流程3: 七维度诊断分析

```
用户在DiagnosticPanel点击[开始诊断]
    ↓
DiagnosticService.run_diagnosis(project_path)
    ↓
按权重顺序执行三阶段诊断:

Phase 1: 规范检查 (基础分40%)
    ├── 调用 SpecCheckerService.run_checks()
    └── 计算合规得分 (0-100)

Phase 2: LSP兼容性诊断 (专项15%)
    ├── LSPCompatibilityChecker.scan_plc_output_dir()
    ├── detect_stub_misuse()  # 检测builtin stub误用
    ├── analyze_call_chain()  # 分析OB→FB调用链完整性
    └── check_missing_impl()  # 检查缺失的FB/FC实现

Phase 3: 健康度分析 (综合45%)
    ├── structure_score: 目录/文件齐全性 (15%)
    ├── naming_score: 命名规范符合率 (15%)
    ├── doc_coverage: 必需文档存在情况 (15%)
    ├── code_quality: 语法/注释综合评分 (20%, 权重最高)
    ├── config_compliance: .plc_project.json正确性 (10%)
    ├── test_coverage: 测试用例匹配度 (10%)
    └── library_status: 库引用状态 (15%)

    ↓
加权计算 overall_score = Σ(dimension_score × weight)
    ↓
生成 DiagnosticReport
    ├── overall_score: 85.5
    ├── dimension_scores: {structure: 90, naming: 80, ...}
    ├── issues: List[DiagnosticIssue]
    │   ├── dimension: "naming"
    │   ├── severity: Severity.HIGH
    │   ├── rule_id: "NAMING_001"
    │   ├── message: "30%变量名不规范"
    │   └── suggestion: "批量重命名为匈牙利命名法"
    └── suggestions: List[str]  # 改进建议 (score<80时生成)
        ↓
UI展示:
    ├── 总分仪表盘 (0-100刻度)
    ├── 雷达图 (七维度可视化)
    ├── 问题列表 (按严重程度排序)
    └── [一键修复] 按钮 (针对可自动修复的问题)
```

### 3.4 页面与交互设计

#### 主窗口布局 — PyWebView五区IDE布局 (V4.x)

```
┌─────────────────────────────────────────────────────────────────────┐
│  标题栏 [SW-2026-005 PLC项目管理工具] ─── 最小化/最大化/关闭        │
├────┬─────────┬──────────────────────────────────────┬───────────────┤
│    │         │                                      │               │
│ A  │    S    │          W                            │               │
│ c  │    i    │      工 作 区 (Multi-Tab)              │               │
│ t  │    d    │                                      │               │
│ i  │    e    │  ┌────────────────────────────────┐  │               │
│ v  │    b    │  │ [Dashboard] [Editor] [HMI] ... │  │               │
│ i  │    a    │  │                                │  │               │
│ t  │    r    │  │   当前活动页面内容               │  │               │
│ y  │         │  │                                │  │               │
│ B  │ 📁项目  │  │                                │  │               │
│ a  │ 🔍检查  │  │                                │  │               │
│ r  │ 📊诊断  │  │                                │  │               │
│    │ 📄文档  │  │                                │  │               │
│    │ 🔧修复  │  └────────────────────────────────┘  │               │
│    │         │                                      │               │
├────┴─────────┼──────────────────────────────────────┤               │
│              │          B (BottomPanel)              │               │
│              │  [终端] [输出] [问题] [日志]           │               │
│              │                                      │               │
├──────────────┴──────────────────────────────────────┴───────────────┤
│  StatusBar: [就绪] | 项目: DJ-2026-005 | 健康度: 85分 | V4.2      │
└─────────────────────────────────────────────────────────────────────┘

五区说明:
  ActivityBar (最左侧图标列): 全局导航，点击切换Sidebar内容
  Sidebar (左侧面板): 上下文相关面板(项目树/检查规则/诊断维度/文档列表)
  Workbench (中央多标签区): 主工作区，支持多Tab切换
  BottomPanel (底部面板): 终端/输出/问题/日志，可折叠
  StatusBar (底部状态栏): 项目状态/健康度/版本信息
```

#### 主窗口布局 — PyQt5回退布局 (V2.x遗留)

```
┌─────────────────────────────────────────────────────────────┐
│  菜单栏 [文件] [编辑] [视图] [项目] [工具] [帮助]           │
├──────────┬─────────────────────────────────┬───────────────┤
│          │                                 │               │
│  左侧面板  │       中央工作区 (TabWidget)     │   右侧面板    │
│          │                                 │               │
│ ┌──────┐ │  ┌─────────────────────────┐   │ ┌───────────┐ │
│ │项目树 │ │  │                         │   │ │诊断面板   │ │
│ │      │ │  │   [Dashboard/Editor/...] │   │ │           │ │
│ │📁 DJ │ │  │                         │   │ ├───────────┤ │
│ │ 📁ZD │ │  │                         │   │ │检查面板   │ │
│ │      │ │  │                         │   │ │           │ │
│ └──────┘ │  └─────────────────────────┘   │ ├───────────┤ │
│          │                                 │ │测试运行器 │ │
│          │                                 │ │           │ │
│          │                                 │ └───────────┘ │
├──────────┴─────────────────────────────────┴───────────────┤
│  状态栏: [就绪] | 项目: DJ-2026-005 | 健康度: 85分        │
└─────────────────────────────────────────────────────────────┘
```

#### 关键UI组件清单

| 组件名称 | 类名 | 功能说明 | 交互方式 |
|----------|------|----------|----------|
| 仪表盘页面 | DashboardPage | 统计卡片(4个)+健康度图+最近项目列表+快捷按钮 | 只读展示 |
| 项目树控件 | ProjectTree | 左侧DockWidget，显示项目目录结构 | 右键菜单/双击打开 |
| ST代码编辑器 | STEditor | 中央TabWidget页面，支持语法高亮 | 编辑/保存/片段插入 |
| 规范检查面板 | SpecCheckPanel | 右侧DockWidget，规则选择+结果列表 | 勾选规则/启动检查 |
| 诊断面板 | DiagnosticPanel | 右侧DockWidget，七维度评分+问题列表 | 启动诊断/查看详情 |
| 测试运行器 | TestRunnerPanel | 右侧DockWidget，用例管理+执行控制 | 运行/查看结果 |
| HMI映射表 | HMIMapper | 中央TabWidget页面，双向表格 | 编辑单元格/导入Excel |
| 新建项目向导 | NewProjectDialog | 模态对话框，5步向导 | 上一步/下一步/完成 |

## 4. 非功能需求

### 4.1 性能需求

| 指标 | 要求 | 测试方法 |
|------|------|----------|
| 应用启动时间 | ≤ 3秒 (冷启动) | 计时从main.py到MainWindow.show() |
| 项目扫描速度 | ≤ 5秒 (1000文件内) | 使用timeit测量FileScanner耗时 |
| 检查响应时间 | 单文件 ≤ 100ms | 对每个checker进行性能基准测试 |
| 内存占用 | 运行时 ≤ 200MB | 使用memory_profiler监控 |
| UI帧率 | ≥ 30fps (动画流畅) | PyQt5性能分析工具 |

### 4.2 安全需求

- **本地存储安全**: settings.json 敏感信息（最近项目路径）不包含密码
- **文件读写权限**: 仅访问用户指定的项目目录，不越权读取
- **输入校验**: 所有用户输入经过validators.py白名单过滤
- **异常处理**: 全局try-catch机制，避免崩溃导致数据丢失

### 4.3 兼容性需求

| 环境 | 最低要求 | 推荐配置 |
|------|----------|----------|
| 操作系统 | Windows 10 (1803+) | Windows 11 |
| WebView2 Runtime | Win10 1803+自带; Win7需单独安装 | 系统自带或Evergreen安装包 |
| Python版本 | 3.9+ | 3.11+ |
| 屏幕分辨率 | 1280×720 | 1920×1080或更高 |
| 字体支持 | Consolas (等宽字体) | 安装Source Code Pro |

### 4.4 可扩展性需求

- **检查器扩展**: 新增Checker只需继承BaseChecker并注册到RuleRegistry（零修改现有代码）
- **文档模板扩展**: 在`src/templates/documents/`添加.md文件并在config.py注册即可
- **项目模板扩展**: 在`src/templates/project_structures/`添加.json文件
- **品牌支持扩展**: 在config.py的PLC_CONFIG.supported_brands添加新品牌枚举

## 5. 数据需求

### 5.1 核心数据模型

#### Project (项目实体)
```python
@dataclass
class Project:
    name: str                           # 项目名称
    code: str                           # 项目编号 (如 "DJ-2026-005")
    path: str                           # 项目根目录(绝对路径)
    business_line: BusinessLine         # 业务线枚举 (PACKAGING/LOGISTICS/...)
    template_id: str                     # 模板ID ("TPL-SINGLE-PLC-M001")
    manager: str                         # 项目经理
    description: str                     # 项目描述
    plc_brand: Optional[PLCBrand]        # PLC品牌
    hmi_brand: Optional[HMIBrand]        # HMI品牌
    created_at: datetime                 # 创建时间
    updated_at: datetime                 # 最后更新时间
```

#### Document (文档实例)
```python
@dataclass
class Document:
    doc_type: str                        # 文档类型 (REQ/DES/IFC/UM/...)
    doc_number: str                      # 文档编号 (如 "REQ-DJ-2026-005-V1.0.0")
    version: str                          # 版本号
    author: str                           # 作者
    content: str                          # 文档内容(Markdown格式)
    path: str                             # 文件保存路径
    created_at: datetime                   # 创建时间
```

#### DiagnosticReport (诊断报告)
```python
@dataclass
class DiagnosticReport:
    overall_score: float                 # 总分 (0-100)
    dimension_scores: Dict[str, float]   # 各维度得分
    issues: List[DiagnosticIssue]         # 问题列表
    project_path: str                    # 被诊断项目路径
    checked_at: datetime                  # 诊断时间
```

### 5.2 数据存储方案

| 数据类型 | 存储位置 | 格式 | 说明 |
|----------|----------|------|------|
| 用户偏好设置 | `data/settings.json` | JSON | 最近项目列表/主题/窗口大小 |
| 项目元数据 | `{project}/.plc_project.json` | JSON | 项目基本信息/技术选型/状态 |
| 模板定义 | `src/templates/*.json` | JSON | M001/S001目录结构和占位文件 |
| 文档模板 | `src/templates/documents/*.md` | Markdown | 10种标准文档模板 |
| 日志文件 | `logs/*.log` | 文本 | loguru自动轮转 |

### 5.3 数据安全措施

- **自动备份**: 关闭项目前自动保存settings.json
- **版本兼容**: 配置文件格式变更时提供迁移脚本
- **损坏恢复**: settings.json损坏时使用默认配置启动
- **敏感信息**: 不存储密码/token等敏感数据

## 6. 范围限定

### 6.1 功能范围 (MVP V1.2.0)

**包含的功能**:
- ✅ 项目CRUD操作 (创建/打开/关闭/删除)
- ✅ 6种代码规范检查器 (命名/语法/注释/配置/定时器/变量)
- ✅ 7种文档模板自动生成 (REQ/DES/IFC/UM/CHG/ALM/VAR/IO/ARC)
- ✅ 七维度诊断分析 (含LSP兼容性检查)
- ✅ ST代码编辑器 (基础语法高亮)
- ✅ HMI变量映射表 (基本增删改查)
- ✅ 测试用例管理 (SCLTest解析执行)

**明确排除的功能** (V2.0规划):
- ❌ Git版本控制集成
- ❌ 多人协作/云端同步
- ❌ PLC在线连接/下载程序
- ❌ 仿真调试功能
- ❌ 移动端适配

### 6.2 技术范围

**技术栈边界**:
- GUI框架: PyWebView为主(Chromium前端)，PyQt5为回退路径 (V4.x架构变更)
- 前端技术: HTML5 + CSS3 + JavaScript (Chromium/WebView2 Runtime渲染)
- Python版本: 最低3.9 (不支持3.8及以下)
- 操作系统: 仅Windows (不考虑Linux/macOS跨平台)
- 数据库: MVP阶段仅使用JSON文件存储 (暂不引入SQLite)

**第三方依赖限制**:
- pywebview为核心GUI依赖 (≥6.2.1)，PyQt5作为回退路径保留
- WebView2 Runtime: Win10 1803+自带, Win7需单独安装
- openpyxl用于Excel导入导出功能(含接口变量表导出)
- pydantic用于数据模型验证 (核心依赖，不可省略)

### 6.3 时间范围

| 里程碑 | 目标版本 | 计划日期 | 交付物 |
|--------|----------|----------|--------|
| **M1: 核心框架** | V1.0.0 | 2026-04-20 | Application/EventBus/SettingsManager |
| **M2: 项目管理** | V1.0.0 | 2026-04-25 | ProjectService/TemplateService/新建向导 |
| **M3: 检查引擎** | V1.0.0 | 2026-04-30 | 6种Checker + RuleRegistry |
| **M4: 文档生成** | V1.1.0 | 2026-05-05 | DocumentService + 10种模板 |
| **M5: 诊断系统** | V1.1.0 | 2026-05-08 | DiagnosticService + HealthAnalyzer |
| **M6: 编辑器增强** | V1.2.0 | 2026-05-15 | STEditor + 代码片段库 |
| **MVP发布** | V1.2.0 | 2026-05-30 | 完整功能 + 用户手册 + 打包exe |
| **V2.x 迭代** | V2.0~V2.5 | 2026-05~06 | 变更管理/架构重构/信号治理/缺陷修复 |
| **V3.1 规范增强** | V3.1.0 | 2026-06 | M12 FB接口检查/M13 自动修复/M14 Excel导出/M15 SysLib扫描 |
| **V3.2 架构优化** | V3.2.0 | 2026-06 | 项目作用域管理/库引用解析/性能优化 |
| **V4.0 PyWebView迁移** | V4.0.0 | 2026-06 | GUI架构从PyQt5迁移至PyWebView+Chromium，五区IDE布局 |
| **V4.1 前端增强** | V4.1.0 | 2026-06 | 前端交互优化/响应式布局/主题系统 |
| **V4.2 稳定化** | V4.2.0 | 2026-06 | 缺陷修复/性能调优/打包发布 |
| **V3.0-RC** | V3.0.0 | 2026-Q3 | 发布候选版：打包exe + ST词法增强 + 诊断优化 |
| **V3.0+ 语言服务** | V3.0+ | 2026-Q3~Q4 | SCL解析器/LSP服务器/类型检查（见§6.5） |

### 6.4 技术债与已知限制（V2.0-R2）

| 编号 | 描述 | 影响 | 计划 |
|------|------|------|------|
| TD-01 | main_window.py 仍有~926行，布局构建代码可继续抽取 | 可维护性 | V2.1 |
| TD-02 | 变更管理缺工作流闭环(生成→审核→回写) | 功能完整性 | V2.1 |
| TD-03 | SpecCheckPanel存在双实例(Tab+Dock)，结果不同步 | 资源浪费 | V2.1 |
| TD-04 | 诊断面板同步方法可能阻塞UI | 用户体验 | V2.1 |
| TD-05 | ST词法分析器标点只处理分号 | 检查完整性 | V2.1 |

### 6.5 V3.0+ 未来展望：SCL 语言服务器

> ⚠️ **本章节为未来迭代方向规划，不属于当前工作范围。当前工作焦点为 V2.5 缺陷修复和 V3.0-RC 发布候选。**

> **战略定位**：替代 Go 版 Dynamic Siemens Language Support 插件，将 SW-2026-005 从"项目管理工具"升级为"PLC 开发工具链"。

#### 6.5.1 背景与动机

当前 SCL/ST 开发依赖 VS Code 扩展 "Dynamic Siemens Language Support"（Go 后端），存在以下问题：
- Go 语言栈与团队 Python 技术栈不一致，维护成本高
- SCL→Go 编译器语义保真度不足（VAR_IN_OUT 值拷贝、IEC 定时器无法映射）
- .scltest 测试框架依赖 Go 工具链，无法在纯 Python 环境运行
- 插件功能局限于语法诊断，缺乏项目管理、规范检查、文档生成等工程化能力

SW-2026-005 已具备项目管理全生命周期能力，将 SCL 语言服务集成进来可实现**1+1>2**的协同效应。

#### 6.5.2 语言选型决策

经过 Python / C# / C 三语言深度对比（详见可行性分析报告），决策如下：

| 语言 | 结论 | 理由 |
|------|------|------|
| **C** | ❌ 排除 | 无 LSP 生态、开发效率极低、字符串处理原始 |
| **C#** | ⚠️ 可选扩展 | TIA Portal Openness API 是 C# 专属（独有价值），但与现有 Python 项目断裂 |
| **Python** | ✅ 核心实现 | 与现有项目无缝集成、同进程 API 调用、pygls LSP 框架成熟 |

**最终方案**：Python 核心语言服务 + C# TIA Portal 网关（可选扩展）

#### 6.5.3 V3.0+ 功能模块规划

| 模块ID | 模块名称 | 功能描述 | 优先级 | 目标版本 |
|--------|----------|----------|--------|----------|
| M12 | SCL 词法分析器 | Token 流生成，关键字/标识符/字面量/运算符/注释识别 | P0 | V3.0 |
| M13 | SCL 语法分析器 | 递归下降解析器，构建完整 AST | P0 | V3.0 |
| M14 | SCL 类型检查器 | 类型系统、语义分析、实时诊断 | P0 | V3.0 |
| M15 | LSP 服务器 | pygls 实现，支持补全/悬停/跳转/引用/Inlay Hints | P0 | V3.1 |
| M16 | .plc.json 作用域管理 | 多项目隔离、库引用解析、.liblink GUID 匹配 | P1 | V3.2 |
| M17 | .s7dcl/.s7res 解析 | FBD 接口定义解析 + 可视化预览 | P1 | V3.2 |
| M18 | 标签表 XML 编辑器 | 表格化编辑 + XML 诊断（未定义类型/重复标签） | P1 | V3.2 |
| M19 | TIA Portal 网关 | C# Openness API 封装，项目创建/编译下载/硬件组态 | P2 | V4.0 |

**明确不实现**（与 Go 插件的差异）：

| 功能 | 不实现原因 |
|------|-----------|
| SCL→Go/SCL→Python 编译器 | 最复杂且最不可靠的模块，SCL 语义与通用语言存在根本差异 |
| .scltest 测试执行引擎 | 依赖编译器，PLC 测试应通过 TIA Portal 仿真或 C# Openness 网关 |

#### 6.5.4 架构方案

采用**混合模式**：核心语言服务实现为独立 Python LSP 服务器（可被 VS Code 消费），同时 PyQt5 应用通过直接 Python API 调用同一套语言服务（同进程零开销）。

```
┌──────────────────────────────────────────────────────────────┐
│                    SW-2026-005 应用 (Python)                  │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  UI 层 (PyWebView/Chromium) — V4.x 主路径              │  │
│  │  UI 层 (PyQt5) — V4.x 回退路径                         │  │
│  └───────────────────────┬────────────────────────────────┘  │
│                          │ Python API 直接调用                 │
│  ┌───────────────────────▼────────────────────────────────┐  │
│  │  语言服务层 (LanguageService) — V3.0+ 新增              │  │
│  │  SCL Parser / Type Checker / Symbol Index / Scope Mgr  │  │
│  │  S7DCL Parser / S7RES Parser / Tag Table XML Parser    │  │
│  └───────────────────────┬────────────────────────────────┘  │
│                          │ pygls LSP 适配层                    │
│  ┌───────────────────────▼────────────────────────────────┐  │
│  │  LSP 服务器 (pygls) — VS Code / 其他编辑器              │  │
│  └────────────────────────────────────────────────────────┘  │
│                          │ HTTP (可选)                        │
│  ┌───────────────────────▼────────────────────────────────┐  │
│  │  TIA Portal Gateway (C#) — V4.0 可选扩展               │  │
│  │  Openness API / 项目创建 / 编译下载 / 硬件组态           │  │
│  └────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

#### 6.5.5 迭代路线图

| 阶段 | 版本 | 核心交付 | 验收标准 |
|------|------|---------|---------|
| **阶段一** | V3.0 | SCL 解析器（词法+语法+AST） | SCL 源码→完整 AST，覆盖 90%+ 语法结构 |
| **阶段二** | V3.0 | 类型检查与诊断 | 实时诊断：类型错误/未声明变量/重复声明/类型不兼容 |
| **阶段三** | V3.1 | LSP 服务器 | VS Code 可连接，获得补全/悬停/跳转/引用 |
| **阶段四** | V3.2 | 项目作用域与文件格式 | .plc.json 隔离、库引用、FBD 预览、标签表编辑 |
| **阶段五** | V3.3 | 优化与扩展 | 增量解析/符号持久化/代码格式化/重构/快速修复 |
| **阶段六** | V4.0 | C# TIA Portal 网关（可选） | Openness API 集成，编译下载/硬件组态 |

#### 6.5.6 新增依赖

```
pygls>=1.3.0              # LSP 服务器框架
lsprotocol>=2023.0.0      # LSP 协议类型定义
```

#### 6.5.7 关键优势（相比 Go 插件）

| 维度 | Go 插件 | SW-2026-005 V3.0+ |
|------|---------|-------------------|
| 开发效率 | Go 编译慢、调试不便 | Python 即时运行、REPL 调试 |
| 桌面集成 | 仅 VS Code 扩展 | VS Code + PyWebView独立应用 + PyQt5回退双模式 |
| 项目管理 | 无 | 完整项目管理生命周期 |
| 规范检查 | 仅语法/类型诊断 | 6维规范检查 + LSP 诊断 + 健康度 |
| 文档生成 | 无 | 10种文档模板自动生成 |
| 变更管理 | 无 | SyncEngine + 变更单管理 |
| 多品牌支持 | 仅 Siemens | 5种 PLC 品牌（架构已预留） |
| TIA Portal 集成 | 无 | C# Openness 网关（V4.0 可选） |

## 7. 风险评估

### 7.1 风险识别矩阵

| 风险ID | 风险类型 | 风险描述 | 影响程度 | 发生概率 | 风险等级 |
|--------|----------|----------|----------|----------|----------|
| R01 | 技术风险 | PyWebView/前端学习曲线，Chromium渲染调试复杂 | 中 | 中 | 🟡 中 |
| R02 | 技术风险 | WebView2 Runtime在旧版Windows需单独安装 | 中 | 低 | 🟢 低 |
| R03 | 范围风险 | 功能蔓延，MVP范围无法按时交付 | 高 | 中 | 🟠 高 |
| R04 | 兼容性风险 | 不同Windows版本下PyQt5行为差异 | 中 | 低 | 🟢 低 |
| R05 | 性能风险 | 大型项目(1000+文件)扫描耗时过长 | 中 | 中 | 🟡 中 |
| R06 | 依赖风险 | 第三方库版本冲突 (pydantic v2 vs v1) | 低 | 低 | 🟢 低 |

### 7.2 应对策略

| 风险ID | 应对策略 | 责任人 | 触发条件 |
|--------|----------|--------|----------|
| R01 | 参考官方示例代码，优先实现核心功能；PyQt5保留为回退路径 | 开发工程师 | UI开发进度滞后>3天 |
| R02 | 提供WebView2离线安装包指引，Win10 1803+无需额外安装 | 开发工程师 | 旧版Windows环境安装失败 |
| R03 | 严格执行MoSCoW优先级排序，P2/P3功能延后至V2.0 | 项目经理 | Sprint计划超出容量20% |
| R04 | 在Win10和Win11双环境下测试，建立CI流水线 | 测试工程师 | 发现平台相关Bug |
| R05 | 采用并发扫描(ThreadPoolExecutor)+LRU缓存机制 | 开发工程师 | 扫描时间>10秒(500文件) |
| R06 | 锁定依赖版本(requirements.txt精确版本号)，提供虚拟环境脚本 | 开发工程师 | 依赖安装失败 |

## 8. 验收标准

### 8.1 功能验收

| 验收项 | 验收标准 | 测试方法 |
|--------|----------|----------|
| 项目创建 | 能使用M001/S001模板成功创建项目，目录结构完整 | 手工测试 + 自动化脚本 |
| 规范检查 | 6种检查器均能正常执行，误报率<5% | 测试用例集(test_checker_framework.py) |
| 文档生成 | 10种模板均能正确填充变量并生成Markdown | 自动化对比预期输出 |
| 诊断分析 | 七维度评分计算准确，问题定位到具体文件/行号 | Mock项目数据验证 |
| ST编辑器 | 语法高亮正常，代码片段插入无报错 | 手工编辑测试 |

### 8.2 性能验收

| 验收项 | 指标要求 | 验证工具 |
|--------|----------|----------|
| 启动速度 | 冷启动≤3秒 | timeit + 日志时间戳 |
| 内存占用 | 空闲状态≤150MB | Windows任务管理器 |
| 扫描性能 | 500文件项目≤5秒 | 内置计时器 |
| UI响应 | 操作延迟≤200ms | 用户体验主观评价 |

### 8.3 质量验收

| 验收项 | 标准 | 验证方式 |
|--------|------|----------|
| 代码覆盖率 | 核心模块≥80% | pytest --cov |
| 静态分析 | 无Critical/High级别问题 | pylint/flake8 |
| 文档完整性 | 所有公开API均有docstring | Sphinx autodoc |
| 用户手册 | 覆盖所有已实现功能 | 人工走查 |

## 9. 项目计划

### 9.1 迭代计划 (Scrum模式)

| Sprint | 时间范围 | 目标 | 主要任务 |
|--------|----------|------|----------|
| Sprint 1 | 04-15 ~ 04-19 | M1核心框架 | Application/EventBus/SettingsManager/ConfigLoader |
| Sprint 2 | 04-22 ~ 04-26 | M2项目管理 | ProjectService/TemplateService/NewProjectDialog/ProjectTree |
| Sprint 3 | 04-29 ~ 05-03 | M3检查引擎 | BaseChecker/RuleRegistry/6种Checker实现 |
| Sprint 4 | 05-06 ~ 05-10 | M4文档生成 | DocumentService/10种模板/变量替换引擎 |
| Sprint 5 | 05-13 ~ 05-17 | M5诊断系统 | DiagnosticService/HealthAnalyzer/LSPCompatibilityChecker |
| Sprint 6 | 05-20 ~ 05-24 | M6编辑器增强 | STEditor/QScintilla集成/代码片段库 |
| Sprint 7 | 05-27 ~ 05-30 | MVP发布 | 集成测试/性能优化/打包exe/用户手册 |

### 9.2 资源需求

| 资源类型 | 数量 | 说明 |
|----------|------|------|
| 开发工程师 (AI) | 1人 | 全职投入，负责全部开发任务 |
| 测试工程师 (AI) | 0.5人 | 兼职，负责测试用例设计和执行 |
| 文档专家 (AI) | 0.3人 | 兼职，负责技术文档和用户手册 |
| 开发机 | 1台 | Windows 11, 16GB RAM, SSD |
| 测试机 | 1台 | Windows 10, 用于兼容性测试 |

## 10. 附录

### 10.1 参考资料

| 资料名称 | 版本 | 来源 | 用途 |
|----------|------|------|------|
| IEC 61131-3 国际标准 | 第3版 | IEC官网 | PLC编程语言规范参考 |
| PyQt5 官方文档 | 5.15.x | Qt官方 | GUI框架API参考 |
| QScintilla 文档 | 2.13.x | Riverbank Computing | 代码编辑器组件参考 |
| Python编码规范 (PEP 8) | - | Python.org | 代码风格指南 |
| 全局规范仓库索引 | V3.1.0 | 内部文档库 | PM流程和文档模板规范 |

### 10.2 竞品分析

| 产品名称 | 类型 | 优势 | 劣势 | 本产品差异化 |
|----------|------|------|------|--------------|
| Siemens TIA Portal | IDE | 官方集成度高 | 仅支持Siemens，价格昂贵 | **免费开源 + 多品牌支持** |
| Codesys Development System | IDE | IEC标准最完善 | 商业软件，学习成本高 | **轻量化 + 专注项目管理** |
| PC WORX (Phoenix Contact) | IDE | 稳定性好 | 市场占有率低 | **Python生态 + 可定制性强** |
| 手动文件夹管理 | 无工具 | 零成本 | 效率低下，易出错 | **自动化 + 规范化 + 可追溯** |

### 10.3 变更记录

| 版本 | 变更日期 | 变更内容 | 变更人 | 审核人 |
|------|---------|----------|--------|--------|
| V1.0.0 | 2026-05-08 | 初始版本，基于README和架构设计文档提炼完整需求 | AI Assistant (Trae) | [待审核] |
| V2.1.0 | 2026-05-22 | 深度审查后文档同步：修正M04/M07状态为占位符、新增M11 Sync模块、新增6.4技术债章节、反映V2.0-R2实际修复状态(13项修复+6项降级) | AI Assistant (Trae) | [待审核] |
| V2.2.0 | 2026-05-28 | 新增§6.5 V3.0+未来展望：SCL语言服务器规划（Python核心+C#可选网关）、M12~M19模块定义、迭代路线图、语言选型决策；⚠️明确标注为未来迭代方向，非当前工作范围 | AI Assistant (Trae) | [待审核] |
| V4.0.0 | 2026-06-01 | V4.x PyWebView架构变更：GUI从PyQt5迁移至PyWebView+Chromium(五区IDE布局)、PyQt5保留回退路径；补充V3.1~V4.2里程碑；更新§3.4页面设计/§4.3兼容性/§6.2技术范围/§6.3时间范围/§7风险评估 | AI Assistant (Trae) | [待审核] |

---

**文档版本**: V4.0.0
**编制日期**: 2026-06-01
**编制人**: AI Assistant (Trae)
**审核人**: [待审核]

*本文档遵循全局规范仓库 `00_Obsidian_Base全局规范文件仓库` 的PRD模板格式*
