# auto-pm 项目全面诊断报告（第二版）

> 诊断时间：2026-07-08 | 版本：V0.9.0 | 代码规模：125 源文件（20,212 行）+ 117 测试文件

---

## 一、整体评价

**项目成熟度：★★★★☆ （工业级工程化项目，细节仍有打磨空间）**

这是一个结构清晰、功能完善的自动化项目管理工具，涵盖 CLI + GUI 双入口、五层架构（CLI → Application Facade → Core Service → DB Repository → Models）、12 状态机变更管理流程，已经过多轮 Dogfooding 验证。整体代码质量在同类型内部工具中属于**上乘**水平。

**本次诊断范围**：逐文件审查全部 125 个源文件、117 个测试文件、所有配置与文档，相比首次诊断新增发现 7 项问题。

---

## 二、架构优势 ✅

| 维度 | 评价 |
|------|------|
| **分层架构** | 五层分离清晰：`cli/` → `application/` → `core/` → `db/` → `models/`，依赖方向单向 |
| **Protocol 接口** | `core/protocols.py` 使用 `@runtime_checkable Protocol`，4 个域契约定义完整（Project/Change/PLC/Scanner） |
| **Facade 模式** | `application/` 层 5 个 Facade 统一聚合入口，GUI/CLI 通过同一接口访问业务逻辑 |
| **职责拆分** | ChangeService 从 870 行上帝类拆分为 6 个职责单一类（parser/generator/guard_checker/ledger_updater/markdown_editor/file_locator） |
| **原子写入** | `file_utils.write_file` 实现 temp + `os.replace` 原子写入 + 乐观锁（`expected_mtime`），数据安全性好 |
| **文件快照** | `read_file_snapshot` 通过读前/读后 mtime 一致性校验，降低 read-modify-write 场景竞态 |
| **Schema 迁移** | `db/schema.py` 的 `migrate_schema()` 通过 `_column_exists()` 实现幂等增量迁移 |
| **增量扫描** | `SyncService` 双重判据（file_mtime + scanner_version），CHG-085 修复了 scanner 逻辑变更不触发重扫的 P1 缺陷 |
| **日志体系** | 双 handler（stderr + 文件轮转），`_SilentTimedRotatingFileHandler` 处理沙箱限制优雅 |
| **审计日志** | 独立 `audit.py` 实现操作审计，按天轮转保留 90 天，格式化为 `ACTION=xxx | KEY=VAL` 便于检索 |
| **测试体系** | 测试覆盖 17 个子目录，含冒烟/单元/集成/GUI 多层级标记，117 个测试文件 |
| **Pydantic v2 模型** | 全面使用 Pydantic v2 + `ConfigDict(from_attributes=True)`，类型安全性好 |
| **多来源去重** | `ProjectScanner._deduplicate_projects()` 实现路径去重 + project_id 去重 + 优先级合并 |
| **Windows 编码** | `__main__.py` 设置 `PYTHONUTF8=1`，`cli/__main__.py` 通过 Windows API 检测控制台代码页精准适配 |

---

## 三、发现的问题与建议

### 🔴 高优先级（影响可靠性/可维护性）

---

#### 1. `get_project()` 存在 O(n) 全量扫描性能问题

```python
# core/project_service.py L207-220
def get_project(self, project_id: str) -> Optional[ProjectInfo]:
    for proj in self.list_projects():  # ⚠️ 每次调用都触发全量文件系统扫描
        if proj.project_id == project_id:
            return proj
```

> [!WARNING]
> `get_project()` 每次调用都执行 `list_projects()` → `_scanner.scan()` 进行全量文件系统递归扫描，而不是优先查 DB 缓存。当工作空间项目数量增长时，每次查询单个项目都要扫描全部文件系统。
>
> **连锁放大**：`update_project_meta()` (L346) 和 `search_projects()` (L320) 内部都调用了 `get_project()` 或 `list_projects()`，导致一次"更新元数据"操作触发**两次**全量扫描（L346 查项目 + L356 重新查询更新后结果）。

**建议**：优先走 `get_project_cached()`，DB 不可用时才降级到文件扫描：
```python
def get_project(self, project_id: str) -> Optional[ProjectInfo]:
    if self._repo is not None:
        return self.get_project_cached(project_id)
    # 降级：文件系统扫描
    for proj in self.list_projects():
        if proj.project_id == project_id:
            return proj
    return None
```

---

#### 2. `DatabaseManager.get_connection()` 每次调用都创建新连接

```python
# db/connection.py L37-48
def get_connection(self) -> sqlite3.Connection:
    self._ensure_dir()
    conn = sqlite3.connect(self.db_path)  # ⚠️ 每次调用创建新连接
    conn.row_factory = sqlite3.Row
    conn.execute("PRAGMA journal_mode=WAL")
    conn.execute("PRAGMA foreign_keys=ON")
    return conn
```

> [!WARNING]
> 每次操作都创建新连接并重新设置 PRAGMA。`with self.db.get_connection() as conn` 的 `as` 语法在 `sqlite3.connect` 上只是事务管理，**不会关闭连接**。
>
> **实际影响量化**：`ProjectRepository` 中每个方法（`upsert`/`get_by_id`/`list_all`/`list_by_stack`/`list_by_phase`/`list_by_business_line`/`count`/`get_mtime`/`delete`）都调用 `get_connection()`。一次 `_sync_projects()` 操作中，假设有 30 个项目需要同步，将创建约 **60+ 个连接**（list_all + 30×upsert + stale 检测）。

**建议**：改为连接池模式或单例连接：
```python
def get_connection(self) -> sqlite3.Connection:
    if self._conn is None or self._conn_closed:
        self._ensure_dir()
        self._conn = sqlite3.connect(self.db_path)
        self._conn.row_factory = sqlite3.Row
        self._conn.execute("PRAGMA journal_mode=WAL")
        self._conn.execute("PRAGMA foreign_keys=ON")
    return self._conn
```

---

#### 3. `except Exception` 过度使用（31 处，涉及 15 个文件）

在 15 个源文件中发现 **31 处** `except Exception` 宽泛捕获（较首次诊断的 23 处增加，因新增文件）：

| 文件 | 数量 | 严重程度 | 说明 |
|------|------|----------|------|
| `ui/factories.py` | 8 | ⚠️ 中 | 全部带 `# noqa: BLE001` 注释，说明是有意为之的 UI 防崩溃 |
| `application/workbench_facade.py` | 8 | 🔴 高 | 部分无异常记录（L153 `except Exception: pass`） |
| `application/system_facade.py` | 7 | 🔴 高 | L71/L135/L154 无异常记录的静默吞没 |
| `core/project_service.py` | 4 | 🔴 高 | L192/L196 `return 0` 静默吞没，掩盖 DB 查询错误 |
| `spec/services/frontmatter_svc.py` | 2 | 🔴 高 | L91/L146 完全无异常记录 |
| `spec/services/fix_svc.py` | 1 | ⚠️ 中 | 嵌套在修复循环中 |
| 其他文件（CLI/change/db/sync） | ~10 | ⚠️ 中 | 多数带 `as e` 并有日志记录 |

**分级建议**：
- **CLI 层**（`cli/*.py`）：`except Exception as e` + `click.echo(error)` 可接受（最外层兜底）
- **UI 层**（`factories.py`）：带 `# noqa: BLE001` 的防崩溃写法可保留，但应添加 `log.debug` 记录
- **Core/Application 层**：必须缩窄为具体异常类型或至少添加 `log.warning(exc_info=True)`
- **静默 `return 0`/`pass`**（L192/L196/L153）：**必须修复**，添加 warning 日志

---

#### 4. `_get_project_mtime()` 重复实现（3 处代码克隆）

同一功能在 **3 个文件** 中存在近乎相同的实现：

```python
# 1. core/project_scanner.py L577-592 — get_project_mtime() (staticmethod)
# 2. core/project_service.py L692-694 — _get_project_mtime() (委托给 scanner)
# 3. db/sync.py L240-263 — _get_project_mtime() (独立重复实现)
```

> [!IMPORTANT]
> `sync.py` 的实现是 `project_scanner.py` 的完整克隆（逐行一致），违反 DRY 原则。如果 mtime 计算逻辑变更（如新增项目标志文件），需要同步修改两处，容易遗漏。

**建议**：`sync.py` 直接调用 `ProjectScanner.get_project_mtime()`，消除重复：
```python
# db/sync.py
from auto_pm.core.project_scanner import ProjectScanner

# 在 _sync_projects 中：
mtime = ProjectScanner.get_project_mtime(proj.path)
```

---

#### 5. `setup_logger()` 模块级调用绕过 `AppContext` 配置（新发现）

全项目有 **18 处** 模块级 `log = setup_logger(log_level="INFO", app_name="auto_pm")` 调用，硬编码 `log_level="INFO"`：

```python
# 典型模式（出现在 18 个文件中）
from auto_pm.logging.logging import setup_logger
log = setup_logger(log_level="INFO", app_name="auto_pm")  # ⚠️ 硬编码 INFO
```

> [!WARNING]
> `AppContext.__init__` 中通过 `AutoPmConfig` 读取 `.env` 的 `log_level` 配置，但这 18 个模块的 logger 在**导入时**就已创建，使用硬编码的 `"INFO"` 级别。`setup_logger` 内部虽然幂等（已有 handler 不重复添加），但**首次调用的级别固化**，后续 `AppContext` 初始化的配置无法覆盖。
>
> 用户在 `.env` 中设置 `LOG_LEVEL=DEBUG` 时，这些模块的日志不会生效。

**建议**：改用标准 `logging.getLogger(__name__)` 模式，在 `setup_logger` 中只配置 root `auto_pm` logger 一次：
```python
# 各模块中：
import logging
log = logging.getLogger(__name__)

# app_context.py 中：
setup_logger(log_level=self.app_config.log_level, app_name="auto_pm")
```

---

#### 6. `change/parser.py` 文件过大（837 行）

> [!IMPORTANT]
> `parser.py` 是 837 行的单文件，承担了变更单 Markdown 解析的全部逻辑（章节拆分、§3~§10 共 8 个章节解析、状态推断、影响分析转换等）。虽然 ChangeService 已拆分为 6 个子类，但 Parser 本身仍是"上帝解析器"。

**建议**：按章节拆分为多个解析方法类，或使用策略模式注册各章节解析器：
```
parser/
├── base.py           # ChgParser 主类 + 章节拆分
├── info_parser.py    # §3 变更基本信息解析
├── reason_parser.py  # §4 变更原因解析
├── impact_parser.py  # §6 影响分析解析
└── status_inferrer.py # 状态推断逻辑
```

---

### 🟡 中优先级（代码质量/可维护性）

---

#### 7. `Optional[X]` 与 `X | None` 混用（48+ 处 `Optional`）

项目中 `Optional[X]` 出现 **48+ 处**（分布在 `project_service.py`/`project_scanner.py`/`protocols.py`/`change_service.py`/`file_locator.py`/`logging.py` 等），同时 `dto.py` 和 `models/change.py` 已使用 `X | None` 写法。

```python
# protocols.py 混用
def get_project(self, project_id: str) -> Optional[ProjectInfo]: ...
def import_project(self, ..., business_line: str | None = None) -> str: ...
```

**建议**：统一使用 `X | None`（Python 3.11+），全局替换 `Optional[X]` → `X | None`，减少 `from typing import Optional` 导入。

---

#### 8. Facade 层 `Any` 类型参数（11 处，涉及 5 个 Facade）

5 个 Facade 的 `__init__` 中共有 **11 个参数** 标注为 `Any`：

| Facade | Any 参数 | 已有 Protocol 可替代 |
|--------|----------|---------------------|
| `workbench_facade.py` | `dashboard_service`, `asset_summary_service` | 无（需新建 Protocol） |
| `system_facade.py` | `pm_session_service`, `template_service` | 无（需新建 Protocol） |
| `delivery_facade.py` | `doc_refresh_service`, `report_service`, `asset_summary_service`, `project_service` | `ProjectServiceProtocol` 可用 |
| `spec_facade.py` | `spec_check_service`, `spec_center_service` | 无（需新建 Protocol） |
| `change_facade.py` | `_summary_to_dto(summary: Any)`, `_request_to_dto(cr: Any)` | 可用 `ChangeSummary`/`ChangeRequest` |

**建议**：
1. `delivery_facade.py` 的 `project_service` 应立即改为 `ProjectServiceProtocol`
2. `change_facade.py` 的 DTO 转换方法应使用具体类型
3. 其他 Service 逐步补充 Protocol 定义

---

#### 9. `FacadeRegistry.initialize()` 使用 `dict[str, Any]` 非类型安全

```python
# ui/registry.py L22
def initialize(self, services: dict[str, Any]) -> None:
    dashboard_service = services.get("dashboard_service")  # ⚠️ 字符串 key，无类型检查
```

**建议**：使用 `TypedDict` 或 `dataclass` 定义服务容器：
```python
@dataclass
class ServiceContainer:
    project_service: ProjectServiceProtocol
    change_service: ChangeServiceProtocol
    dashboard_service: Any  # 待定义 Protocol
    # ...
```

---

#### 10. 17 个 TODO 待完成项

| 文件 | TODO 数量 | 内容摘要 |
|------|-----------|----------|
| [workbench_facade.py](file:///c:/Users/fubai/Desktop/My_Workspace/01_Project自动化项目管理/Python自动化项目总库/02_在研项目/SW-2026-008_auto-pm_自动化项目管理工具/auto_pm/application/workbench_facade.py) | 7 | health_status/last_activity_at/document_status/vartable_status/pending_actions |
| [delivery_bridge.py](file:///c:/Users/fubai/Desktop/My_Workspace/01_Project自动化项目管理/Python自动化项目总库/02_在研项目/SW-2026-008_auto-pm_自动化项目管理工具/auto_pm/ui/qml/bridges/delivery_bridge.py) | 3 | 资产刷新/汇总展示 QML 接入 |
| [system_bridge.py](file:///c:/Users/fubai/Desktop/My_Workspace/01_Project自动化项目管理/Python自动化项目总库/02_在研项目/SW-2026-008_auto-pm_自动化项目管理工具/auto_pm/ui/qml/bridges/system_bridge.py) | 2 | 模板应用 QML 接入 |
| [change_bridge.py](file:///c:/Users/fubai/Desktop/My_Workspace/01_Project自动化项目管理/Python自动化项目总库/02_在研项目/SW-2026-008_auto-pm_自动化项目管理工具/auto_pm/ui/qml/bridges/change_bridge.py) | 1 | 创建/流转/编辑 QML UI |
| DTO 文件 (system_dto.py / delivery_dto.py) | 4 | Service 结构细化、DocRefreshService 参数修复 |

**建议**：将这些 TODO 转为 GitHub Issues 或变更单跟踪，避免遗忘。

---

#### 11. 根目录散落临时文件（已入库的 5 个需清理）

通过 `git ls-files` 验证，以下文件**已被 git 跟踪**（需从仓库中删除）：

| 文件 | 大小 | 状态 | 建议 |
|------|------|------|------|
| `PM_SESSION_SW-2026-008.md.bak_v060_combined` | 465KB | ⚠️ 已入库 | `git rm` 并加入 `.gitignore` (`*.bak_*`) |
| `PM_SESSION_SW-2026-008.md.bak_v060_s9` | 121KB | ⚠️ 已入库 | 同上 |
| `PM_SESSION_SW-2026-008.md.bak_v060_split` | 478KB | ⚠️ 已入库 | 同上 |
| `claude_plan` | 12KB | ⚠️ 已入库 | `git rm` 并加入 `.gitignore` |
| `pytest_nongui_result.txt` | 19KB | ⚠️ 已入库 | `git rm` 并加入 `.gitignore` (`pytest_*.txt`) |

以下文件**已被 `.gitignore` 覆盖**（`*.log` 规则），无需额外处理：
- `gui_automation_test.log` / `gui_automation_test_v1.log`
- `mypy_w1s06.log` / `ruff_w1s06.log` / `pytest_run.log` / `pytest_w1s06_full.log`

**清理命令**：
```bash
git rm --cached PM_SESSION_SW-2026-008.md.bak_* claude_plan pytest_nongui_result.txt
echo "*.bak_*" >> .gitignore
echo "pytest_*.txt" >> .gitignore
echo "claude_plan" >> .gitignore
```

---

#### 12. `ProjectInfo` 模型直接原地修改属性（违反 Pydantic 不可变性原则）（新发现）

`ProjectScanner._deduplicate_projects()` 和 `_enrich_from_plc_json()` 中直接修改 `ProjectInfo` 实例属性：

```python
# core/project_scanner.py L100-119 (_deduplicate_projects 中)
if not primary.name and other.name:
    primary.name = other.name     # ⚠️ 直接修改 Pydantic model 属性
if primary.stack == "unknown" and other.stack != "unknown":
    primary.stack = other.stack   # ⚠️ 同上

# core/project_scanner.py L456-473 (_enrich_from_plc_json 中)
if info.stack == "unknown":
    info.stack = "plc"            # ⚠️ 同上
```

> [!NOTE]
> `ProjectInfo`（即 `Project`）使用 Pydantic v2 默认配置（`model_config = ConfigDict(from_attributes=True)`），默认**允许**属性赋值（不是 `frozen=True`）。所以这在技术上是合法的，但风格上与 Pydantic 鼓励的不可变数据模型原则不一致。如果未来需要 `frozen=True`（如用作 dict key 或放入 set），这些代码将全部报错。

**建议**：短期可接受（当前不设 `frozen=True`），但建议在合并阶段使用 `model_copy(update={...})` 替代原地修改：
```python
primary = primary.model_copy(update={"name": other.name})
```

---

### 🟢 低优先级（风格/优化建议）

---

#### 13. Ruff 规则集偏保守

```toml
# .ruff.toml
select = ["E4", "E7", "E9", "F", "I", "T20"]
```

当前仅启用了基础的 `E4/E7/E9`（pycodestyle 错误子集）、`F`（Pyflakes）、`I`（isort）、`T20`（print 检查）。

**建议**：逐步启用更多规则以提升代码质量：
```toml
select = [
    "E4", "E7", "E9", "F", "I", "T20",
    "B",      # flake8-bugbear: 常见 bug 模式
    "BLE",    # flake8-blind-except: 宽泛异常捕获（配合 #3 整改）
    "SIM",    # flake8-simplify: 代码简化
    "UP",     # pyupgrade: Python 版本升级建议（配合 #7 Optional 统一）
    "RUF",    # Ruff 专有规则
]
```

---

#### 14. `AutoPmConfig` 配置类功能单薄

```python
# config/app_config.py
class AutoPmConfig(BaseSettings):
    app_name: str = __app_name__
    log_level: Literal[...] = "INFO"
    # Add more ...  ← 注释暗示尚未扩展（与 V0.9.0 版本不匹配）
```

项目中大量硬编码配置分散在各模块中：

| 硬编码项 | 位置 | 建议集中到 Config |
|----------|------|-------------------|
| `scan_depth=4` | `list_projects()` 默认参数 | `AutoPmConfig.scan_depth` |
| `.auto-pm/index.db` | `DatabaseManager.DB_DIR/DB_FILE` | `AutoPmConfig.db_dir/db_file` |
| `backupCount=30` | `logging.py` L92 | `AutoPmConfig.log_backup_count` |
| `backupCount=90` | `audit.py` L62 | `AutoPmConfig.audit_backup_count` |
| `02_在研项目` | `paths.py` WORKSPACE_PROJECTS_SUBDIR | `AutoPmConfig.workspace_projects_subdir` |
| `max_depth=3` | `_find_plc_json_recursive` | `AutoPmConfig.plc_json_search_depth` |

---

#### 15. `os.path` 与 `pathlib.Path` 混用

- **主要使用 `os.path`**：`core/`（project_service.py/project_scanner.py/paths.py/constants.py）、`change/`（全部）、`db/sync.py`、`utils/file_utils.py`
- **使用 `pathlib.Path`**：`db/connection.py`（L11/L35）、`logging/logging.py`（L28-34）、`logging/audit.py`（L32-33）、`spec/core/scanner.py`

**建议**：长期目标统一到 `pathlib.Path`，短期不急于改动（涉及面广）。新代码优先使用 `pathlib`。

---

#### 16. `change/models.py` 与 `models/change.py` 存在模型重叠

- `auto_pm/change/models.py`（8.6KB）：定义常量（`STATUS_FLOW`/`VALID_DOMAINS`/`VALID_BUSINESS_NATURES`）、验证函数（`validate_domain()`/`validate_status_transition()`）
- `auto_pm/models/change.py`（6.9KB）：定义 Pydantic 数据模型（`ChangeRequest`/`ChangeSummary`/`ImpactAnalysis`/`ApprovalRecord`）

虽然 V0.9.0 已将**数据模型**迁移到 `models/change.py`、**常量和验证逻辑**保留在 `change/models.py`，但两个文件名完全相同（只是包路径不同），容易造成混淆。

**建议**：将 `change/models.py` 重命名为 `change/constants.py` 或 `change/validators.py`，消除歧义。

---

#### 17. `.gitignore` 缺少关键排除规则（新发现）

当前 `.gitignore` 未覆盖以下已入库或可能入库的文件类型：

```gitignore
# 建议新增
*.bak_*           # PM_SESSION 备份文件（3个，共 1MB+）
pytest_*.txt      # 测试结果文本
claude_plan       # AI 生成的临时计划文件
*.benchmarks/     # 性能基准测试结果
test_reports/     # 测试报告目录
test_screenshots/ # 测试截图目录
screenshots/      # 截图目录
.trae/            # Trae IDE 配置
.agents/          # Agent 配置
```

---

#### 18. `change/change_service.py` 仍较大（887 行）（新发现）

虽然 ChangeService 已经拆分出 6 个子类，但 `change_service.py` 本身仍有 **887 行**（36.7KB），包含：
- 方法总数约 25 个
- `list_all_changes()` 方法体 57 行
- `create_change_request()` 方法体 72 行

**建议**：当前体量可接受（每个方法职责清晰），但如果继续增长应考虑将 `list_all_changes` 等跨项目查询方法提取到独立的 `ChangeQueryService`。

---

#### 19. `ProjectInfo` 别名的向后兼容包装方法冗余（新发现）

`project_service.py` L663-694 保留了 **7 个向后兼容包装方法**，全部是一行委托：

```python
def _try_identify_project(self, project_path: str) -> Optional[ProjectInfo]:
    """[已委托] ..."""
    return self._scanner.try_identify_project(project_path)

def _read_copier_answers(self, project_path: str) -> Optional[ProjectInfo]:
    """[已委托] ..."""
    return self._scanner.read_copier_answers(project_path)
# ... 5 more
```

> [!NOTE]
> 这些方法以 `_` 前缀标记为私有，理论上外部不应直接调用。如果测试代码也未直接调用这些方法，可以安全删除。

**建议**：检查是否有外部调用方，无则删除这些 7 行一对一委托。

---

## 四、安全性检查

| 检查项 | 状态 | 说明 |
|--------|------|------|
| SQL 注入 | ✅ 安全 | 全部 573 行 Repository 代码使用参数化查询 `?` |
| 文件路径注入 | ✅ 安全 | 路径基于 `workspace_root` 拼接，`import_project` 使用 `os.path.abspath` 规范化 |
| 原子写入 | ✅ 安全 | temp + `os.replace` + `os.fsync` 模式 |
| 乐观锁 | ✅ 安全 | `expected_mtime` 防 lost update，`read_file_snapshot` 双重校验 |
| 敏感信息 | ✅ 安全 | `.env_template` 仅 17 字节，无硬编码密钥 |
| 外键约束 | ✅ 开启 | `PRAGMA foreign_keys=ON`，`ON DELETE CASCADE` 保证级联删除 |
| 审计追踪 | ✅ 完善 | `audit.py` 独立记录关键操作，保留 90 天 |
| YAML 解析 | ✅ 安全 | 全部使用 `yaml.safe_load`（非 `yaml.load`） |
| JSON 解析 | ✅ 安全 | 标准 `json.load`，无 `eval` |
| 临时文件清理 | ✅ 安全 | `write_file` 在异常时 `os.unlink(tmp_path)` 清理临时文件 |
| 编码安全 | ✅ 安全 | 全部使用 `encoding="utf-8"` 明确指定编码 |

---

## 五、文档完整性检查

| 文档 | 状态 | 说明 |
|------|------|------|
| `README.md` | ✅ 完善 | 16KB，含安装/使用/架构/贡献指南 |
| `CHANGELOG.md` | ✅ 完善 | 77KB，记录从 V0.1.0 到 V0.9.0 的完整变更历史 |
| `docs/里程碑迭代计划_V2.1.md` | ✅ 完善 | 24KB，M0-M5 迭代计划详尽 |
| `docs/归档索引.md` | ✅ 完善 | 4.8KB，文档归档索引 |
| `09_整改项/README.md` | ✅ 完善 | 6KB，整改项目录说明 |
| `09_整改项/remediation_plan.md` | ✅ 完善 | 12KB，整改计划 |
| `09_整改项/M2~M4 Landing Plans` | ✅ 完善 | 4 个 Landing Plan 文档 |
| `pyproject.toml` | ✅ 完善 | 构建/依赖/测试/工具全配置 |
| `.pre-commit-config.yaml` | ✅ 完善 | pre-commit hooks 配置 |
| `Taskfile.yml` | ✅ 完善 | Task runner 配置 |
| `PM_SESSION_SW-2026-008.md` | ⚠️ 偏大 | 147KB，建议定期归档旧内容 |

---

## 六、测试体系评估

| 维度 | 状态 | 说明 |
|------|------|------|
| 测试文件数 | 117 | 覆盖 17 个子模块 |
| 测试标记 | 6 种 | `gui`/`cli`/`smoke`/`unit`/`integration`/`slow` |
| Coverage 配置 | ✅ | `--cov=auto_pm/`，HTML+XML+JUnit 三格式输出 |
| 测试隔离 | ✅ | `pytest-env` 设置 `ENVIRONMENT=test` |
| 测试 fixtures | ✅ | `conftest.py` 2.8KB + `tests/fakes/` 目录提供 Fake 实现 |
| 健康检查 | ✅ | `test_fixture_health.py` 15.5KB，专门验证 fixture 正确性 |
| Bug 回归测试 | ✅ | 4 个专项 Bug 回归测试文件（bug1~bug5） |
| 冒烟测试 | ✅ | `test_smoke.py` 9.3KB，核心功能快速验证 |
| PM_SESSION 大小监控 | ✅ | `test_pm_session_size.py` 4.4KB，防止 PM_SESSION 过大 |

---

## 七、总结优先级排序

| 优先级 | 编号 | 问题 | 影响 | 工作量 |
|--------|------|------|------|--------|
| 🔴 P0 | #2 | DB 连接泄漏风险 | 长时间运行可能耗尽资源 | 小 |
| 🔴 P0 | #1 | `get_project()` 全量扫描 | 每次单项目查询都扫全盘 | 小 |
| 🔴 P1 | #5 | `setup_logger` 硬编码绕过配置 | 用户配置 DEBUG 不生效 | 中 |
| 🔴 P1 | #3 | 31 处宽泛 `except Exception` | 掩盖真实错误 | 中 |
| 🔴 P1 | #4 | `_get_project_mtime` 三处重复 | DRY 违反，维护一致性风险 | 小 |
| 🟡 P2 | #11 | 5 个临时文件已入库 | 仓库整洁度 + 仓库体积 (+1MB) | 小 |
| 🟡 P2 | #10 | 17 个 TODO 未闭环 | 功能遗漏风险 | 需评估 |
| 🟡 P2 | #7 | `Optional` 与 `| None` 混用 | 风格不统一 | 小 |
| 🟡 P2 | #8 | Facade 参数 `Any`（11 处） | 类型检查缺失 | 中 |
| 🟡 P2 | #9 | `FacadeRegistry` 类型不安全 | 运行时错误发现晚 | 小 |
| 🟡 P2 | #12 | ProjectInfo 原地修改属性 | 未来 frozen=True 迁移障碍 | 中 |
| 🟡 P2 | #17 | `.gitignore` 规则不完整 | 新文件可能误入库 | 小 |
| 🟡 P3 | #6 | `parser.py` 过大（837 行） | 可维护性 | 大 |
| 🟡 P3 | #18 | `change_service.py` 偏大（887 行） | 可维护性 | 大 |
| 🟢 P3 | #13 | Ruff 规则集偏保守 | 潜在 bug 漏检 | 小 |
| 🟢 P3 | #14 | 配置类功能单薄 | 硬编码分散 | 中 |
| 🟢 P4 | #15 | `os.path` 与 `pathlib` 混用 | 风格不统一 | 大 |
| 🟢 P4 | #16 | 变更模型文件名歧义 | 概念位置模糊 | 小 |
| 🟢 P4 | #19 | 7 个冗余向后兼容委托方法 | 代码冗余 | 小 |

---

## 八、相比首版诊断的变化

| 维度 | 首版（2026-07-07） | 第二版（2026-07-08） |
|------|-------------------|---------------------|
| 诊断范围 | 核心文件抽查 | 全部 125 源文件 + 117 测试文件逐文件审查 |
| 发现问题数 | 13 项 | 19 项（新增 #4/#5/#12/#17/#18/#19，#11 精确化） |
| `except Exception` | "23 个文件" | 精确定位：31 处，涉及 15 个文件 |
| `Optional` 统计 | 未量化 | 精确统计：48+ 处 |
| Facade `Any` | 2 处示例 | 完整统计：11 处，涉及 5 个 Facade |
| 根目录文件 | 猜测入库状态 | `git ls-files` 验证：5 个已入库 |
| 代码量 | "~50+ 源文件，~15k+ 行" | 精确：125 文件，20,212 行 |
| 新增发现 | — | setup_logger 硬编码绕过配置 (#5)、_get_project_mtime 重复 (#4)、ProjectInfo 原地修改 (#12)、.gitignore 不完整 (#17)、change_service.py 偏大 (#18)、冗余委托方法 (#19) |

---

> [!TIP]
> **立即可做的 Quick Wins**（工作量小、收益高）：
> 1. 修复 `get_project()` 优先走缓存（5 行代码改动）
> 2. `DatabaseManager` 添加连接复用（20 行改动）
> 3. `sync.py._get_project_mtime` 改为调用 `ProjectScanner.get_project_mtime`（3 行改动）
> 4. 清理根目录临时文件 + 更新 `.gitignore`（5 分钟）
> 5. 统一 `Optional[X]` → `X | None`（全局替换）
