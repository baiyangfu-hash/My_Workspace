# 里程碑迭代计划 V2.1

> **状态标识：V0.5.0 收口后已更新进度表 + 新增长期路线图（2026-07-01）**
> 当前状态真源请以 `PM_SESSION_SW-2026-008.md` 为准。
> V0.5.0+ 长期路线图见 §8（V0.5.x 稳定期 Week 1-4 + V0.6.0 长期路线 Week 5-8+ + V0.7.0+ 更长期方向）。

> 项目：SW-2026-008 auto-pm 自动化项目管理工具
> 版本：V2.1 计划（V0.5.0 收口时更新进度表 + 新增长期路线图）
> 创建日期：2026-06-22
> 最近更新：2026-07-01（V0.5.0 版本统一收口：进度表更新到实际完成度 26/27 + 新增 §8 V0.5.0+ 长期路线图）
> 基线：V0.5.0 已发布（pyproject 0.5.0；CHANGELOG [0.5.0] - 2026-07-01；全量回归 1477 passed 6 skipped 0 failed）
> 关联文档：[PM_SESSION_SW-2026-008.md](PM_SESSION_SW-2026-008.md)、[CHANGELOG.md](CHANGELOG.md)、[00_项目基础信息/006_技术债评估报告.md](00_项目基础信息/006_技术债评估报告.md) §12 长期治理建议

## 0. 背景与目标

### 0.1 背景

V2.0.1-A/C 完成后，项目存在三类问题：

1. **文档与代码不一致**：6 项严重不一致（版本号、CLI 参数、UI 组件等）
2. **架构技术债**：2 个上帝类（ProjectService 570 行、ChangeService 890 行）、UI 跨层访问 DB/plc
3. **能力缺口**：8 项 P1 缺口（SubstanceChecker、python 命令、CLI 参数补全等）

### 0.2 目标

通过 4 个里程碑、27 个迭代，实现：

- 文档与代码 100% 一致
- 架构分层清晰，无上帝类，无跨层调用
- INT 声明的接口 100% 实现
- 测试覆盖率 ≥85%
- PRD 路线图 V2.1~V2.3 交付

### 0.3 路线图

```
M1 稳基座（文档/版本对齐）     →  M2 补能力（CLI/Service 缺口）
        ↓                                ↓
M3 重架构（拆上帝类+接口抽象）  →  M4 扩功能（V2.1~V2.3 路线图）
```

---

## 1. M1：稳基座里程碑（文档与版本对齐）

**目标**：消除文档与代码的严重不一致，建立可信的文档基线。
**风险**：低（纯文档对齐，无代码逻辑变更）
**前置条件**：无

### M1-Iter1：同步 pyproject.toml 版本号

| 项 | 内容 |
|----|------|
| **任务** | 将 `pyproject.toml` version 从 0.1.0 同步至 0.2.1 |
| **涉及文件** | `pyproject.toml` |
| **验收标准** | `pyproject.toml` version="0.2.1"，与 CHANGELOG.md [0.2.1] 一致 |
| **测试方式** | `pip show auto-pm` 显示 0.2.1；`auto-pm --version` 显示 0.2.1 |

### M1-Iter2：修正 INT 文档（标注待实现项）

| 项 | 内容 |
|----|------|
| **任务** | 修正 INT 文档中"已实现"但实际未实现的接口声明，标注为"待实现" |
| **涉及文件** | `00_项目基础信息/002_接口文档_INT.md` |
| **修正项** | S2: `gui --role` 标注"待实现"；S3: `project create/edit --business-line` 标注"待实现"；M1: `ProjectService.import_project/classify_project/search_projects` 标注"待实现"；M2: `project import` 缓存写入标注"待实现"；M3: `project list --phase/--search/--json` 标注"待实现" |
| **验收标准** | INT 文档所有"待实现"项明确标注，无"已实现"假声明 |
| **测试方式** | 人工审阅 INT 文档 |

### M1-Iter3：更新 README 项目结构章节

| 项 | 内容 |
|----|------|
| **任务** | 更新 README 项目结构描述，对齐实际 8 子模块；python 命令标注"计划 V1.2.0" |
| **涉及文件** | `README.md` |
| **修正项** | L1: `ui/` 子模块描述从"views/widgets/dialogs/models"改为实际 8 子模块（workspace/global_pages/widgets/dialogs/models/navigation/project_list/change_center）；M4: python init/check 标注"占位，计划 V1.2.0 实现" |
| **验收标准** | README 项目结构与 `auto_pm/ui/` 实际目录一致 |
| **测试方式** | 人工审阅 README |

### M1-Iter4：更新 TEC 7.1 节 V2.0.1-A/C 修复说明

| 项 | 内容 |
|----|------|
| **任务** | 在 TEC 7.1 节补充 V2.0.1-A/C 已完成的修复说明 |
| **涉及文件** | `00_项目基础信息/004_技术方案文档_TEC.md` |
| **补充内容** | V2.0.1-A: site 模块 GBK 编码修复（PYTHONUTF8=1）；V2.0.1-C: 042/016 规范对齐（19 项冲突修复） |
| **验收标准** | TEC 7.1 节包含 V2.0.1-A/C 修复说明 |
| **测试方式** | 人工审阅 TEC |

### M1-Iter5：同步 spec/checklist 至 V2.0.1 实际状态

| 项 | 内容 |
|----|------|
| **任务** | 同步 spec.md 和 checklist.md 至 V2.0.1-A/C 修复后的实际状态 |
| **涉及文件** | `.trae/specs/rebuild-auto-pm-v2-unified/spec.md`、`.trae/specs/rebuild-auto-pm-v2-unified/checklist.md` |
| **同步内容** | 标注 V2.0.1-A/C 已修复项；更新验收检查点状态 |
| **验收标准** | spec/checklist 反映 V2.0.1 实际状态 |
| **测试方式** | 人工审阅 |

### M1-Iter6：测试计划添加过时用例标注

| 项 | 内容 |
|----|------|
| **任务** | 在 V2.0-全功能自动化测试计划.md 头部添加角色系统用例过时标注 |
| **涉及文件** | `09_整改项/V2.0-全功能自动化测试计划.md` |
| **标注内容** | 角色系统相关用例（GUI-MW-006~010、GUI-ROLE-001~009、GUI-WS-010~013、E2E-ROLE-001~004）已过时，V2.0 已删除角色系统 |
| **验收标准** | 测试计划头部有过时标注 |
| **测试方式** | 人工审阅 |

### M1 出口标准

- [ ] `pyproject.toml` 版本号 0.2.1
- [ ] INT 文档无"已实现"假声明
- [ ] README 项目结构与实际一致
- [ ] TEC 7.1 节含 V2.0.1-A/C 修复说明
- [ ] spec/checklist 反映 V2.0.1 实际状态
- [ ] 测试计划有过时标注
- [ ] 全量测试无回归（778+ passed）

---

## 2. M2：补能力里程碑（CLI/Service 缺口）

**目标**：补齐 INT 声明但未实现的核心能力，消除"声明 vs 实现"鸿沟。
**风险**：中（涉及 CLI 参数、Service 方法新增）
**前置条件**：M1 完成

### M2-Iter1：project create/edit --business-line 参数

| 项 | 内容 |
|----|------|
| **任务** | 为 `project create` 和 `project edit` 命令补充 `--business-line` 参数 |
| **涉及文件** | `auto_pm/cli/project.py`、`auto_pm/core/project_service.py` |
| **实现细节** | `cmd_create` 新增 `--business-line/-bl` 选项（SW/DJ/ZD/XT/WX）；`cmd_edit` 新增 `--business-line/-bl` 选项；`ProjectService.update_project_meta` 支持 business_line 字段 |
| **验收标准** | `auto-pm project create --stack plc --id TEST-001 --name 测试 --business-line SW` 可创建；`auto-pm project edit TEST-001 --business-line DJ` 可更新 |
| **测试方式** | 新增单元测试 `tests/cli/test_project.py::test_create_with_business_line`、`test_edit_business_line` |

### M2-Iter2：project list --phase/--search/--json 参数

| 项 | 内容 |
|----|------|
| **任务** | 为 `project list` 命令补充 `--phase`/`--search`/`--json` 参数 |
| **涉及文件** | `auto_pm/cli/project.py` |
| **实现细节** | `--phase` 按阶段筛选（developing/commissioning/production/archived）；`--search` 关键字搜索（编号/名称/描述）；`--json` 输出 JSON 格式 |
| **验收标准** | 三参数均可正常工作；`--json` 输出合法 JSON |
| **测试方式** | 新增单元测试 |

### M2-Iter3：project import --force + 写缓存

| 项 | 内容 |
|----|------|
| **任务** | 为 `project import` 补充 `--force` 选项，并在导入后自动 sync_to_cache |
| **涉及文件** | `auto_pm/cli/project.py` |
| **实现细节** | `--force` 强制覆盖已存在的目标目录；导入完成后调用 `ProjectService.sync_to_cache(force_full=True)` |
| **验收标准** | `--force` 可覆盖；导入后 DB 缓存自动更新 |
| **测试方式** | 新增单元测试 |

### M2-Iter4：ProjectService 三方法实现

| 项 | 内容 |
|----|------|
| **任务** | 实现 `import_project`/`classify_project`/`search_projects` 三方法 |
| **涉及文件** | `auto_pm/core/project_service.py` |
| **实现细节** | `import_project(src, business_line, move, force)`：导入项目并写缓存；`classify_project(project_id, business_line)`：更新业务线分类；`search_projects(keyword)`：关键字搜索 |
| **验收标准** | 三方法可用，单元测试覆盖 |
| **测试方式** | 新增单元测试 `tests/core/test_project_service.py` |

### M2-Iter5：gui --role 参数

| 项 | 内容 |
|----|------|
| **任务** | 实现 `gui --role` 参数，或修正 INT 标注为"不实现" |
| **涉及文件** | `auto_pm/cli/gui.py`、`00_项目基础信息/002_接口文档_INT.md` |
| **决策** | V2.0 已删除角色系统，建议修正 INT 标注为"不实现（V2.0 已删除角色系统）" |
| **验收标准** | INT 文档与代码一致 |
| **测试方式** | 人工审阅 |

### M2-Iter6：python init/check 实现

| 项 | 内容 |
|----|------|
| **任务** | 实现 `python init` 和 `python check` 命令 |
| **涉及文件** | `auto_pm/cli/python/__init__.py`、`auto_pm/core/template_service.py` |
| **实现细节** | `python init`：调用 python-tool Copier 模板创建 Python 项目骨架；`python check`：检查 Python 项目规范（210/211/220） |
| **验收标准** | `auto-pm python init SW-2026-009 --name 测试` 可创建项目；`python check` 可检查 |
| **测试方式** | 新增单元测试 |

### M2-Iter7：SubstanceChecker 实质化检查

| 项 | 内容 |
|----|------|
| **任务** | 实现 `SubstanceChecker` 类，执行文档实质化检查 |
| **涉及文件** | `auto_pm/plc/checker.py` |
| **实现细节** | 检查 PRD/DSN/INT/TEC 文档是否为空壳（字数 < 阈值）、章节是否完整、关键内容是否填充 |
| **验收标准** | `auto-pm plc check <ID>` 可检测文档实质化问题 |
| **测试方式** | 新增单元测试 `tests/plc/test_substance_checker.py` |

### M2-Iter8：修复 KNOWN-1（ChangeService 时序 bug）

| 项 | 内容 |
|----|------|
| **任务** | 修复 ChangeService `transition_status(completed)` 临时文件校验时序 bug |
| **涉及文件** | `auto_pm/change/change_service.py` |
| **实现细节** | 调整临时文件创建与校验的顺序，确保校验在文件存在时执行 |
| **验收标准** | `transition_status(completed)` 流程稳定，无时序错误 |
| **测试方式** | 新增回归测试 |

### M2 出口标准

- [ ] CLI 参数补全（--business-line/--phase/--search/--json/--force）
- [ ] ProjectService 三方法实现
- [ ] python init/check 实现
- [ ] SubstanceChecker 实现
- [ ] KNOWN-1 修复
- [ ] 测试覆盖率 ≥85%
- [ ] 全量测试通过

---

## 3. M3：重架构里程碑（技术债清理）

**目标**：拆分上帝类，引入接口抽象，消除跨层调用。
**风险**：高（涉及核心架构重构）
**前置条件**：M2 完成

### M3-Iter1：拆分 ProjectService

| 项 | 内容 |
|----|------|
| **任务** | 将 `ProjectService`（570 行）拆分为 4 个类 |
| **涉及文件** | `auto_pm/core/project_service.py` → `project_scanner.py` + `project_metadata_writer.py` + `project_sync_coordinator.py` + `project_service.py`（精简版） |
| **拆分方案** | `ProjectScanner`（扫描+识别）、`ProjectMetadataWriter`（写 .copier-answers.yml/.plc.json）、`ProjectSyncCoordinator`（DB 缓存同步）、`ProjectService`（CRUD 编排，< 200 行） |
| **验收标准** | 单类 < 200 行，单一职责，全量测试通过 |
| **测试方式** | 现有测试 + 新增拆分后类的单元测试 |

### M3-Iter2：拆分 ChangeService

| 项 | 内容 |
|----|------|
| **任务** | 将 `ChangeService`（890 行）拆分为 4 个类 |
| **涉及文件** | `auto_pm/change/change_service.py` → `change_status_transitioner.py` + `change_markdown_editor.py` + `change_number_generator.py` + `change_service.py`（精简版） |
| **拆分方案** | `ChangeStatusTransitioner`（状态机+门禁）、`ChangeMarkdownEditor`（章节正则操作）、`ChangeNumberGenerator`（编号生成）、`ChangeService`（CRUD 编排，< 200 行） |
| **验收标准** | 单类 < 250 行，状态机独立可测试，全量测试通过 |
| **测试方式** | 现有测试 + 新增状态机单元测试 |

### M3-Iter3：引入 Protocol 接口

| 项 | 内容 |
|----|------|
| **任务** | 为 Service/Repository 定义 Protocol 接口 |
| **涉及文件** | 新增 `auto_pm/core/protocols.py` |
| **接口定义** | `ProjectServiceProtocol`、`ChangeServiceProtocol`、`TemplateServiceProtocol`、`ReportServiceProtocol`、`RepositoryProtocol` |
| **验收标准** | Service/Repository 实现对应 Protocol；`ReportService.__init__` 用 Protocol 替代 `object` |
| **测试方式** | mypy 类型检查通过 |

### M3-Iter4：新增 PlcService

| 项 | 内容 |
|----|------|
| **任务** | 新增 `PlcService` 封装 `PlcChecker`/`PlcRepairer` |
| **涉及文件** | 新增 `auto_pm/core/plc_service.py` |
| **实现细节** | `PlcService.check_project(path)`、`PlcService.repair_project(path, dry_run, rename_confirm)`、`PlcService.standardize_docs(path, apply)` |
| **验收标准** | UI 通过 `PlcService` 调用，不直接访问 `plc` 模块 |
| **测试方式** | 新增单元测试 |

### M3-Iter5：UI 层去除直接 DB 访问

| 项 | 内容 |
|----|------|
| **任务** | 将 UI 层的 DB 初始化和 ScanLogRepository 查询封装到 Service |
| **涉及文件** | `auto_pm/ui/main_window.py`、`auto_pm/app_context.py` |
| **实现细节** | DB 初始化移到 `AppContext`；`ScanLogRepository.get_latest` 封装到 `ProjectService.get_last_scan_time` |
| **验收标准** | `ui/` 模块无 `from auto_pm.db import` 导入 |
| **测试方式** | grep 验证 + 全量测试 |

### M3-Iter6：统一路径约定

| 项 | 内容 |
|----|------|
| **任务** | 统一路径约定到 `path_resolver.py`，消除 3 处硬编码 |
| **涉及文件** | `auto_pm/change/path_resolver.py`、`auto_pm/change/change_service.py`、`auto_pm/db/sync.py` |
| **实现细节** | `path_resolver.py` 作为单一真源；`change_service`/`sync.py` 引用常量 |
| **验收标准** | 路径约定单一定义，无硬编码 |
| **测试方式** | grep 验证 |

### M3-Iter7：统一模板名映射和业务线选项

| 项 | 内容 |
|----|------|
| **任务** | 统一模板名映射和业务线选项到单一来源 |
| **涉及文件** | 新增 `auto_pm/constants.py`；`auto_pm/models/enums.py` |
| **实现细节** | `STACK_TEMPLATE_MAP = {"plc": "plc-standard", "python": "python-tool"}` 单一定义；业务线选项引用 `models.enums` |
| **验收标准** | 无硬编码重复 |
| **测试方式** | grep 验证 |

### M3-Iter8：Markdown 状态结构化（可选）

| 项 | 内容 |
|----|------|
| **任务** | 将变更单状态持久化改用 YAML front matter，减少正则脆弱性 |
| **涉及文件** | `auto_pm/change/change_service.py`、`auto_pm/change/parser.py`、`auto_pm/change/generator.py` |
| **实现细节** | 变更单 Markdown 头部添加 YAML front matter 存储状态；提供迁移脚本 |
| **验收标准** | 状态读写不依赖正则；现有变更单可迁移 |
| **测试方式** | 单元测试 + 迁移测试 |
| **备注** | 此迭代为可选项，视 M3 前期迭代进度决定是否执行 |

### M3 出口标准

- [ ] ProjectService 拆分完成（单类 < 200 行）
- [ ] ChangeService 拆分完成（单类 < 250 行）
- [ ] Protocol 接口定义完成
- [ ] PlcService 封装完成
- [ ] UI 层无直接 DB 访问
- [ ] 路径约定统一
- [ ] 模板名映射/业务线选项统一
- [ ] 全量测试通过，覆盖率 ≥85%

---

## 4. M4：扩功能里程碑（V2.1~V2.3 路线图）

**目标**：按 PRD 路线图交付剩余功能。
**风险**：中（新功能开发）
**前置条件**：M3 完成

### M4-Iter1：变量表 Tab（vartable）

| 项 | 内容 |
|----|------|
| **任务** | 实现工作区变量表 Tab |
| **涉及文件** | `auto_pm/ui/workspace/workspace_view.py`、新增 `auto_pm/ui/workspace/vartable_tab.py` |
| **验收标准** | 工作区 5 Tab 完整 |
| **版本** | V2.3 |

### M4-Iter2：ReportService 多报告类型

| 项 | 内容 |
|----|------|
| **任务** | 扩展 ReportService，支持多报告类型 |
| **涉及文件** | `auto_pm/core/report_service.py` |
| **实现细节** | 项目报告/变更报告/规范报告/扫描报告 |
| **验收标准** | 4 种报告可生成 |
| **版本** | V2.x |

### M4-Iter3：SpecCenterView 完整规范管理

| 项 | 内容 |
|----|------|
| **任务** | 扩展 SpecCenterView，支持规范索引/检索/对比 |
| **涉及文件** | `auto_pm/ui/global_pages/spec_center.py` |
| **验收标准** | 规范中心可索引/检索/对比 |
| **版本** | V2.x |

### M4-Iter4：验收流程增强（V2.2.0）

| 项 | 内容 |
|----|------|
| **任务** | 完整实现 `implementing → pending_acceptance → accepting → completed` 验收流程 |
| **涉及文件** | `auto_pm/change/change_service.py` |
| **验收标准** | 验收流程完整，含返工路径 |
| **版本** | V2.2 |

### M4-Iter5：归档流程（V2.3.0）

| 项 | 内容 |
|----|------|
| **任务** | 完整实现 `completed → archived` 归档流程 |
| **涉及文件** | `auto_pm/change/change_service.py` |
| **验收标准** | 归档流程完整 |
| **版本** | V2.3 |

### M4 出口标准

- [ ] 变量表 Tab 交付
- [ ] ReportService 多报告类型
- [ ] SpecCenterView 完整
- [ ] 验收流程增强
- [ ] 归档流程完整
- [ ] 全量测试通过

---

## 5. 测试策略

### 5.1 测试分层

| 层级 | 测试类型 | 工具 | 频率 |
|------|----------|------|------|
| L1 | 单元测试 | pytest | 每次提交 |
| L2 | 集成测试 | pytest | 每次迭代 |
| L3 | GUI 交互测试 | pytest-qt + QTest | 每个里程碑 |
| L4 | 端到端测试 | scripts/gui_plc_full_test.py | 每个里程碑 |

### 5.2 测试脚本工具更新计划

在 M1 完成后、M2 开始前，更新测试脚本工具：

1. **更新 `scripts/gui_plc_full_test.py`**：
   - 移除角色系统相关测试步骤
   - 新增 business_line 参数测试
   - 新增 python init/check 测试

2. **新增 `scripts/smoke_test.py`**：
   - CLI 冒烟测试（所有命令 --help）
   - GUI 冒烟测试（启动 + 关闭）
   - DB 初始化测试

3. **更新 `tests/conftest.py`**：
   - 修复 `tmp_workspace` fixture 的目录结构（对齐 LSP-907）
   - 新增 `tmp_python_project` fixture

4. **更新 `Taskfile.yml`**：
   - 修复 Windows 兼容性（`.venv/bin` → `.venv/Scripts`）
   - 新增 `smoke` 任务

### 5.3 质量门禁

每个迭代必须通过：

- `ruff check auto_pm/ tests/`
- `ruff format --check auto_pm/ tests/`
- `mypy auto_pm/`
- `pytest tests/ -x`
- 测试覆盖率 ≥85%

---

## 6. 风险管理

### 6.1 风险清单

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| M3 拆分上帝类引入回归 | 高 | 高 | 拆分前补齐测试覆盖；小步拆分，每步回归 |
| M2-Iter6 python 命令需求不明确 | 中 | 中 | M1 阶段澄清需求 |
| M3-Iter8 Markdown 结构化破坏现有格式 | 中 | 高 | 提供迁移脚本；保留兼容模式 |
| 测试脚本更新引入 flaky | 中 | 中 | 修复 isVisible() 误用 |

### 6.2 回滚策略

- 每个迭代独立提交，便于回滚
- M3 拆分类时保留旧类作为兼容层（一个迭代后删除）

---

## 7. 执行进度跟踪

### 7.1 进度表（V0.5.0 收口时更新，2026-07-01）

| 里程碑 | 迭代数 | 已完成 | 进行中 | 待开始 | 状态 |
|--------|--------|--------|--------|--------|------|
| M1 稳基座 | 6 | 6 | 0 | 0 | ✅ 已完成（V0.3.x） |
| M2 补能力 | 8 | 8 | 0 | 0 | ✅ 已完成（V0.3.x） |
| M3 重架构 | 8 | 8 | 0 | 0 | ✅ 已完成（V0.3.x~V0.4.x） |
| M4 扩功能 | 5 | 4 | 0 | 1 | 🔄 部分完成（4/5 已完成） |
| **合计** | **27** | **26** | **0** | **1** | **✅ 96% 完成** |

> **M4 详细进度**：
> - ✅ M4-Iter1 变量表 Tab（vartable）：V2.3 Week4 已完成（2026-07-01）
> - ⏳ M4-Iter2 ReportService 多报告类型：未完成（V0.5.x+ 评估）
> - ✅ M4-Iter3 SpecCenterView 完整规范管理：V2.2 Week3 已完成（2026-06-30）
> - ✅ M4-Iter4 验收流程增强（V2.2.0）：已完成（9 步状态流转 draft→archived 全部实现）
> - ✅ M4-Iter5 归档流程（V2.3.0）：已完成（archived 状态已实现）

### 7.2 进度记录

| 日期 | 迭代 | 状态 | 备注 |
|------|------|------|------|
| 2026-06-22 | - | 计划创建 | 创建里程碑迭代计划文档 |
| 2026-06-25 | M1 | ✅ 已完成 | M1 稳基座 6 迭代全部完成（V0.3.x 期间） |
| 2026-06-26 | M2 | ✅ 已完成 | M2 补能力 8 迭代全部完成（V0.3.x 期间） |
| 2026-06-29 | M3 | ✅ 已完成 | M3 重架构 8 迭代全部完成（V0.3.x~V0.4.x 期间，含上帝类拆分 + Protocol 接口 + UI 层去除 DB 访问） |
| 2026-06-30 | M4-Iter3 | ✅ 已完成 | SpecCenterView 完整规范管理（V2.2 Week3 GUI 规范中心页改造） |
| 2026-06-30 | M4-Iter4 | ✅ 已完成 | 验收流程增强（9 步状态流转完整实现，CHG-SCPT-2026-078 走完整 9 步） |
| 2026-06-30 | M4-Iter5 | ✅ 已完成 | 归档流程（archived 状态已实现，CHG-SCPT-2026-001 已归档） |
| 2026-07-01 | M4-Iter1 | ✅ 已完成 | 变量表 Tab（V2.3 Week4：VariableTableModel + VariableTableEditor + VartableTab + 33 测试） |
| 2026-07-01 | M4-Iter2 | ⏳ 待开始 | ReportService 多报告类型（V0.5.x+ 评估优先级） |

---

## 8. V0.5.0+ 长期路线图（6 周+ 长期计划）

> 本章节为 V0.5.0 收口时新增的长期路线图，作为后续版本演进的参考基线。
> **精简原则**：PM 文档适当精简，聚焦关键决策与产出，避免过度细化。

### 8.1 V0.5.x 稳定期（Week 1-4，2026-07-02 ~ 2026-07-29）

**目标**：V0.5.0 收口后的稳定期，聚焦真实项目验证 + 已知限制评估。

| 周次 | 治理重点 | 预期产出 | 决策点 |
|------|----------|----------|--------|
| Week 1 | 5 格式 Parser 真实样本评估 | 评估报告（DJ-2026-005 之外引入 1-2 个真实项目样本验证 Parser 鲁棒性） | 是否需要新增 Parser？FormatDetector 识别策略是否需调整？ |
| Week 2 | VartableTab 角色权限评估 | 决策记录（是否移除 GUI 层软约束，简化为"全部可编辑"） | 是否移除角色权限？是否引入后端权限系统？ |
| Week 3 | VariableConverter schema 版本管理评估 | 评估报告（是否引入 `converter_schema_version: 1` + 迁移脚本） | 是否需要 schema 版本管理？ |
| Week 4 | 测试执行耗时跟踪 + M4-Iter2 ReportService 优先级评估 | 性能基线 + 优先级决策 | ReportService 是否启动？测试规模是否超过 2000 需重新评估 pytest-xdist？ |

**稳定期出口标准**：
- V0.5.0 收口基线稳定运行 4 周，无 Critical bug
- 5 格式 Parser 真实样本评估完成
- VartableTab 角色权限决策完成
- M4-Iter2 ReportService 优先级明确

### 8.2 V0.6.0 长期路线（Week 5-8+，2026-07-30 ~ 2026-08-26+）

**目标**：根据 V0.5.x 稳定期评估结果，启动长期路线。

| 周次 | 治理重点 | 预期产出 | 依赖 |
|------|----------|----------|------|
| Week 5-6 | M4-Iter2 ReportService 多报告类型（若 V0.5.x 评估启动） | 4 种报告类型（项目报告/变更报告/规范报告/扫描报告） + 单元测试 | V0.5.x Week 4 优先级决策 |
| Week 7 | VartableTab 角色权限治理（若 V0.5.x 评估移除） | 简化 VartableTab 为"全部可编辑" + 回归测试 | V0.5.x Week 2 决策 |
| Week 8 | dogfooding 闭环 + V0.6.0 版本统一收口 | CHG-*.md 闭环 + pyproject 0.5.0→0.6.0 + CHANGELOG [0.6.0] + 文档对齐 | 前 3 周完成 |

**长期路线出口标准**：
- M4-Iter2 ReportService 完成或明确不启动
- VartableTab 角色权限治理完成
- V0.6.0 版本统一收口
- 全量回归无回归

### 8.3 更长期（V0.7.0+，2026-09+）

**潜在方向**（根据 V0.6.0 收口时评估）：
- **新格式 Parser 扩展**：若 V0.5.x 评估发现需要支持新格式，补充对应 Parser + 测试
- **VariableConverter schema 版本管理**：若 V0.5.x 评估决定引入，提供迁移脚本
- **测试并行化重评估**：若测试规模超过 2000，重新评估 pytest-xdist
- **后端权限系统**：若 V0.5.x 评估决定引入，设计权限模型 + 实现
- **持续 dogfooding 闭环**：每个版本至少 1 次 CHG-*.md 闭环

### 8.4 长期治理原则

1. **小步快跑**：每个版本只聚焦 1-2 个主线，避免大规模重构
2. **验证优先**：所有变更必须通过 `pytest --tb=long` + ruff + mypy 三重门禁
3. **沉淀经验**：关键技术突破沉淀到 `006_技术债评估报告.md` §12 长期治理建议
4. **预防为主**：新增代码遵循预防机制（类型标注/docstring/dogfooding），防止技术债再产生
5. **长期视角**：技术债治理是长期持续过程，每个版本评估和治理

---

## 9. 附录

### 9.1 相关文档索引

- [PM_SESSION_SW-2026-008.md](PM_SESSION_SW-2026-008.md) - 项目会话记录
- [CHANGELOG.md](CHANGELOG.md) - 变更日志
- [00_项目基础信息/001_产品需求文档_PRD.md](00_项目基础信息/001_产品需求文档_PRD.md) - PRD
- [00_项目基础信息/002_接口文档_INT.md](00_项目基础信息/002_接口文档_INT.md) - INT
- [00_项目基础信息/003_详细设计说明书_DSN.md](00_项目基础信息/003_详细设计说明书_DSN.md) - DSN
- [00_项目基础信息/004_技术方案文档_TEC.md](00_项目基础信息/004_技术方案文档_TEC.md) - TEC
- [09_整改项/2026-06-30_项目全量审查报告.md](09_整改项/2026-06-30_项目全量审查报告.md) - V0.4.2 整改批次最终审查报告
- [09_整改项/README.md](09_整改项/README.md) - 09_整改项目录索引

### 9.2 术语表

| 术语 | 含义 |
|------|------|
| M1~M4 | 里程碑 1~4 |
| Iter | 迭代（Iteration） |
| 上帝类 | 承担过多职责的类，违反单一职责原则 |
| Protocol | Python 类型协议，用于定义接口契约 |
| 跨层调用 | 违反分层架构的调用（如 UI 直接访问 DB） |
| VartableTab | GUI 变量表 Tab（V2.3 Week4 实现，QSplitter 三栏布局） |
| FormatDetector | 变量表格式自动识别器（三级识别：文件名→扩展名→内容特征） |
| Dogfooding | 自身使用自身产品的实践（auto-pm 使用 CHG-*.md 管理自身迭代） |
