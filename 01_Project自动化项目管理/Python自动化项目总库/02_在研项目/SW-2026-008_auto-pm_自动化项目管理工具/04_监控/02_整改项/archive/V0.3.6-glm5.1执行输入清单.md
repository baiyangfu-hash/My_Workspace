# SW-2026-008 auto-pm 交给 glm5.1 的执行输入清单

> 用途：给 `glm5.1` 直接作为执行输入，避免它误判当前基线、重复进入已完成里程碑，或继续沿用过期文档口径。
> 更新日期：2026-06-26
> 当前状态真源：`PM_SESSION_SW-2026-008.md`
> 当前执行总计划：`09_整改项/V0.3.0-项目落地执行总计划_重规划版.md`
> 当前问题判断依据：`09_整改项/V0.3.0-项目深度诊断与Dogfood专项报告.md`

---

## 1. 先读结论

你接手时，项目**不是**处于“准备进入 M1/M2/M3”的阶段。

当前真实基线是：

- `pyproject.toml`：`0.3.6`
- `CHANGELOG.md`：最新条目 `[0.3.6]`
- `PRD`：`V2.1.0`
- `PM_SESSION` 当前焦点：`V0.3.0 M3-4 全部完成，待启动 M4 Dogfooding 持续化`
- 当前主线判断：`M0.5`、`M1`、`M2`、`M3.5`、`M3-2`、`M3-3`、`M3-4` 均已完成

因此：

> **禁止**再回头把项目叙述成“先做 M0.5 / M1 / M2 / M3”。  
> 你当前应执行的是“文档收尾 + 元数据收口 + M4 持续化启动”。

---

## 2. 单一真源规则

执行过程中必须遵守以下真源层级：

1. **状态真源**：`PM_SESSION_SW-2026-008.md`
2. **执行总计划**：`09_整改项/V0.3.0-项目落地执行总计划_重规划版.md`
3. **问题判断依据**：`09_整改项/V0.3.0-项目深度诊断与Dogfood专项报告.md`
4. **当前迭代 spec**：`.trae/specs/v2.1-change-management-enhancement/spec.md`
5. **历史计划参考**：
   - `02_设计/里程碑迭代计划_V2.1.md`
   - `docs/里程碑迭代计划_V2.1.md`

注意：

- 两份 `里程碑迭代计划_V2.1.md` 已被降级为历史/镜像参考，**不得**再作为当前状态判断依据。
- 不允许新建独立状态文件替代 `PM_SESSION`。

---

## 3. 当前已确认事实

以下事实可以直接作为你的起点：

### 3.1 代码与功能

- `change` CLI 现已包含：`list / show / create / transition / edit`
- `ProjectScanner` 已兼容 `project_description`，并支持版本回退读取
- 台帐路径已按 PLC/Python 项目类型分流
- `verification_conclusion` 已改为规则校验，不再要求字面量 `全部通过`
- GUI 变更管理增强已推进到 `M3-4` 完成

### 3.2 仍然存在的问题

- `project show SW-2026-008` 的 `阶段` 仍显示为 `-`
- `mypy auto_pm` 当前实测通过
- `ruff check .` 当前实测仍有 `34` 个问题
- 高层文档虽然已大幅收口，但仍需按最新事实继续统一

### 3.3 本轮建议你优先处理的真实问题

1. 修复 `phase` 回退链路
2. 收口高层文档中的质量与版本口径
3. 启动 `M4 Dogfooding 持续化`

---

## 4. 本轮执行目标

本轮目标不是“新增大功能”，而是完成以下 3 件治理型工作：

### G1 文档收尾

把高层文档统一到当前真实基线：

- `0.3.6`
- `V2.1.0`
- 最近完整签字：`1155 passed, 1 skipped`
- 当前实测：`mypy auto_pm` 通过、`ruff check .` 仍有 34 个问题

### G2 元数据收口

让 `project show` 的 `阶段` 字段恢复可信，不再显示 `-`。

### G3 M4 启动

将 dogfood 从“已经演练过两轮”推进到“持续化机制正式开始”。

---

## 5. 推荐执行顺序

严格按下面顺序执行，不要跳步：

### Step 1：复核当前状态

先运行并记录结果：

```powershell
& "c:\Users\fubai\Desktop\My_Workspace\.venv\Scripts\Activate.ps1"
auto-pm -w "c:\Users\fubai\Desktop\My_Workspace" project show SW-2026-008
auto-pm -w "c:\Users\fubai\Desktop\My_Workspace" change list SW-2026-008
pytest -q
ruff check .
mypy auto_pm
```

要求：

- 不能只引用旧文档结论，必须拿到当前实测
- 如果本轮结果与文档不一致，以当前实测为准，并回写真源文档

### Step 2：修复 `phase` 问题

先选一种方案，不要两种都做：

方案 A：

- 在项目元数据中显式维护 `phase`

方案 B：

- 扩展 `ProjectScanner._read_phase_from_pm_session()`，使其能从当前 `PM_SESSION` 实际结构推导阶段

done_when：

- `auto-pm -w "<workspace>" project show SW-2026-008` 中 `阶段` 不再为 `-`

### Step 3：处理 `ruff 34` 既存问题

优先顺序：

1. `scripts/gui_plc_full_test.py`
2. `scripts/run_tests.py`
3. `tests/gui/test_17_edit_change_dialog.py`

done_when：

- `ruff check .` 返回 0 errors

### Step 4：回写文档真源

至少同步这些文档：

- `PM_SESSION_SW-2026-008.md`
- `.trae/specs/v2.1-change-management-enhancement/spec.md`
- `00_项目基础信息/006_技术债评估报告.md`
- `00_项目基础信息/001_产品需求文档_PRD.md`

要求：

- 版本号、测试数、质量口径一致
- 不能一处写 `ruff 0 errors`，另一处实测又保留 `34 errors`

### Step 5：启动 M4

以真实 CHG 为入口启动 `M4 Dogfooding 持续化`：

- 创建新 CHG
- 跑通本阶段必要状态流转
- 留下验证证据和回写记录

done_when：

- `PM_SESSION` 的 `next_focus` 不再只是“待启动 M4”
- 而是已经进入 `M4` 的实际执行态

---

## 6. 禁止事项

执行时禁止做下面这些事：

- 禁止把项目重新叙述为“尚未进入 M1/M2/M3”
- 禁止把两份 `里程碑迭代计划_V2.1.md` 当成当前状态真源
- 禁止只改代码不回写 `PM_SESSION`
- 禁止沿用旧的“全绿”口径而不做本轮实测
- 禁止新建独立状态文件替代 `PM_SESSION`

---

## 7. 每步完成后必须回写

至少回写以下 4 类信息：

1. `PM_SESSION §6 Implementation Log`
2. `PM_SESSION §7 Verification Log`
3. `PM_SESSION §8 Handoff Notes`
4. `PM_SESSION §9 Next Actions`

尤其注意：

- 必须明确写清楚“本轮实际验证结果”
- 必须区分“已验证”和“未验证”
- 不允许把未运行验证的推断写成既成事实

---

## 8. 你执行完后的交付标准

本轮交付完成时，应满足以下条件：

- `project show` 的 `阶段` 字段可信
- `ruff check .` 为 0 errors
- 高层文档口径统一到当前真实状态
- `PM_SESSION` 明确记录本轮实施与验证结果
- `M4` 已从“待启动”切换到“已启动并有证据”

---

## 9. 建议你优先阅读的文件

按顺序阅读：

1. `PM_SESSION_SW-2026-008.md`
2. `09_整改项/V0.3.0-项目深度诊断与Dogfood专项报告.md`
3. `09_整改项/V0.3.0-项目落地执行总计划_重规划版.md`
4. `.trae/specs/v2.1-change-management-enhancement/spec.md`
5. `00_项目基础信息/006_技术债评估报告.md`
6. `00_项目基础信息/001_产品需求文档_PRD.md`

---

## 10. 最短指令版

如果你只保留一句指令给 `glm5.1`，用下面这句：

> 以 `PM_SESSION_SW-2026-008.md` 为唯一状态真源，从当前真实基线 `V0.3.6 / M3-4 完成` 出发，先收口 `phase` 与 `ruff 34` 两个残留问题，再统一高层文档口径，最后正式启动 `M4 Dogfooding 持续化`，每步都必须回写 `PM_SESSION §6-§9`。
