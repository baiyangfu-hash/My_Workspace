# V0.4.2 第1周：DJ-2026-005 工程资产首版补齐方案

> 项目：SW-2026-008 auto-pm
> 日期：2026-06-29
> 模式：项目推进
> 状态真源：`PM_SESSION_SW-2026-008.md`
> 上游计划：`00_项目管理/03_执行过程/2026-06-29_V0.4.2-未来6周滚动计划.md` §4.1
> 本轮目标：在 `DJ-2026-005/02_PLC程序/工程资产/` 下落地首版 `io_points.csv` / `program_blocks.yml` / `communications.yml`，让 `doc refresh --dry-run` 输出真实内容而非"待补齐"占位

## 1. 现状基线

### 1.1 已就位

- `DJ-2026-005` 项目被 auto-pm 正确识别为 PLC 项目（PM_SESSION + 递归 `.plc.json` 补齐 stack="plc"）
- 三个 marker 已 retrofit 到位（CHG-SCPT-2026-074 闭环成果）：
  - `015_DJ-2026-005_IO分配表_IO.md` 第 27/42 行 `plc-io-overview`
  - `016_DJ-2026-005_PLC程序设计总文档_PLC.md` 第 299/305 行 `plc-program-components`
  - `016_DJ-2026-005_PLC程序设计总文档_PLC.md` 第 485/494 行 `plc-asset-index`
- 三个 marker 当前内容均为"待补齐 0 项"占位

### 1.2 唯一缺口

- `DJ-2026-005/02_PLC程序/工程资产/` 目录不存在
- 因此 `AssetSummaryService.build_summary` 返回 `status="missing"`，三文件 summary 均为 `_missing_file_summary`
- `DocRefreshService` 渲染时进入"待补齐"分支

### 1.3 资产契约（必填字段）

来源：`auto_pm/core/asset_summary_service.py` 第 24-34 行

| 文件 | 必填字段 | 校验规则 |
|------|----------|----------|
| `io_points.csv` | `station,signal_type,address,tag,signal_name,device,comment`（7 列全必填） | DictReader.fieldnames 必须包含全部 7 列；空行（全单元格 strip 后均空）跳过；signal_type 大写后必须命中 DI/DO/AI/AO 才进入计数 |
| `program_blocks.yml` | 顶层 `blocks` 列表；每元素 `name,type,path,responsibility` | 顶层必须有 `blocks` 键且为 list；每元素的 4 个键 strip 后非空 |
| `communications.yml` | 顶层 `channels` 列表；每元素 `name,protocol,role,endpoint,notes` | 顶层必须有 `channels` 键且为 list；每元素的 5 个键 strip 后非空 |

## 2. 资料来源盘点

### 2.1 IO 点资料来源（io_points.csv）

| 来源 | 路径 | 可提取字段 | 备注 |
|------|------|-----------|------|
| **主源**：015 IO 分配表 | `02_PLC程序/程序文档/015_DJ-2026-005_IO分配表_IO.md` §3-§6 | station / signal_type / address / tag / signal_name / device / comment | 表格化清晰，按"模块-章节"划分，可逐表格提取 |
| 辅源：源程序差异表 | 015 §12 | tag / signal_name / comment | 源程序实际用途与 EPLAN 物理接线有差异，需在 comment 中标注 |
| 辅源：export 接口变量表 | `export/FB_*_接口变量表.xlsx` | - | 本轮不解析 xlsx，仅记录存在 |

### 2.2 程序块资料来源（program_blocks.yml）

| 来源 | 路径 | 可提取字段 | 备注 |
|------|------|-----------|------|
| **主源**：PLC_ST 实际目录 | `02_PLC程序/PLC_ST/{OB1,DB1,common,conveyor,external,feeder,pickplace}/` | name / type / path | 真实代码组织，**以实际目录为准**（016 文档描述的 V4.0.0 设计已与实际落地有偏差） |
| 辅源：016 §5.1 组件清单 | `02_PLC程序/程序文档/016_*_PLC.md` §5.1 | name / type / responsibility | 设计意图，与实际目录差异需在 responsibility 中说明 |
| 辅源：各 FB PRD 接口文档 | `02_PLC程序/PLC_ST/{conveyor,external,feeder,pickplace,common}/PRD/接口文档_IFC-*.md` | responsibility | 用于补全职责描述 |

**关键差异**（实际目录 vs 016 文档）：
- 016 §5.1 写的是 `PRG_MainControl_DJ2026005`，实际是 `OB1/OB1.scl`（命名未对齐设计）
- 016 §5.1 写的是 `FB_1001_Conveyor4Layer_BufferFraming`，实际是 `conveyor/FB_1002_SingleLayerConveyor_BufferFraming.scl`（编号 1001→1002，且"四层"变"单层"）
- 016 §5.1 把 `FB_ExternalDeviceInteraction` 合并到打胶机里，实际 `external/FB_ExternalDeviceInteraction.scl` 独立成块

**策略**：以实际目录为唯一真源，016 文档作为 responsibility 的语义参考。

### 2.3 通讯对象资料来源（communications.yml）

| 来源 | 路径 | 可提取字段 | 备注 |
|------|------|-----------|------|
| **主源**：016 §3.1 系统功能表 | `016_*_PLC.md` §3.1 | name / protocol / role / endpoint / notes | 明确列出 5 类外部交互 |
| 辅源：015 §2.5 HMI 配置 | `015_*_IO.md` §2.5 | endpoint | HMI 设备型号 |
| 辅源：015 §2.1-2.2 模块配置 | `015_*_IO.md` §2.1-2.2 | endpoint | 远程 IO 站号型号 |
| 辅源：external 接口文档 | `02_PLC程序/PLC_ST/external/PRD/接口文档_IFC-FB3001-ExternalDeviceInteraction.md` | notes | 外部设备交互细节 |

**5 个通讯对象**：
1. HMI（Proface GP4501ww，Ethernet/RS-232/485，M/D 区数据交互）
2. UpstreamDevice-组框机（hardwired，预留 7DI+5DO，待现场确定）
3. DownstreamDevice-打胶机（hardwired，X75-X79/X102/Y50/Y51/Y44/Y47，安全联锁+送料协调）
4. RemoteIO-分布式IO（fieldbus-可替换，NZ2MFB1-32DT 站号1+站号2，取料机构传感器+电磁阀）
5. MES-上位机（ethernet，D406-D425 报警队列上传）

## 3. 字段映射方案

### 3.1 io_points.csv 字段映射

| 目标字段 | 来源 | 提取规则 | 空值策略 |
|---------|------|---------|---------|
| `station` | 015 章节标题或"所属层级"列 | CPU本体→`cpu`；DI扩展模块→`di_ext_{N}`；DO扩展模块→`do_ext_{N}`；远程IO→`remote_io_{N}`；按工站→`l1`/`l2`/`l3`/`l4`/`common`/`pickplace` | 不允许空 |
| `signal_type` | 015"信号类型"列 | 直接映射：DI/DO；高速脉冲标注为 DO（comment 注明"高速脉冲"）；无 AI/AO 点（全为数字量） | 不允许空 |
| `address` | 015"地址"列或"本地地址"+"全局映射"组合 | CPU 本体用 `X0`/`Y0`；扩展模块用 `DI{N}:X{addr}` 或 `DO{N}:Y{addr}`；远程用 `RIO{N}:X{addr}` | 不允许空 |
| `tag` | 015"变量名"列 | 直接映射（如 `Z_Home_Sensor`、`L4_Block_Solenoid`） | 不允许空 |
| `signal_name` | 015"功能描述"列 | 直接映射（如"Z轴原点传感器"） | 不允许空 |
| `device` | 015"来源设备"/"目标设备"/"执行器件"列 | 直接映射；缺失时填"未指定" | 不允许空，默认"未指定" |
| `comment` | 综合来源 | 模块归属+原理图页码+差异说明；如`P35/EFS1/16.7; 源程序: 变频器1异常检测` | 不允许空，至少填模块归属 |

**特殊处理**：
- 源程序 §12 差异表（X14-X17 实际是变频器异常检测，非安全门）：保留 EPLAN 物理接线为主，comment 中追加"源程序用途: 变频器异常检测"
- Y24-Y27/Y50 指示灯蜂鸣器（EPLAN 未独立列出，源程序定义）：作为独立条目收录，device 填"源程序定义"
- 打胶机 X76/X102/Y44/Y47：直接收录

### 3.2 program_blocks.yml 字段映射

| 目标字段 | 来源 | 提取规则 |
|---------|------|---------|
| `name` | PLC_ST 目录名 / 文件名（去扩展名） | OB1→`OB1`；DB1/GlobalVars.db→`GlobalVars`；FB_*.scl→文件名去 `.scl` |
| `type` | 文件扩展名 + 目录约定 | `.scl` 在 OB1 目录→`OB`；`.db`→`DB`；`.scl` 在其他目录→`FB` |
| `path` | 相对项目根的路径 | `02_PLC程序/PLC_ST/OB1/OB1.scl` 等 |
| `responsibility` | 016 §5.1 表格 + 各 FB 接口文档标题 | 简明描述核心职责（不超过 30 字） |

**7 个真实程序块**：

| name | type | path | responsibility |
|------|------|------|---------------|
| OB1 | OB | 02_PLC程序/PLC_ST/OB1/OB1.scl | 主循环与调用编排（设计意图为 PRG_MainControl_DJ2026005） |
| GlobalVars | DB | 02_PLC程序/PLC_ST/DB1/GlobalVars.db | 全局变量与 IO 映射 |
| FB_2001_CommonAlarm_AllStation | FB | 02_PLC程序/PLC_ST/common/FB_2001_CommonAlarm_AllStation.scl | 公共报警管理（三站报警汇总+MES队列） |
| FB_1002_SingleLayerConveyor_BufferFraming | FB | 02_PLC程序/PLC_ST/conveyor/FB_1002_SingleLayerConveyor_BufferFraming.scl | 输送设备控制（设计意图为 FB_1001_Conveyor4Layer） |
| FB_ExternalDeviceInteraction | FB | 02_PLC程序/PLC_ST/external/FB_ExternalDeviceInteraction.scl | 外部设备交互（打胶机+组框机联锁） |
| FB_1004_GlueMachineFeeder_BufferFraming | FB | 02_PLC程序/PLC_ST/feeder/FB_1004_GlueMachineFeeder_BufferFraming.scl | 打胶机送料控制 |
| FB_1003_PickPlace_BufferFraming | FB | 02_PLC程序/PLC_ST/pickplace/FB_1003_PickPlace_BufferFraming.scl | 取放料控制（Z+X1+夹爪协同） |

### 3.3 communications.yml 字段映射

| 目标字段 | 来源 | 提取规则 |
|---------|------|---------|
| `name` | 016 §3.1 + 015 §2.5 | HMI / UpstreamDevice / DownstreamDevice / RemoteIO / MES |
| `protocol` | 015 §2.5 + 016 §3.1 | ethernet / hardwired / fieldbus / rs485 等 |
| `role` | 016 §3.1 | 人机界面 / 上游设备 / 下游设备 / 分布式IO / 上位机系统 |
| `endpoint` | 015 设备型号 + 016 §3.1 | 设备型号 + 接口规格；预留设备填"待现场确定" |
| `notes` | 综合来源 | 信号清单+地址范围+联锁说明 |

**5 个通讯对象**：

| name | protocol | role | endpoint | notes |
|------|----------|------|----------|-------|
| HMI | ethernet | 人机界面 | Proface GP4501ww 10" TFT (RS-232/485 + Ethernet) | M0-M49 模式/操作; D100-D112 工艺参数; M12/M13 状态回写 |
| UpstreamDevice | hardwired | 上游设备（组框机） | 待现场确定 | 预留 7DI+5DO; 含急停联锁+开门互锁+送料请求协调; 310-312 段报警码 |
| DownstreamDevice | hardwired | 下游设备（打胶机） | 硬接线 X75-X79/X102/Y50/Y51/Y44/Y47 | 安全联锁+送料协调; 300-302 段报警码; 故障/急停/通讯异常联锁 |
| RemoteIO | fieldbus | 分布式IO | 三菱 NZ2MFB1-32DT 站号1+站号2 (接线盒B5) | 站1: 取料机构 14DI+6DO; 站2: 预留扩展; 总线实现可替换(原CC-Link已取消强绑定) |
| MES | ethernet | 上位机系统 | 待对接 | D406-D425 最近10条去重报警队列; 新报警脉冲标志触发上传 |

## 4. 提取流程

### 4.1 总体策略：半自动（一次性脚本辅助）

按用户决策（2026-06-29）：**半自动策略**——读真实文档+一次性 Python 脚本辅助生成，落盘后删除脚本，不进 auto-pm 主代码。

理由：
- 6 周计划 §4.1 允许手工首版+记录来源
- 6 周计划 §5.2 把"大规模新解析器"列为暂停信号，半自动一次性脚本不触发
- 字段映射规则明确，脚本只需做"格式转换"，无需复杂解析逻辑

### 4.2 执行步骤

1. **创建目录**：`DJ-2026-005/02_PLC程序/工程资产/`
2. **生成 io_points.csv**：
   - 一次性脚本硬编码 90+ 真实 IO 点（已从 015 文档表格逐行确认）
   - 按 §3.1 字段映射规则生成 7 列 CSV
   - 编码 utf-8-sig（兼容 BOM，匹配 AssetSummaryService 第 131 行）
3. **生成 program_blocks.yml**：
   - 一次性脚本根据 PLC_ST 实际目录枚举 + 硬编码 responsibility
   - 按 §3.2 字段映射规则生成
4. **生成 communications.yml**：
   - 一次性脚本硬编码 5 个通讯对象（从 016 §3.1 + 015 §2.5 提取）
   - 按 §3.3 字段映射规则生成
5. **删除一次性脚本**：脚本完成生成后立即删除，不进版本库
6. **验证**：
   - `auto-pm project show DJ-2026-005` 应展示真实资产摘要
   - `auto-pm doc refresh DJ-2026-005 --dry-run` 应输出真实计数和真实条目
   - `auto-pm doc refresh DJ-2026-005 --dry-run --json` 应返回 refreshed_files

### 4.3 边界与不做

- **不做**：解析 015 文档表格的通用解析器（属 §5.2 暂停信号）
- **不做**：解析 xlsx 接口变量表（export/*.xlsx 留作后续工作）
- **不做**：在 auto-pm 主代码新增任何资产提取模块
- **不做**：修改 016 文档描述（设计意图与实际代码的差异保留在 program_blocks.yml 的 responsibility 字段中）

## 5. 验收口径（done_when）

按 6 周计划 §4.1 + glm 执行输入清单 §5 第1周要求：

| 验收项 | 期望结果 |
|--------|---------|
| `02_PLC程序/工程资产/` 目录存在 | 三个文件齐全 |
| `io_points.csv` 行数 | ≥ 90 条真实 IO 点（覆盖 CPU 本体 + DI 扩展 + DO 扩展 + 远程 IO） |
| `program_blocks.yml` blocks 数 | = 7 项（与 PLC_ST 实际目录一致） |
| `communications.yml` channels 数 | = 5 项（HMI + 上下游 + 远程IO + MES） |
| `auto-pm project show DJ-2026-005` | 工程资产摘要显示 `healthy` 或 `warning`，三个 count 均 > 0 |
| `auto-pm doc refresh DJ-2026-005 --dry-run` | 三个自动区输出真实内容（不再是"待补齐"占位） |
| 至少 1 组回归测试 | 覆盖真实资产场景（在 tests/core 或 tests/cli 补一组断言） |
| 一次性脚本已删除 | 脚本不进版本库，仅在方案文档中记录生成过程 |

## 6. 风险与边界

### 6.1 已识别风险

| 风险 | 缓解措施 |
|------|---------|
| 015 文档 IO 点统计为"已用 118 点"，但实际表格逐行提取可能少于此数 | 接受首版偏差，在 comment 中标注"来源: 015 文档表格逐行提取"；§9.3 总计 152 点（已用 118）含 HMI 占用 X16-X17，需在 station 标注 |
| 016 §5.1 组件清单与 PLC_ST 实际目录有 3 处差异 | 以实际目录为唯一真源；responsibility 中标注设计意图；不在本轮修改 016 文档 |
| 源程序 §12 差异表显示 X14-X17 实际用途与 EPLAN 不同 | comment 中追加"源程序用途: ..."；保留 EPLAN 物理接线为主 |
| Y24-Y27/Y50 指示灯蜂鸣器 EPLAN 未独立列出 | 作为独立条目收录，device 填"源程序定义" |
| 半自动脚本可能因硬编码偏差引入错误 | 生成后必须用 `auto-pm project show` 和 `doc refresh --dry-run` 双重验证；偏差项列入 open_questions |

### 6.2 暂停信号（来自 6 周计划 §5.2）

- 真实项目资产补齐需要大规模新解析器 → 触发暂停（本轮不触发，半自动一次性脚本不在禁止之列）
- 第二样本暴露的问题要求重写 PlcChecker 或 DocRefreshService 核心架构 → 第3周才会遇到

### 6.3 不在本轮范围

- 第二样本复核（属第3周）
- 008 试运行报告"补齐前后"价值差异（属第2周）
- V0.4.3 版本号升级（属第5周）

## 7. 回写计划

完成资产补齐后，必须回写：

1. **PM_SESSION_SW-2026-008.md**：
   - §2 Current Focus：切换到"V0.4.2 第1周资产补齐已完成，进入第2周价值闭环"
   - §3 Status Summary：in_progress 增加"V0.4.2 第2周"；completed 增加"V0.4.2 第1周资产补齐"
   - §5 Logs：新增 change_log 条目
   - §6 Implementation Log：新增本轮实施记录
   - §7 Verification Log：新增本轮验证记录
   - §8 Handoff Notes：更新 current_state、next_focus、watchouts
   - §9 Next Actions：更新下一步动作
2. **008_试运行报告_PILOT.md**：追加"V0.4.2 第1周 DJ-2026-005 资产补齐前后差异"证据（属第2周正式写入，本轮先记录关键数据）
3. **本方案文档**：执行完毕后在末尾追加"执行结果"章节

## 8. 本文输出物

- 本文：DJ-2026-005 资产补齐方案
- 落地物：`DJ-2026-005/02_PLC程序/工程资产/{io_points.csv,program_blocks.yml,communications.yml}`
- 验证证据：`auto-pm project show` + `doc refresh --dry-run` 输出
- 回写真源：`PM_SESSION_SW-2026-008.md` §2/§3/§5/§6/§7/§8/§9
