# M5 规范与台账管理迭代计划

## Meta

- 项目: SW-2026-008 auto-pm
- 版本基线: V1.0.0
- 创建日期: 2026-07-11
- 创建技能: pm-workflow
- 前置里程碑: M4 变更管理+项目管理 GUI 补全（CHG-114 + CHG-115，第 46 次 dogfooding 闭环 closed）

## 1. 迭代背景

M4 迭代（变更管理完整闭环 + 项目管理补齐 7 项）已全部闭环。继续推进 GUI 功能覆盖补全，本轮聚焦 M5 规范与台账管理 6 项。

### 1.1 后端现状核实

经 Grep + Read 实测，6 项的后端 CLI/Service **全部就绪**，工作集中在 Facade 层补齐 + Bridge Slot 新增 + QML 对话框新建。

| 序号 | 项目 | CLI 命令 | 后端 Service | Facade 方法 | Bridge Slot | QML 对话框 |
|------|------|---------|-------------|-------------|-------------|------------|
| 1 | delivery asset | ✅ 已有 | ✅ 已有 | ✅ 已有 | ✅ 已有 | ❌ 需新增 |
| 2 | pm-session archive | ✅ `pm-session archive` | ✅ PmSessionArchiveService (CHG-109) | ❌ 需新增 | ❌ 需新增 | ❌ 需新增 |
| 3 | ledger reconcile | ✅ `ledger reconcile` | ✅ LedgerReconciler (CHG-108) | ❌ 需新增 | ❌ 需新增 | ❌ 需新增 |
| 4 | spec index | ✅ `spec index` | ✅ 已有 | ❌ 需新增 | ❌ 需新增 | ❌ 需新增 |
| 5 | spec report | ✅ `spec report` | ✅ 已有 | ❌ 需新增 | ❌ 需新增 | ❌ 需新增 |
| 6 | spec frontmatter | ✅ `spec frontmatter` | ✅ 已有 | ❌ 需新增 | ❌ 需新增 | ❌ 需新增 |

## 2. 实施策略

用户决策：**分 6 个独立 CHG**（最细粒度，每项一个 CHG），**CHG-116 先行**。

### 2.1 CHG 编号规划

| CHG 编号 | 项目 | 工作量 | 依赖 | 说明 |
|----------|------|--------|------|------|
| CHG-116 | delivery asset | 低 | 无 | Bridge 已有，仅需 QML 对话框 |
| CHG-117 | pm-session archive | 中 | 无 | Facade + Bridge + QML，后端 Service 已有 |
| CHG-118 | ledger reconcile | 中 | 无 | Facade + Bridge + QML，后端 Service 已有 |
| CHG-119 | spec index | 中 | 无 | Facade + Bridge + QML，后端 Service 已有 |
| CHG-120 | spec report | 中 | 无 | Facade + Bridge + QML，后端 Service 已有 |
| CHG-121 | spec frontmatter | 中 | 无 | Facade + Bridge + QML，后端 Service 已有 |

### 2.2 启动顺序

CHG-116 → CHG-117 → CHG-118 → CHG-119 → CHG-120 → CHG-121（串行推进，每个 CHG 走完整 dogfooding 闭环）

## 3. 每个 CHG 的标准任务拆解

### CHG-116: delivery asset（交付资产管理对话框）

**后端现状**：Facade `get_asset_summary` + `refresh_asset_summary` 已有，Bridge `getAssetSummary` + `refreshAssetSummary` Slot 已有

**任务**：
- T0 变更单创建（CLI `auto-pm change create`，dogfooding）
- T1 QML 对话框新建 `AssetSummaryDialog.qml`（展示项目资产摘要 + 刷新按钮）
- T2 UI 集成（main.qml 注册对话框 + WorkspaceView 触发入口）
- T3 测试（QML 组件语法验证 + bridge 交互测试）
- T4 全量门禁（ruff 0 + mypy + pytest + PM_SESSION size）
- T5 CHG-116 闭环（§6-§11 填写 + 状态流转 closed）

**DoR**：Bridge Slot 已就绪 ✅
**DoD**：QML 对话框可展示资产摘要 + 刷新功能可用 + 全量门禁全绿 + CHG 状态 closed

### CHG-117: pm-session archive（PM_SESSION 归档对话框）

**后端现状**：CLI `pm-session archive` 已有，PmSessionArchiveService 已有（CHG-109），Facade 无 archive 方法

**任务**：
- T0 变更单创建
- T1 Facade 方法新增 `archive_pm_session(section, keep_recent, dry_run)`（system_facade.py）
- T2 Bridge Slot 新增 `archivePmSession(section, keepRecent, dryRun)`（system_bridge.py）
- T3 QML 对话框新建 `PmSessionArchiveDialog.qml`（章节选择 + 保留数量 + 预览/执行）
- T4 UI 集成（main.qml 注册 + SettingsView 或系统菜单触发）
- T5 测试（facade + bridge 单元测试 + QML 组件语法验证）
- T6 全量门禁
- T7 CHG-117 闭环

**DoR**：PmSessionArchiveService 已就绪 ✅
**DoD**：GUI 可执行 §6/§8 归档 + 预览模式可用 + 全量门禁全绿 + CHG 状态 closed

### CHG-118: ledger reconcile（变更台账对账对话框）

**后端现状**：CLI `ledger reconcile` 已有，LedgerReconciler 已有（CHG-108），Facade 无 reconcile 方法

**任务**：
- T0 变更单创建
- T1 Facade 方法新增 `reconcile_ledger(project_id, auto_fix)`（change_facade.py）
- T2 Bridge Slot 新增 `reconcileLedger(projectId, autoFix)`（change_bridge.py）
- T3 QML 对话框新建 `LedgerReconcileDialog.qml`（差异展示 + 自动修复按钮）
- T4 UI 集成（main.qml 注册 + ChangeCenterView 触发入口）
- T5 测试（facade + bridge 单元测试 + QML 组件语法验证）
- T6 全量门禁
- T7 CHG-118 闭环

**DoR**：LedgerReconciler 已就绪 ✅
**DoD**：GUI 可展示台账差异 + 自动修复可用 + 全量门禁全绿 + CHG 状态 closed

### CHG-119: spec index（规范索引生成对话框）

**后端现状**：CLI `spec index` 已有，后端 Service 已有，Facade 无 index 方法

**任务**：
- T0 变更单创建
- T1 Facade 方法新增 `generate_spec_index()`（spec_facade.py）
- T2 Bridge Slot 新增 `generateSpecIndex()`（spec_bridge.py）
- T3 QML 对话框新建 `SpecIndexDialog.qml`（索引生成 + 结果展示）
- T4 UI 集成（main.qml 注册 + SpecCenterView 触发入口）
- T5 测试（facade + bridge 单元测试 + QML 组件语法验证）
- T6 全量门禁
- T7 CHG-119 闭环

**DoR**：后端 spec index Service 已就绪 ✅
**DoD**：GUI 可生成规范索引 + 结果展示 + 全量门禁全绿 + CHG 状态 closed

### CHG-120: spec report（规范报告生成对话框）

**后端现状**：CLI `spec report` 已有，后端 Service 已有，Facade 无 report 方法

**任务**：
- T0 变更单创建
- T1 Facade 方法新增 `generate_spec_report()`（spec_facade.py）
- T2 Bridge Slot 新增 `generateSpecReport()`（spec_bridge.py）
- T3 QML 对话框新建 `SpecReportDialog.qml`（报告生成 + 结果展示）
- T4 UI 集成（main.qml 注册 + SpecCenterView 触发入口）
- T5 测试（facade + bridge 单元测试 + QML 组件语法验证）
- T6 全量门禁
- T7 CHG-120 闭环

**DoR**：后端 spec report Service 已就绪 ✅
**DoD**：GUI 可生成规范报告 + 结果展示 + 全量门禁全绿 + CHG 状态 closed

### CHG-121: spec frontmatter（规范 Frontmatter 检查/修复对话框）

**后端现状**：CLI `spec frontmatter` 已有，后端 Service 已有，Facade 无 frontmatter 方法

**任务**：
- T0 变更单创建
- T1 Facade 方法新增 `check_spec_frontmatter(auto_fix)`（spec_facade.py）
- T2 Bridge Slot 新增 `checkSpecFrontmatter(autoFix)`（spec_bridge.py）
- T3 QML 对话框新建 `SpecFrontmatterDialog.qml`（检查结果展示 + 自动修复按钮）
- T4 UI 集成（main.qml 注册 + SpecCenterView 触发入口）
- T5 测试（facade + bridge 单元测试 + QML 组件语法验证）
- T6 全量门禁
- T7 CHG-121 闭环

**DoR**：后端 spec frontmatter Service 已就绪 ✅
**DoD**：GUI 可检查规范 Frontmatter + 自动修复可用 + 全量门禁全绿 + CHG 状态 closed

## 4. 风险登记

| 风险 | 严重度 | 缓解措施 |
|------|--------|----------|
| spec index/report/frontmatter 后端 Service 接口契约未确认 | 🟡中 | T1 前需 Read 后端 Service 代码确认接口签名 |
| QML 对话框组件复用现有 GlassPanel 风格 | 🟢低 | 参照 CHG-115 的 ProjectEditDialog/DeleteConfirmDialog 模式 |
| 6 个 CHG 串行推进周期较长 | 🟡中 | 每个 CHG 闭环后立即启动下一个，不等待 |

## 5. dogfooding 闭环规划

每个 CHG 走完整 9 步状态流转：`draft → submitted → under_review → approved → implementing → pending_acceptance → accepting → completed → closed`

- CHG-116: 第 47 次 dogfooding 闭环
- CHG-117: 第 48 次 dogfooding 闭环
- CHG-118: 第 49 次 dogfooding 闭环
- CHG-119: 第 50 次 dogfooding 闭环
- CHG-120: 第 51 次 dogfooding 闭环
- CHG-121: 第 52 次 dogfooding 闭环

## 6. 验收标准

- 6 个 CHG 全部状态 closed
- 6 个 QML 对话框全部可加载（QQmlApplicationEngine 验证）
- 全量门禁 6 项全绿（ruff 0 + mypy + tests/application + tests/qml + tests/spec + PM_SESSION size）
- 台帐对账无差异（`ledger reconcile` 验证）
- PM_SESSION §3 spec_compliance dogfooding 计数 46→52
