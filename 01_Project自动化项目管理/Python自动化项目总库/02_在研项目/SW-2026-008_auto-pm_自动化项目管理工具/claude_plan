# auto-pm V1.0 重构方案：从「一坨」到「清晰架构」

> **给 fubai 的话**: 你的直觉是对的。下面是诚实的诊断。

---

## 一、诚实诊断：当前架构到底有什么问题

### 1.1 你说的「一坨」是真的

当前项目是一个典型的 **AI 辅助开发的产物**——功能堆叠很快，但架构从未被真正设计过。

```
你感受到的「一坨」              实际的技术问题
─────────────────────         ──────────────────────────────
"前后端没分离"        ←───→   QmlBridge 是 1015 行的上帝类
                              直接 import Service，无 API 接口层
                              QML 前端和 Python 后端是物理绑死的

"没有接口"            ←───→   虽然有 protocols.py 定义了接口
                              但 QML 通过 bridge 直连 Service
                              没有 HTTP/REST/JSON-RPC 任何形式的接口层

"说不清楚架构"        ←───→   CLI/GUI/Service/DB 四层看似清晰
                              但实际调用关系是网状耦合
                              18 个向后兼容委托方法说明重构没做彻底
```

### 1.2 具体问题清单

| # | 问题 | 严重程度 | 位置 |
|---|------|----------|------|
| 1 | **QmlBridge 上帝类** — 1015 行，承担了数据转换、错误处理、业务逻辑三重职责 | 🔴 严重 | `qml_bridge.py` |
| 2 | **没有 API 层** — GUI 直连 Service，如果将来想换前端（比如 Web），必须重写所有调用 | 🔴 严重 | 全局架构 |
| 3 | **前端技术栈选型问题** — PySide6 QML 对一个电气工程师来说学习曲线太陡，且社区资源少 | 🟡 中等 | `ui/qml/` |
| 4 | **向后兼容债务** — 12+6 个纯委托方法，是重构没做到底的残留物 | 🟡 中等 | `change_service.py`, `project_service.py` |
| 5 | **文档膨胀** — PRD 78KB、GUI 原型 70KB、DSN 88KB，维护成本极高 | 🟡 中等 | `00_项目基础信息/` |
| 6 | **过度工程** — 4 角色适配、12 状态机、7 领域 DOM 对一个单人工具来说是过度设计 | 🟡 反思 | 全局 |

### 1.3 根因分析

> [!IMPORTANT]
> **根因不是代码质量差，而是缺少一个面向你自己的架构决策**。
>
> 这个项目一开始是 CLI 工具，后来加了 pywebview，再迁移到 PySide6 QWidget，又迁移到 QML——每次迁移都是「在旧架构上贴新 UI」，从未真正设计过前后端分离的接口层。

---

## 二、重构目标

### 2.1 你的三重身份

| 身份 | 重构中学到的技能 |
|------|-----------------|
| **电气工程师** | 工具本身帮你管理 PLC 项目（产品价值） |
| **项目经理** | 用这个项目实践需求管理、迭代计划、变更控制（项目管理技能） |
| **Python 开发者** | 通过重构学习：API 设计、前后端分离、Web 开发、数据库设计（编程技能） |

### 2.2 架构目标

```
重构前（当前）                    重构后（V1.0）
──────────────                   ──────────────

┌─ QML (1015 行 bridge) ──┐      ┌─ Web 前端 (HTML/JS) ──────┐
│  直接 import Service     │      │  fetch("/api/projects")    │
│  零接口、零分离           │      │  技术栈：你已经会的 HTML/JS │
└────────┬────────────────┘      └────────┬──────────────────┘
         │ 直接函数调用                      │ HTTP JSON API
         │                                 │
         ├── CLI (Click)                   ├── CLI (Click)
         │                                 │
┌────────┴────────────────┐      ┌────────┴──────────────────┐
│   Service Layer          │      │   API Layer (FastAPI)      │
│   (ProjectService etc.)  │      │   /api/projects GET/POST   │
│                          │      │   /api/changes  GET/POST   │
│   DB Layer (SQLite)      │      │   Service Layer (不变)      │
└──────────────────────────┘      │   DB Layer (不变)           │
                                  └─────────────────────────────┘
```

> [!TIP]
> **为什么用 Web 前端而不是继续 QML？**
> 1. 你已经有 HTML 原型（`02_设计/Html原型预览/` 里的 126KB index.html）
> 2. Web 前端的学习资源是 QML 的 100 倍
> 3. FastAPI + HTML/JS 是 Python 工程师最主流的全栈路径
> 4. 将来可以直接部署到浏览器，不依赖 PySide6 安装

---

## 三、重构方案（3 阶段，8 周）

### Phase 1：插入 API 层（第 1-2 周）

**目标**: 不动前端，在后端插入 FastAPI API 层。

**核心变更**:

```
auto_pm/
├── api/                          # [NEW] FastAPI API 层
│   ├── __init__.py
│   ├── app.py                    # FastAPI app 实例
│   ├── routes/
│   │   ├── projects.py           # GET/POST/PUT/DELETE /api/projects
│   │   ├── changes.py            # GET/POST/PUT /api/changes
│   │   ├── plc.py                # POST /api/plc/check, /api/plc/repair
│   │   └── dashboard.py          # GET /api/dashboard
│   ├── schemas.py                # Pydantic 请求/响应模型（复用 models/）
│   └── deps.py                   # 依赖注入（Service 实例管理）
├── cli/                          # [保留] CLI 调用 Service 层
├── core/                         # [保留] Service 层不变
├── change/                       # [保留]
├── db/                           # [保留]
└── models/                       # [保留] API 和 CLI 共用模型
```

**学到的技能**:
- FastAPI 路由设计
- RESTful API 设计原则
- Pydantic 请求/响应 Schema
- 依赖注入模式

**关键代码示例**:

```python
# api/routes/projects.py
from fastapi import APIRouter, Depends
from auto_pm.core.project_service import ProjectService
from auto_pm.api.deps import get_project_service

router = APIRouter(prefix="/api/projects", tags=["projects"])

@router.get("/")
def list_projects(
    stack: str | None = None,
    phase: str | None = None,
    service: ProjectService = Depends(get_project_service),
):
    """列出所有项目 — 1 个路由替代 QmlBridge 里的 5 个 Slot"""
    return service.list_projects()

@router.get("/{project_id}")
def get_project(project_id: str, service = Depends(get_project_service)):
    return service.get_project(project_id)
```

**验证标准**: `curl http://localhost:8000/api/projects` 能返回 JSON 项目列表。

---

### Phase 2：Web 前端（第 3-5 周）

**目标**: 基于你已有的 HTML 原型，构建真正的 Web 前端。

**技术选型**:

| 选项 | 推荐度 | 理由 |
|------|--------|------|
| **原生 HTML + JS + CSS** | ⭐⭐⭐⭐⭐ | 你已经有原型，学习成本最低，完全掌控 |
| Vue.js | ⭐⭐⭐⭐ | 如果想进阶，Vue 3 是最适合 Python 开发者的前端框架 |
| React | ⭐⭐⭐ | 生态更大但学习曲线更陡 |

**推荐方案**: 先用**原生 HTML/JS**（基于你 `Html原型预览/` 的 126KB index.html），通过 `fetch()` 调 FastAPI 接口。等你熟悉后再考虑是否引入 Vue。

**前端结构**:

```
auto_pm/
├── web/                           # [NEW] Web 前端（FastAPI 静态文件托管）
│   ├── index.html                 # 主页
│   ├── css/
│   │   └── styles.css
│   ├── js/
│   │   ├── api.js                 # fetch 封装（调 /api/*）
│   │   ├── app.js                 # 主应用逻辑
│   │   ├── projects.js            # 项目列表/详情
│   │   ├── changes.js             # 变更管理
│   │   └── dashboard.js           # 驾驶舱首页
│   └── components/                # 可复用 HTML 片段
│       ├── sidebar.html
│       ├── project-card.html
│       └── change-table.html
```

**学到的技能**:
- `fetch()` API 调用
- DOM 操作和事件处理
- CSS 布局（Flexbox/Grid）
- 前后端数据流（JSON 请求→渲染→用户交互→请求）

**验证标准**: 浏览器打开 `http://localhost:8000`，能看到项目列表、点击进入项目详情、创建变更单。

---

### Phase 3：打磨与清理（第 6-8 周）

**目标**: 清理技术债，删除旧代码，完善体验。

| 任务 | 内容 |
|------|------|
| 删除 QML 代码 | 移除 `ui/qml/`、`ui/change_center/`、`ui/widgets/` 等 PySide6 代码 |
| 删除 pywebview 依赖 | 从 `pyproject.toml` 移除 PySide6 和 pywebview |
| 清理委托方法 | 移除 18 个 `_xxx` 向后兼容委托方法 |
| 文档瘦身 | PRD 从 78KB 精简到 ~20KB，只保留当前有效内容 |
| mypy 清零 | 509 行 mypy 错误清零（删除 PySide6 测试后大部分自动消失） |
| 添加 API 测试 | `pytest` 测试 FastAPI 路由（`httpx.AsyncClient`） |
| 启动方式统一 | `auto-pm serve` 启动 FastAPI 服务 + 打开浏览器 |

**学到的技能**:
- API 集成测试
- 技术债管理和代码清理
- 项目发布流程

---

## 四、重构原则

### 4.1 不动核心，只换壳

```
保留的（70%代码量）               替换的（30%代码量）
─────────────────               ─────────────────
✅ core/ (Service 层)            ❌ ui/qml/ (QML 前端)
✅ change/ (变更管理)             ❌ ui/change_center/ (QWidget)
✅ db/ (SQLite 层)               ❌ ui/widgets/ (QWidget)
✅ models/ (Pydantic 模型)       ❌ ui/dialogs/ (QWidget)
✅ plc/ (PLC 检查)               ❌ qml_bridge.py (1015 行上帝类)
✅ cli/ (CLI 命令)               ❌ gui.py (PySide6 启动入口)
✅ utils/ (工具函数)
✅ config/ (配置)
```

### 4.2 每个阶段独立可用

- Phase 1 完成后：CLI + API 可用，旧 QML GUI 也还能用
- Phase 2 完成后：CLI + API + Web GUI 可用
- Phase 3 完成后：删除旧代码，项目干净

### 4.3 学习路径映射

```
Phase 1 → 后端开发能力（API 设计、依赖注入、接口文档）
Phase 2 → 全栈开发能力（前后端联调、数据流、用户交互）  
Phase 3 → 工程素养（技术债管理、测试、发布）
```

---

## 五、需要你确认的问题

> [!IMPORTANT]
> 以下问题会影响重构方案的选择，请先确认。

### Q1: 前端技术栈

当前方案推荐 **原生 HTML/JS + FastAPI**。你是否有偏好？
- A: 原生 HTML/JS（最简单，基于你已有的原型）
- B: Vue.js（更现代，但需要学习新框架）
- C: 继续用 PySide6 QML（不推荐，但保留选项）

### Q2: 重构范围

- A: **全量重构**（推荐）— 3 阶段 8 周，彻底前后端分离
- B: **最小重构** — 只插入 API 层，保留 QML 前端
- C: **推倒重来** — 新建项目，只迁移核心 Service 代码

### Q3: 启动方式

- A: `auto-pm serve` 启动 FastAPI → 浏览器打开 localhost（推荐）
- B: Electron 打包为桌面应用（更重，但更像桌面工具）
- C: pywebview 包裹 Web 前端（轻量桌面化）

### Q4: 你的 Python 熟练度

这会影响我在代码里加多少注释和教学内容：
- A: 能写函数和类，理解 import，但不太熟悉装饰器和异步
- B: 熟悉 Click/Pydantic/pytest 这些库，看得懂现有代码
- C: 能独立写中等复杂度的 Python 项目

---

> [!NOTE]
> 这个重构方案本身就是一次**项目管理实践** ——
> 你可以用 auto-pm 的变更单流程来管理这次重构（dogfooding）。
> 每个 Phase 创建一个 CHG 变更单，走完 12 状态流转。
