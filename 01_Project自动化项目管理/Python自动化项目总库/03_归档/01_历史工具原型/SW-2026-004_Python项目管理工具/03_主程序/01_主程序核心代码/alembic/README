Alembic Migrations
====================

此目录包含数据库迁移脚本。

常用命令：
-----------

# 初始化迁移环境（已完成）
alembic init alembic

# 创建新迁移
alembic revision -m "描述迁移内容"

# 自动生成迁移（基于模型变化）
alembic revision --autogenerate -m "描述迁移内容"

# 升级到最新版本
alembic upgrade head

# 升级到指定版本
alembic upgrade <revision_id>

# 降级一个版本
alembic downgrade -1

# 降级到指定版本
alembic downgrade <revision_id>

# 查看当前版本
alembic current

# 查看迁移历史
alembic history

# 查看迁移状态
alembic heads

注意：
------
- 迁移脚本会自动应用到项目数据库
- 不要手动修改已应用的迁移脚本
- 如果需要修改已应用的迁移，请创建新的迁移
