# ==============================================================================
# Auto-PM 工业级 AI 研发工作台 - 依赖清单
# ==============================================================================

# 核心桌面端与 GUI 依赖
PySide6>=6.5.0

# 核心 CLI 与规则引擎依赖
click>=8.1.0
rich>=13.0.0
jinja2>=3.1.0
pyyaml>=6.0.0

# 增强工具（可选平滑降级）
psutil>=5.9.0

# 打包构建工具
pyinstaller>=6.0.0

# 开发与自动化测试套件 (Dev & Tests)
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
ruff>=0.1.0
mypy>=1.5.0
types-PyYAML>=6.0.0
types-requests>=2.31.0
