# CHG-SPEC-2026-001：Obsidian 规范库 P0 治理修补（草案）

- 项目：SYS-2026-001_WorkspaceGovernance
- 关联项目：SW-2026-008
- 来源方案：`PLC_工单_Markdown_整理包/90_解决方案_Obsidian规范完善_DJ-2026-005校准版.md`
- 状态：`draft`
- 变更类型：SPEC（规范法典）
- 建议审批粒度：包级；文件级由执行 checklist 约束

## 1. 目标

修复现有 Obsidian 规范库的 frontmatter、canonical_path、registry、INDEX、CHK、活跃区死链和历史台账回链问题，使规范库重新具备可验证、可导航、可追溯的治理基础。P0 不新增业务规范，不恢复物理历史快照，不删除文件。

## 2. 批准前提

本草案不授权实施。只有架构师批准本单及其 DEC 后，GLM 或其他执行代理才可修改白名单内文件。`PLC_工单_Markdown_整理包` 仅为证据输入，不能作为规范库覆盖源。

## 3. 精确白名单

完整白名单、WBS、验收标准见：

`.auto-pm/reports/DJ-2026-005_方案审查_P0白名单_WBS_2026-09-06.md`

主包允许修改：

- `00_Obsidian_Base全局规范文件仓库/spec_registry.json`
- `00_Obsidian_Base全局规范文件仓库/00_INDEX_全局规范索引.md`
- `00_Obsidian_Base全局规范文件仓库/01_项目管理域/**` 中报告列出的 15 个精确文件
- `00_Obsidian_Base全局规范文件仓库/02_Python开发域/**` 中报告列出的 6 个精确文件
- `00_Obsidian_Base全局规范文件仓库/03_PLC自动化域/**` 中报告列出的 4 个精确文件
- `00_Obsidian_Base全局规范文件仓库/04_驾驶舱与全栈域/**` 中报告列出的 4 个精确文件
- `00_Obsidian_Base全局规范文件仓库/05_跨域工具规范/**` 中报告列出的 2 个精确文件

以上目录写法仅用于分组说明，执行时必须展开为报告中的精确路径；禁止使用目录级 glob 作为写入授权。

## 4. 禁止范围

- `00_Obsidian_Base全局规范文件仓库/Archive_Cold/**`
- `.trae/**`
- `00_Infrastructure/auto_pm/**`
- 研发母体源码、稳定 release、active/previous 指针、deployment manifest
- Git index 中已有 staged/unstaged 候选
- 删除、重命名、恢复历史快照或整体提交

## 5. 验收门禁

1. registry 一致性 Exit 0。
2. 活跃区真实死链为 0。
3. INDEX 对 67 条活跃规范真实链接覆盖率 100%。
4. CHK A 类检查全过。
5. `auto-pm doc check --strict` Exit 0。
6. 每个变更文件有前后 SHA-256、回滚路径和 scoped diff。
7. SYS PM_SESSION、DEC、handoff、ledger reconcile 完整闭环。

## 6. 风险与停止条件

- 如发现白名单外引用必须停止并回报，不得自行扩权。
- 如 registry、INDEX 或 CHK 修复互相冲突，保留现场并建立决策问题。
- 如门禁非零，状态保持 `pending_review` 或 `blocked_by_evidence`，不得进入 P1。
- 驾驶舱对冷区元数据报告的依赖解除另建 SW 变更单，不在本 CHG-SPEC 内实施。

## 7. 待架构师决策

- 是否批准 P0 包级实施。
- `050_模板结构规范_TPL` 采用“删除死链引用、保持现有结构约定”方案。
- 台账回链统一改为 registry 版本逻辑锚，不恢复物理版本快照。
- 冷区文件只登记、不批量修复或迁移。
