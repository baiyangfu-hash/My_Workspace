# SW-2026-008“研发母体 + 稳定部署”重制实施方案

> 文档状态：**规划基线已批准；仅 NG-WP-01 与 NG-WP-02 获准执行；NG-WP-03 起仍未授权**  
> 治理真源：`SYS-2026-001_WorkspaceGovernance`  
> 研发母体：`01_Project自动化项目管理/Python自动化项目总库/02_在研项目/SW-2026-008_auto-pm_自动化项目管理工具`  
> 稳定部署：`00_Infrastructure/auto_pm`  
> 运行状态：`My_Workspace/.auto-pm`  
> REC-0 基线：HEAD `313d60fecc19655866f91f9b7cbb089c56df609d`；26 个既有 staged；1 个既有 unstaged SW PM_SESSION  
> 编号规则：本方案使用 `NG-WP-*`，不沿用旧 R1/R2/R3，也不复用旧 WBS-2 至 WBS-8 的完成状态。

> 执行授权：`CHG-DOCU-2026-003` / `DEC-20260905-A646F158`；User 于 2026-09-05 明确批准持续至 NG-WP-02。NG-WP-01 已完成治理登记；NG-WP-02 因候选可移植解释器门禁阻塞。该授权不包含源码回收、产品测试、入口、稳定部署、运行状态、Git stage/commit、发布、切流或回退。

## 1. 目的与最终状态

本方案替代旧的实施路线图作为后续执行候选，但不改写、删除或移动旧文件。旧 ADR、矩阵、WBS、GO/NO-GO、CHG、DEC、handoff 和 PM_SESSION 均作为历史证据保留；只有在本方案获得批准并完成独立治理落账后，才能把旧实施方案登记为 `HISTORICAL_SUPERSEDED`。物理归档、Git 提交和清理分别需要独立批准。

最终状态必须同时满足：

1. SW-2026-008 是唯一可编辑源码、测试、模板和发布配置真源。
2. `00_Infrastructure/auto_pm` 只保存不可变稳定制品、启动器、双槽指针和部署 manifest，不承担日常研发。
3. `.auto-pm` 只保存运行数据库、decision、handoff、报告、缓存和日志，不成为源码或治理规范真源。
4. 研发母体可以在隔离环境独立安装、测试、构建，不依赖稳定部署源码，也不通过 editable、`.pth` 或永久 `PYTHONPATH` 污染运行环境。
5. 发布只能沿“母体 Commit → 候选制品 → 非活动槽验证 → User 批准 → 原子切流”单向发生；任何异常均 fail-closed。
6. 项目归档、恢复、事务、决策和工作流执行共享同一授权、路径白名单和补偿契约，不保留绕过门禁的硬删除入口。

## 2. 已知事实和设计约束

REC-0 已静态扫描稳定部署侧 377 个 Python 文件，形成核心 92 文件依赖闭包和扩展 114 文件闭包。详细证据位于：

- `C:/Users/fubai/.codex/visualizations/2026/09/05/01a06f4b-eb60-7e83-8ee7-f3d84cfec583/REC-0/REC-0_REPORT.md`
- 同目录 `batch_whitelist.json`、`file_manifest.csv`、`topology.json`、`evidence_validation.json`、`freeze_validation.json`

必须承认以下事实：

- `workflow_dtos → change_transaction → workflow_orchestrator` 是真实依赖方向。
- project archive 当前没有接入 `workflow_orchestrator` 或 `change_transaction`；它只能作为后续目标依赖，不能把治理顺序伪装成现有调用链。
- DecisionService 尚未消费 `DISP-20260904-3362BFFF-01`，旧 `DEC-20260904-3362BFFF` 可能被回退选择，必须先补授权阻断。
- orchestrator 存在 `git add .`、路径白名单不严和任意 callback 越权；transaction 存在工作区外路径及失败后丢失恢复材料风险。
- archive 存在 CHG/Git 门禁异常放行、恢复路径越界、无事务补偿、硬删除绕过 protected ID、并发台账风险。
- 现有 archive 和 decision 测试顶层导入 CLI；若入口延后，必须显式批准测试拆分，不能宣称原测试文件可独立提前回收。
- 当前 staged 空白检查失败，属于冻结现场；不得通过格式化、reset、restore、stash、clean 或整体提交处理。

## 3. 交付策略

本任务是跨额度窗口的长任务。每个工作包必须遵循“只读恢复 → 精确报批 → 隔离执行 → 机器门禁 → 独立复核 → PM 收口 → 停机”的顺序。任何工作包完成后不得自动启动下一包。

回收不是从稳定部署目录机械复制源码。执行者必须以部署侧字节为证据，在母体建立最小实现，并同时修复已经识别的安全缺陷；原样复制和安全重构要在白名单中分别列明。凡涉及新测试、测试拆分、公共契约或已有母体文件的改动，都必须进入该包的新 CHG、新 DEC 和精确 pathspec。

代码工作一律在独立候选 worktree 或经批准的隔离目录进行。冻结现场只允许只读复核，不允许执行产品命令、测试、格式化或提交。候选验证不得读取或写入真实 `.auto-pm`、真实 Git index 或稳定槽。

## 4. WBS 分解

### NG-WP-00：接管与不可变基线复核

目标：让后续 AI 在每次续跑时确认自己面对的仍是同一现场和同一授权。

输入：本方案、REC-0 产物、ADR-SW008-001、`DISP-20260904-3362BFFF-01`、SYS 与 SW PM_SESSION、当前 Git 状态。

工作：只读核验 Git 根、HEAD、分支、index SHA-256、26 staged、1 unstaged、REC-0 产物哈希、disposition 目标哈希及 8 项 basis evidence。检查是否有新的用户批准；没有则停止。

完成标准：输出新的只读恢复摘要和 `next_legal_action`；任何基线漂移均标记 `BASELINE_DRIFT`，禁止自动修复。

提交边界：无提交、无 PM 落账、无运行状态写入。

### NG-WP-01：旧方案治理归档登记

目标：将旧实施路线图与新方案分清，避免后续 AI 混用旧状态或编号。

工作：新建独立 CHG/DEC，精确列出拟登记为 `HISTORICAL_SUPERSEDED` 的旧规划文档；建立“历史证据 → 新方案”索引。不改写旧文档正文，不移动文件，不删除文件。

最低范围：`005_源码单轨与双槽部署架构决策_ADR.md` 继续作为架构基线；`006_基础设施位与母体差异回收矩阵_MATRIX.md`、`007_WBS2至WBS8实施路线图与验收规程_TEC.md`、`GO_NOGO_CHECK_REPORT.md` 作为历史实施证据。是否物理移入归档目录必须另设工作包。

完成标准：新治理记录明确“旧执行编号和状态不得授权新实施”，且 PM_SESSION/台账对账通过。

提交边界：仅获批治理文档和对应 CHG/台账/PM_SESSION；不含源码、稳定部署、运行状态和冻结 index。

### NG-WP-02：隔离开发面与工具链

目标：建立不会污染稳定运行或冻结现场的母体候选环境。

工作：从获批基线创建独立 worktree；以项目相对方式解析解释器；检测 editable、`.pth`、永久 `PYTHONPATH`、Junction、软链接和硬链接；将 pytest、coverage、junit、ruff、mypy、临时文件输出定向到候选专用目录。

测试：候选内最小 import smoke；导入来源必须位于候选母体；稳定运行 import 来源保持稳定部署；两者均不得回退到另一侧。

完成标准：隔离证据全绿；冻结现场 HEAD/index/status 与 NG-WP-00 指纹一致。

提交边界：优先无源码提交；若需修改母体配置，必须另列精确配置 pathspec，禁止修改稳定部署和根入口。

### NG-WP-03：工作流契约层

目标：在母体建立强类型工作流契约，供事务、决策和编排共同使用。

候选：

- `auto_pm/contracts/workflow_dtos.py`
- `tests/contracts/test_workflow_dtos.py`

工作：依据 REC-0 源 SHA 和母体 ABSENT 前态建立实现；确认 DTO 默认值、枚举、序列化、反序列化和未知字段策略。禁止顺带回收入口或修改部署侧。

门禁：专项 pytest、Ruff、Mypy、import provenance、候选 diff check 全绿。

完成标准：生成结构化 handoff_result 后停机；PM 消费和提交必须分别报批。

### NG-WP-04：事务边界和失败恢复

目标：实现只允许操作获批工作区范围、失败后保留证据且状态真实的事务机制。

候选：

- `auto_pm/application/core/change_transaction.py`
- `tests/application/test_change_transaction.py`
- 为路径 containment、链接、失败补偿新增或拆分的测试文件，须在批准白名单中显式列出。

必须修复：拒绝工作区外绝对路径、`..`、symlink/reparse 越界；created/backup/restore 使用同一 canonical path 契约；恢复失败保留 backup 和错误状态，不得删除恢复材料后声明 `ROLLED_BACK`。

门禁：每个写入/移动/恢复步骤做故障注入；前后目录和字节清单可核验；重复执行幂等；不得访问真实 workspace 状态。

完成标准：专项、回归、Ruff、Mypy、doc check 全绿，输出事务状态机和失败证据。

### NG-WP-05：决策契约与 disposition 强制消费

目标：让任何工作流在执行前识别不可变 disposition，并拒绝已被阻断的旧 DEC。

候选：

- `auto_pm/contracts/decision_package.py`
- `auto_pm/domain/change/decision_service.py`
- 决策专项测试；现有 `tests/contracts/test_decision_package.py` 如保留整文件，需要同时处理其 decision CLI 依赖，或另批批准测试拆分。

工作：定义 disposition 解析、目标 decision SHA-256 核验、blocked/superseded 状态、精确批准文件集合、审批人与时间、未来消费语义。哈希不匹配、记录损坏、目标缺失、状态冲突均 fail-closed。

门禁：真实文件测试覆盖 `DEC-20260904-3362BFFF` 被拒绝；不得修改 `.auto-pm/decisions` 中的历史文件；handoff 和 workflow 消费者测试验证同一结论。

完成标准：旧 DEC 无法再成为恢复、提交、部署或发布授权。

### NG-WP-06：工作流编排安全重制

目标：在母体建立白名单驱动、预检零写、执行可补偿的编排器。

候选：

- `auto_pm/application/core/workflow_orchestrator.py`
- `tests/application/test_workflow_orchestrator.py`
- 经批准的真实 Git 隔离测试。

必须修复：移除/禁用 `git add .` 和无范围 commit；所有 pathspec 使用 canonical containment；限制或移除 `metadata.action` 任意回调；verify-only 不创建目录、审计文件或 decision 状态；禁止默认 partial verification；未实现 resume 时必须显式 fail-closed。

测试：在真实临时 Git 仓库预置 staged/unstaged 哨兵，断言只触及获批候选；测试 blocked disposition、路径越界、callback 越权、执行中断、补偿失败、重复请求和零写预检。不能 mock 掉用于证明安全性的 Git 行为。

完成标准：编排器只产生获批范围差异；没有真实 resume 能力时不得宣传恢复完成。

### NG-WP-07：项目归档领域契约

目标：先定义 archive/restore 的安全语义，再接入现有服务。

工作：定义受保护项目、归档许可、目标路径、archive_code、目录移动、PM_SESSION phase、台账、数据库、扫描器、重复请求、并发锁和补偿顺序；明确“归档”和“硬删除”是不同能力，默认不提供硬删除。

设计要求：archive/restore 使用 NG-WP-04 的事务机制并受 NG-WP-05/06 的批准白名单控制。CHG 查询失败、Git 缺失/非零、台账损坏、路径越界、链接异常和并发冲突全部 fail-closed。

交付物：领域状态机、错误模型、接口契约、故障矩阵、测试矩阵。此包可只做设计与测试骨架；不得移动真实项目。

完成标准：User 对归档语义和是否保留任何 hard-delete 能力作出明确决策。

### NG-WP-08：项目归档核心实现

目标：在母体实现可恢复、可审计、默认无硬删除的 archive/restore。

候选：

- `auto_pm/application/core/project_archive_service.py`
- `auto_pm/application/core/project_service.py`
- `auto_pm/application/core/project_scanner.py`
- core 专项测试与经批准的 service/scanner 回归测试。

执行规则：不得整文件盲拷差异版本；`project_service.py`、`project_scanner.py` 的非归档差异按 hunk 归属审查。入口文件不进入本包。

测试：CHG/Git 异常零副作用；protected ID 在 force/service 路径仍拒绝；显式 dest、ledger `..`、symlink/reparse 越界；move、phase、ledger、DB 每一步失败补偿；并发编号；重复归档；精确 archive_code 更新；扫描深度、误分类和重号项目。

完成标准：事务和授权集成测试全绿；真实项目目录未被测试触及。

### NG-WP-09：项目归档 CLI 与硬删除收口

目标：只在核心安全契约通过后暴露用户入口。

候选：

- `auto_pm/ui/cli/project.py`
- `tests/core/test_project_archive.py` 中的 CLI 测试或批准后的拆分文件
- `tests/core/test_project_service.py`、`tests/core/test_project_scanner.py` 的精确归档相关改动
- 仅在整文件 CLI 确需时加入 `project_fact_snapshot.py`、`project_fact_service.py` 及其测试；不能自动扩大。

工作：archive/restore/list 入口必须调用同一领域服务；删除入口不得直接 `shutil.rmtree`。若 User 决定保留 hard-delete，必须使用独立高风险命令、protected ID 二次门禁、精确项目路径、备份证据和单独批准。

完成标准：所有 CLI 入口均无门禁绕过；帮助文本和退出码准确；原有顶层 CLI 测试依赖已显式处理。

### NG-WP-10：母体全量 Gate 1

目标：证明母体可以独立成为候选制品的唯一源码。

门禁顺序：import provenance；专项测试；受影响消费者回归；pytest 全量；Ruff check；Ruff format check；Mypy strict；doc check；spec check；Git diff check；死链/CLI 注册/动态资源检查。每项记录命令、解释器、工作目录、开始结束时间、Exit Code、测试计数和报告 SHA-256。

失败处理：只回到缺陷所属的已批准工作包修复；不修改稳定部署；不扩大 pathspec；不以 mock、hardcode、自动格式化冻结现场或降低门禁制造绿灯。

完成标准：全部 Exit Code 0，候选工作树可解释，形成 Gate 1 不可变证据包并由独立 AI 复核。

### NG-WP-11：冻结候选提交与制品构建

目标：将已验证母体锁定为唯一 source commit，并生成不可变候选制品。

前置：User 单独批准提交；候选提交只包含逐包已验收 pathspec 与已批准治理证据。禁止对当前冻结 index 使用 `git add .`、整体 commit、reset、stash 或 clean。

工作：提交后记录 commit SHA；从该 commit 构建 wheel/sdist或批准格式的部署制品；生成逐文件 SHA-256 manifest、依赖锁、解释器和构建工具版本；从空隔离目录完成安装验证。

完成标准：制品可以仅由 source commit 重建；构建前后源码树和制品哈希证据完整。

### NG-WP-12：稳定部署双槽骨架

目标：把 `00_Infrastructure/auto_pm` 改造成受控稳定容器。

前置：独立部署结构 CHG/DEC；不继承源码回收批准。

工作：实现 `launcher/`、`releases/<release-id>/`、`active_release.json`、`previous_release.json`、`deployment_manifest.json` 和 `.lock`；指针只接受合法 release-id，解析路径必须位于 releases，manifest 与文件字节完全匹配。写指针需同卷临时文件、刷盘和 `os.replace`。

完成标准：只建立骨架和隔离测试，不切换当前运行入口，不部署正式 active。

### NG-WP-13：根入口与环境解耦

目标：生产入口只加载 active，active 无效时只允许已批准 previous，双槽均无效即非零退出。

工作：移除生产 editable/`.pth`/永久 PYTHONPATH 依赖；根入口和初始化脚本不再回退母体。对 active 正常、active损坏/previous正常、双指针失效、JSON损坏、release-id越界、manifest不匹配做动态测试，全部断言不导入母体。

完成标准：研发和稳定 import 物理隔离；入口变更仍未切流，等待 Gate 2 和 User 批准。

### NG-WP-14：非活动槽 Gate 2

目标：在非活动槽验证获批候选，不影响 active。

工作：部署候选到隔离非活动槽；在净化子进程验证 import provenance、CLI help/doctor、只读 status、spec/doc dry-run、GUI smoke、归档 dry-run 和关键故障注入；核验制品/manifest 哈希。

完成标准：形成发布申请、source commit、version、artifact hash、manifest hash、目标槽、Gate 1/2 报告和回退目标；然后物理停机等待 User 批准。

### NG-WP-15：正式发布与原子切流

目标：仅在 User 对锁定制品明确批准后完成发布。

工作：签发获批 Tag；持锁部署非活动槽；原子更新 previous/active；运行 launcher 冒烟；记录切流前后指针和哈希。失败候选标记 rejected/quarantined，保留现场，不自动删除。

完成标准：active 只指向获批稳定制品；稳定运行验证通过。任何自动回退只允许 active 到已验证 previous，并必须产生告警。

### NG-WP-16：回退演练、观察期和最终收口

目标：证明稳定运行、可回退和治理证据完整。

工作：经独立批准进行指针损坏和 previous 回退演练；完成观察期；核对 SW 的 CHG/PM_SESSION/发布记录与 SYS 治理落账；运行 ledger reconcile；由 User 最终验收。

完成标准：Gate 1/2、发布、运行、回退和 PM 证据一致；工程状态方可从 NO-GO 变为 GO。

### NG-WP-17：旧方案物理归档与历史候选处置

目标：在新架构稳定验收后处理旧方案与历史平铺代码。

工作：先生成精确清单、哈希和恢复位置；旧规划文档按治理索引移入批准的历史目录；稳定容器中不再承担运行责任的平铺源码只可归档，不默认删除。运行状态、失败候选和恢复锚点按保留策略处理。

完成标准：归档台账逐项可追溯、恢复演练通过、所有引用无死链。删除、清理和 Git 提交仍是独立批准边界。

## 5. 批次通用验收模板

每个 NG-WP 必须提供：

1. `skill_context`：baseline_documents、goal、strict_constraints、decision_id、change_id、精确 pathspec、源/目标前态 SHA-256。
2. `handoff_result`：实际 changed_files、每条命令及 Exit Code、测试计数、报告路径和 SHA-256、未运行项、已知限制、`pm_closure`。
3. 范围证明：候选提交差异集合等于批准集合；冻结现场 index/status 指纹前后一致。
4. 安全证明：路径 containment、链接边界、零写预检、fail-closed、失败补偿、幂等和并发测试。
5. 质量证明：专项、回归、全量 pytest、Ruff、Mypy、doc/spec、diff check 均由机器 Exit Code 判定。
6. PM 收口：变更单状态流转、PM_SESSION、handoff 消费、台账和反馈完成一致性对账；执行者不得自行宣布项目结项。

## 6. 长任务、额度重置与断点续跑协议

后续实施 AI 必须把本工作视为可跨多个额度窗口持续的长任务，不得因额度临近耗尽而删减测试、扩大批次、跳过门禁或宣称完成。

每次开始或恢复时：

1. 读取 `AGENTS.md`、本方案、REC-0 报告、SYS/SW PM_SESSION、当前 CHG/DEC/handoff。
2. 只读执行 NG-WP-00，核验 Git 根、HEAD、index、status、批准范围和上次 checkpoint。
3. 一次只处理当前获批工作包；不得自动进入下一 NG-WP。
4. 将进度、已完成门禁、剩余动作、文件哈希和下一条安全命令写入 `.agents/` 下的临时 checkpoint 或当前 task 的结构化状态；不得把过程文件加入 Git。

当可用额度不足或达到限制时，必须执行以下协议：

- 立即停止新的写入、测试、提交、发布或切流动作；不得在命令执行中途人为终止会破坏原子性的步骤。
- 等待额度按账户计划自动重置，不购买、不兑换、不请求使用 usage-reset credit，除非 User 另行明确授权。
- 若 Codex 支持 task heartbeat/长期目标，则保持当前 task 和目标为未完成状态，在预计重置时间后唤醒同一 task；状态无变化时保持安静。
- 额度重置后继续同一 task：先执行 NG-WP-00 和 checkpoint 一致性核验，再从最后一个未完成验收项继续，禁止从头重复已验证且哈希未变的工作。
- 如果平台不能自动唤醒，则留下下面的精确续跑提示，等待 User 在同一 task 恢复；不得把“需要等待额度”标成 blocked 或 completed。

续跑提示：

```text
继续执行 SW-2026-008“研发母体 + 稳定部署”重制实施方案。先读取 AGENTS.md、SYS-2026-001_WorkspaceGovernance/01_项目文档/17_SW-2026-008_研发母体稳定部署_重制实施方案_PM.md、REC-0 报告以及最新 SYS/SW PM_SESSION、CHG、DEC、handoff 和 .agents checkpoint。先只读执行 NG-WP-00，确认 Git 根、HEAD、26 staged、1 unstaged、index SHA-256、批准工作包和 checkpoint 未漂移。只继续当前已获 User 明文批准的 NG-WP；不得使用旧 R1/R2/R3 或旧 WBS 完成状态授权，不得修改 .trae、Obsidian、稳定部署、运行状态、入口或冻结 index，除非它们已被当前新 DEC 精确列入白名单。完成本包全部机器门禁并生成 handoff_result 后停止，等待 PM 收口和下一包批准。若额度耗尽，保存 checkpoint，等待额度自动重置后在同一 task 继续。
```

## 7. 报批结论

本方案只批准规划产物本身，不批准 NG-WP-01 至 NG-WP-17 的任何执行，不批准源码回收、测试写入、环境调整、Git stage/commit/Tag、部署、切流、回退、旧方案物理归档、清理或删除。

建议批准方式：先批准本方案成为新的实施规划基线；随后单独批准 `NG-WP-00 + NG-WP-01` 做治理登记，再逐包批准。任何批准必须包含新的 decision_id、change_id、精确文件白名单和提交边界。
