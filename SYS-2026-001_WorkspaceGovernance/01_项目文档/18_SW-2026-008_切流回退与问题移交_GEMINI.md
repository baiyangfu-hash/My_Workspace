# SW-2026-008 切流回退与问题移交说明（移交 Gemini 处理）

> 文档状态：CHG-SCPT-2026-018 项下正式移交件
> 编制：ZCode，2026-09-05
> 读者：接手 SW-2026-008 缺陷修复的 AI（Gemini）与 User
> 治理真源：`SYS-2026-001_WorkspaceGovernance`；协作规则：根目录 `AGENTS.md`（必读）

## 1. 当前运行状态（回退后，已验证）

- **运行方式**：工作区根 `main.py` → `00_Infrastructure/auto_pm` **平铺源码**直接上线（sys.path 注入，无 editable/.pth 依赖）。QML 入口函数为 `run_qml_gui`（注意：**不存在 `run_qml_app`**，历史入口曾调错导致无法启动）。
- **容器双槽**：`00_Infrastructure/auto_pm/active_release.json` 与 `previous_release.json` 均为 `null`（未切流态）。首个 release `releases/1.2.3-d41eb38/`（450 文件，manifest 已登记）保留在位作为证据与后续修复基线。
- **Git**：主工作区分支 `feature/trae-pro-20260509`，HEAD=`dc0b2fb`+回退提交；研发母体候选树在 detached worktree `C:/Users/fubai/.codex/visualizations/2026/09/05/01a06f4b-eb60-7e83-8ee7-f3d84cfec583/NG-WP-02/candidate`（HEAD=`25ee356`，含全部 NG-WP 母体代码与测试，**勿删**，它是唯一的新架构源码）。
- **.venv**：无 auto_pm 安装（NG-WP-13 已卸载 editable）。Git 钩子 v3 通过 `PYTHONPATH=容器` 每次调用注入运行（提交门禁可用）。

## 2. 时间线与提交索引

| 提交 | 内容 |
|---|---|
| `efee250` | NG-WP-10 Gate1 整改收口（10 项门禁全绿：pytest 1900 passed） |
| `6b02646` | NG-WP-11/12 收口：candidate 冻结 `87183fb` + wheel `auto_pm-1.2.3` + 双槽骨架（launcher/releases/双指针/manifest）+ 钩子 worktree 解析修复 |
| `d41eb38` / `d461015` | NG-WP-13：双槽 bootstrap 入口（`launcher/bootstrap.py`，active→previous 唯一回退）、main.py 去母体回退、`.venv` 卸载 editable、钩子 v3；全量回归 1946 passed |
| `d31f892` | NG-WP-14：release `1.2.3-d41eb38` 部署非活动槽 + Gate 2 十用例净化验证全绿 + 入口 API 缺陷修复（run_qml_app→run_qml_gui） |
| `dc0b2fb` | NG-WP-15：Tag `sw-2026-008-1.2.3-d41eb38` + 原子切流（active 指向 release） |
| 本次回退提交 | 指针重置 null + main.py 恢复平铺运行态（CHG-SCPT-2026-018 实施步 1） |

关键报告（`.auto-pm/reports/`，不入 Git）：`NG-WP-10_Gate1_remedy_report_2026-09-05.md`、`NG-WP-13_isolation_evidence_2026-09-05.md`、`NG-WP-14_gate2_report_2026-09-05.md`、`NG-WP-15_switchover_report_2026-09-05.md`。

## 3. 问题簇详情（本单要解决的三个层次）

### P1 启动闪退（用户报告的直接现象）
- 现象：双击 bat 后控制台秒退、无任何错误。回退已恢复可用性，但**闪退机理未完全闭环**。
- 已证实的直接诱因：启动窗口期 SQLite 运行库锁竞态（残留调试实例持 `.auto-pm/index.db` WAL 锁）+ `start` 控制台随进程关闭吞掉报错。
- 未闭环点：用户唯一一次闪退发生在无我方实例占用时，不排除首同步异常路径未兜底（见 P2）。
- 修复方向：启动脚本崩溃留痕（上次尝试因 `cmd /c` 嵌套引号 + 中英双代码页解析撕裂失败——**教训：bat 内层命令字符串必须纯 ASCII，中文提示只能放外层 chcp 65001 之后的 echo**）；可选：单实例锁（如 `.auto-pm/index.db` 独占探测）。

### P2 首同步 FK 失败（兼容性风险）
- 现象：release 代码首启同步报 `FOREIGN KEY constraint failed`（`auto_pm/infrastructure/db/sync.py:139` 捕获记录，进程存活；重试自愈）。
- 数据侧已验：`.auto-pm/index.db` integrity ok、`PRAGMA foreign_key_check` 0 违规（表：projects / change_requests / approval_history / impact_analysis / scan_log，FK 带 CASCADE）。
- 已排除：插入顺序正常（sync 先项目后变更）；干净副本 + force_full 重同步成功。失败发生在"旧运行库状态 + release 新代码首同步"的组合瞬间，**具体触发语句未捕获**（当时未开 debug 日志）。
- 修复方向：首次切流前的迁移预检/预同步；或 sync 内对 FK 场景显式处理+重试；用 `logging.DEBUG` 复现抓语句。

### P3 release 功能差距（最严重、结构性）
- 事实：release（candidate 回收）较稳定平铺源码 **缺 14 个模块**：`application/core/{dogfood_runner,pm_resume_service,pm_saga_service,project_fact_service}`、`contracts/{pm_resume_context,project_fact_snapshot}`、`domain/change/{substance_checker,substance_injector}`、`infrastructure/git_hook_enforcer`、`ui/cli/{decision,dogfood,git_hook,handoff,pm}`；且 156 个同名文件内容漂移（例：`ai_handoff_service.py` 54,837B→5,966B，"最小实现"回收策略所致）。
- 影响：GUI 核心流程可用（已实测：8 项目/119 变更加载、详情页正常）；但 CLI 功能面缩水（`python -m auto_pm pm/dogfood/handoff/decision/git-hook` 不存在），部分深层服务为瘦身版。
- 注：Git 钩子不受影响（钩子 v3 以 `PYTHONPATH=容器` 走平铺治理源码，`git_hook.py`/`git_hook_enforcer.py` 完整存在于平铺侧）。
- 修复方向（按 CHG-SCPT-2026-018 批准的分批 pathspec）：以稳定平铺源码字节为证据，逐模块回收进母体候选树（`auto_pm.change→auto_pm.domain.change` 等 4 组导入 shim 已存在于 `auto_pm/__init__.py`，回收时保持）；每批跑母体全量 pytest（当前基线 1946 passed/18 skipped，runner：主工作区 `.agents/sw-2026-008_ng-wp-02_candidate_runner.py`）+ Gate 2 净化验证，再按 NG-WP-11 流程重建 release 与切流。

## 4. 架构事实（修复时必须知道）

1. **双槽机制**：指针文件 schema=`release_pointer.v1`；manifest schema=`deployment_manifest.v1`；release-id 正则 `^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$`；校验/原子写/锁全部在 `00_Infrastructure/auto_pm/launcher/bootstrap.py`（stdlib-only）与母体 `auto_pm/application/core/deployment_service.py`（`DeploymentContainer`）。bootstrap 测试：`tests/application/test_deployment_bootstrap.py`（candidate 树）。
2. **导入 shim**：`auto_pm/__init__.py` 把 `auto_pm.db→infrastructure.db`、`auto_pm.change→domain.change`、`auto_pm.core→application.core`、`auto_pm.models→domain.models`——稳定侧旧布局导入在母体新布局上可用。
3. **模板解析**：`app_context.templates_dir = 包父目录/templates`——release 布局必须 `auto_pm/` + `templates/` 并排（现有 release 已如此，167 个模板文件）。
4. **候选树约定**：主工作区与 candidate worktree 的容器/入口文件需逐字节一致（历史闸门有双树哈希校验）；母体代码提交只发生在 candidate worktree（detached），主工作区只提交治理与生产入口。
5. **提交门禁**：pre-commit=全仓台账一致性（`auto_pm git-hook pre-commit`，v3 钩子）；commit-msg=生产代码变更必须在提交信息绑定 `[CHG-SCPT-2026-xxx]`。

## 5. 治理约束（不可越过）

- 遵守根 `AGENTS.md` 四阶段铁律：澄清→报批→执行→验收；每批修复需独立 CHG/DEC + User 明示批准 + 精确 pathspec；禁止 `git add .`/reset/stash/clean 冻结现场。
- 禁止修改：`.trae/`、Obsidian 规范库、稳定部署（本单授权范围内的指针/入口除外）、真实 `.auto-pm` 运行库（除产品自身读写）。
- 状态基线：方案 `SYS-2026-001_WorkspaceGovernance/01_项目文档/17_SW-2026-008_研发母体稳定部署_重制实施方案_PM.md`；本单 `.../01_变更单/CHG-SCPT/CHG-SCPT-2026-018.md`。

## 6. 建议作业顺序

1. bat 崩溃留痕（纯 ASCII 内层命令，外层中文提示）+ 复现验证；评估单实例锁。
2. FK 首同步根因：DEBUG 日志复现（"旧库 + 新代码首同步"组合），落点大概在 repository UPSERT 与历史数据形态差异。
3. 功能差距回收：14 模块 + 漂移文件按依赖序分批（建议先 `git_hook_enforcer`/`pm` 族 CLI，再 saga/fact 服务），每批全量回归 + 独立 CHG。
4. 全量回收完成后：NG-WP-11 流程重建 release → Gate 2 → User 批准 → 重新切流（双槽机制与工具链已就绪，指针操作用 `DeploymentContainer`，勿手改 JSON）。
