# SW-2026-008 驾驶舱 Dogfooding 迭代方案与 WBS

## 0. 文档控制

- 归属项目：`SYS-2026-001_WorkspaceGovernance`
- 产品项目：`SW-2026-008_auto-pm_自动化项目管理工具`
- 当前运行位：`00_Infrastructure/auto_pm`
- PLC 活体标杆：`DJ-2026-005`
- 日期：2026-09-01
- 状态：阶段 1 规划完成，等待用户审批；未启动实现

## 1. 决策摘要

结论：**可行，但只能采用“受控 Dogfooding”，不能让正在修改的驾驶舱自行批准和发布自己。**

`pm-workflow + auto-pm 驾驶舱 + fullstack-engineer + plc-electrical-engineer` 能构成闭环，但四者不是平级编码角色：

| 角色 | 责任 | 禁止事项 |
|---|---|---|
| 用户 / CCB | 批准需求、取舍和最终验收 | 不承担代码调研与测试体力工作 |
| PM 技能 | 澄清、WBS、变更单、交接、PM_SESSION、台账收口 | 不直接实现驾驶舱业务代码，不自批变更 |
| 稳定版驾驶舱 | 提供项目状态、门禁、证据和回退控制面 | 不以候选版本的自检结果作为唯一放行依据 |
| 全栈技能 | 实现 Python/QML/CLI/测试，是驾驶舱代码主执行者 | 不代替 PM 改写最终台账和审批状态 |
| PLC 技能 | 作为 PLC 域客户和验收者，维护域测试样例与标杆验证 | 不直接成为驾驶舱 Python/QML 的共同 owner |
| `DJ-2026-005` | 验证 PLC 检查、模板、变量表、HMI 原型等真实能力 | 不承担驾驶舱产品账和发布账 |

当前成熟度处于“驾驶舱辅助的人工闭环”：运行位已迁移，读取交接和 GUI 收口提示已存在，但 PM 技能描述的 `handoff` 命令尚未落地。目标应是“隔离环境下的可重复自托管”，不追求无人审批的全自动发布。

## 2. 当前完成情况

### 2.1 已完成

- 工作空间治理入口和 `SYS-2026-001` 治理台账已建立。
- 驾驶舱运行代码已迁入 `00_Infrastructure/auto_pm`，根入口和环境安装优先使用基础设施位。
- Doc-as-Code 已拆分代码源与文档源，旧 `SW-2026-008` 母体暂保留产品文档、变更历史和回退参考。
- 文档资产完成首轮冷热分层；变更管理资产只归档、不删除。
- 当前实际加载的是基础设施位 `auto_pm 1.2.3`，迁移提交链可追溯。
- `AiHandoffService` 已具备待办交接读取、规范化和基础校验能力；GUI 能生成 PM 收口上下文。
- `DJ-2026-005` 已具备 PLC、文档和 HTML HMI 原型的真实标杆价值，可用于领域回归。

### 2.2 尚未完成

- 规划前工作树快照有 243 条变更记录，分布于旧驾驶舱母体、PLC 标杆、基础设施位、SYS 台账和运行态文件；本规划文档会再增加一项可解释的 SYS 资产，整体仍不具备直接启动自托管迭代的干净基线。
- PM 技能声明的 `auto_pm handoff ...` 与 `auto_pm handoff close ...` 当前均不是可执行 CLI 命令，技能契约领先于驾驶舱实现。
- 交接服务目前偏“只读收件箱”；缺少创建、原子消费、幂等收口、失败补偿和完整审计证据。
- 候选版本和稳定版本尚未物理隔离，存在“自己生成、自己检查、自己宣布通过”的循环自证风险。
- `doctor` 在工作空间根找不到 `pyproject.toml` 时使用硬编码 `1.1.0`，因此会把实际 `1.2.3` 运行包显示成旧版本；版本探测尚未使用基础设施代码真源。
- `DJ-2026-005` 仍有项目结构重复和部分验收未闭环，不能立即作为满分基因提纯源。
- 旧母体与基础设施位仍处于双轨期，代码与活跃文档的唯一真源边界需要最终锁定。

## 3. 目标运行模型

### 3.1 三平面隔离

1. **稳定控制面**：使用已提交、已验证的驾驶舱版本执行建单、门禁和最终复核；运行时只读，不在其目录中开发。
2. **候选开发面**：每个驾驶舱变更在独立 Git worktree、`codex/` 分支和独立虚拟环境中实现与自测。
3. **证据平面**：每次运行用唯一 `run_id` 把基线 SHA、候选 SHA、命令、退出码和报告写入 `.auto-pm/reports/dogfood/<run_id>/`。

### 3.2 标准闭环

1. 用户批准范围后，稳定版驾驶舱创建 `CHG-SCPT` 与结构化交接请求。
2. PM 技能生成 `skill_context`，指定基线文档、目标、禁止项和验收命令。
3. 全栈技能在候选开发面实现驾驶舱代码并完成候选自检。
4. 稳定版驾驶舱从外部重新检查候选开发面，避免候选版本自证。
5. 涉及 PLC 域能力时，PLC 技能以 `DJ-2026-005` 或隔离夹具执行领域验收。
6. PM 技能核对证据后执行一次性收口；用户完成最终验收。
7. 失败时保留证据并回到稳定版，候选分支不得污染主工作树。

## 4. 强制护栏

- **双钥匙放行**：候选自检通过 + 稳定版外部复核通过，缺一不可。
- **禁止自批**：候选驾驶舱不能修改自己的批准状态或把自己的 handoff 标记为最终闭环。
- **干净树门禁**：创建候选 worktree 前，基线必须无未解释变更；运行态 JSON、数据库和缓存不得进入提交。
- **单一代码真源**：驾驶舱代码只在 `00_Infrastructure/auto_pm` 维护；旧母体代码进入只读回退态。
- **分层文档真源**：产品规划、变更和发布记录归 `SW-2026-008`；工作空间治理决策归 `SYS-2026-001`；不把历史变更档案复制到基础设施位。
- **原子收口**：先验证、再写 PM_SESSION/变更台账/反馈，最后消费 handoff；任一步失败都必须可重试且不能产生重复记录。
- **标杆防污染**：`DJ-2026-005` 未全绿前只能作为诊断标杆，不能直接反向提纯模板。
- **保留人工 CCB**：涉及规范库、项目结构、发布、删除或回退策略的变更必须由用户批准。

## 5. WBS 分解

| WBS | 工作包 | 主要任务 | 交付物 | 前置依赖 | 完成门槛 |
|---|---|---|---|---|---|
| 0 | 基线止血与分流 | 以规划前 243 条变更快照为起点，并纳入本规划新增 SYS 资产；按 SYS、SW、DJ/SysLib、infra、运行态分组，分别验证、提交、归档或恢复；将 313 行且含回流 §7 的 SYS PM_SESSION 合规归档到 150 行以内 | 干净基线、变更分组清单、会话归档、恢复点 | 无 | `git status` 干净；PM_SESSION 门禁通过；每批有 owner 和验证证据 |
| 1 | 唯一真源锁定 | 明确 infra=代码、SW=产品账、SYS=治理账；冻结旧母体代码写入；记录版本指纹 | 真源矩阵、双轨退出条件、风险更新 | WBS 0 | 任一资产只有一个活跃 owner；入口报告版本与 SHA |
| 2 | PM 交接契约修复 | 定义 handoff v1；实现 `create/list/show/close`；以 `request_id` 收口；同步 PM/全栈/PLC 技能命令 | CLI、Schema、技能文档、兼容说明 | WBS 1 | 技能示例逐条可执行；错误命令不再存在 |
| 3 | Saga 原子收口 | 增加预检、幂等键、文件锁/原子替换、失败日志、补偿与重试；最后才标记 consumed | 收口服务、失败恢复报告、单元/集成测试 | WBS 2 | 重复执行无重复台账；故障注入后可安全重试 |
| 4 | 自托管隔离基建 | 建立稳定/候选双 worktree、独立环境、路径白名单、基线/候选指纹和外部复核脚本 | Dogfood runner、运行说明、回退演练记录 | WBS 1、3 | 候选失败不改变稳定控制面；可一键回到已提交版本 |
| 5 | Pilot A 只读闭环 | 用驾驶舱管理自身的 Grooming：读取 SW-2026-008、生成 handoff、列出/查看、PM 消费，不改产品代码 | 首份完整 dogfood 证据包 | WBS 4 | 请求、执行回执、PM 收口、台账对账全部一致 |
| 6 | Pilot B 低风险代码 | 首选修复 `doctor` 的版本真源解析：从基础设施包元数据读取实际版本并消除硬编码 `1.1.0` 回退；由全栈技能在候选面实现，稳定版复核 | 首个代码 Dogfood CHG 和提交 | WBS 5 | `doctor`、包版本和 `pyproject.toml` 一致；pytest、ruff、mypy、python/doc/spec 门禁全绿；回退演练通过 |
| 7 | PLC 域消费验收 | 先收口 `DJ-2026-005` 结构重复与剩余门禁，再验证 PLC checker、模板、变量表、HMI 静态原型；PLC 技能只做域验收 | PLC 域验收矩阵、标杆差距清单 | WBS 0、6 | `plc check`、`doc check`、台账对账全绿；不要求真实硬件下发 |
| 8 | 中风险 Dogfood | 选择 handoff 工作队列或门禁编排等真实驾驶舱功能，完整走 PM→全栈→PLC验收→PM 收口 | 第二个 Dogfood 版本、SLO 数据 | WBS 6、7 | 连续两批无人工补账、无跨树污染、可重复回退 |
| 9 | 迁移收口与退路降级 | 评估移除旧代码回退；旧母体保留产品文档和历史；更新根入口、安装说明和灾备说明 | 单轨运行决策、回退包、迁移收口报告 | WBS 8 | 至少两个稳定版本通过；用户批准后才移除旧代码回退 |

## 6. 里程碑与审批点

| 里程碑 | 内容 | 审批点 |
|---|---|---|
| M0 | 工作树恢复可控和干净 | PM 自检，用户仅看分组结论 |
| M1 | PM 技能命令与驾驶舱能力一致 | 用户批准 CLI/收口契约 |
| M2 | 稳定/候选隔离和回退演练完成 | 用户批准进入自托管 Pilot |
| M3 | 只读 Dogfood 闭环成功 | 无产品代码风险，可进入低风险代码 Pilot |
| M4 | 首个代码 Dogfood 成功 | 用户验收变更和回退证据 |
| M5 | `DJ-2026-005` PLC 域验收通过 | 用户确认标杆达到模板提纯资格 |
| M6 | 连续两批稳定后迁移收口 | 用户决定是否移除旧母体代码回退 |

## 7. 验证矩阵

| 层级 | 最低验证 |
|---|---|
| PM 契约 | CLI 帮助与技能示例一致；Schema 校验；重复 close 幂等；缺失 CHG/证据时硬失败 |
| 驾驶舱核心 | 全量 pytest（禁用遗留覆盖率副作用）、ruff、mypy、`auto_pm python check`、GUI/CLI smoke |
| 工作空间 | `auto_pm doctor`、`auto_pm spec check`、Markdown 链接检查、根目录临时文件检查 |
| 变更账 | `change` 状态、PM_SESSION、版本台账、`ledger reconcile` 四方一致 |
| PLC 域 | `auto_pm plc check`、`auto_pm doc check --strict`、变量/接口一致性、HMI 导航和 JS 静态检查 |
| 隔离与回退 | 候选故障注入、稳定版外部复核、候选丢弃/重建、证据留存 |

## 8. 回退方案

### 8.1 回退触发条件

- 候选版本导致稳定入口不可用、账目部分写入、门禁误放行或跨项目误修改。
- 同一 handoff 重试产生重复 CHG、重复 PM_SESSION 记录或错误 consumed 状态。
- PLC 标杆出现相对稳定基线的行为回归。
- 无法解释的工作树扩散或运行态文件进入提交。

### 8.2 回退动作

1. 停止候选执行，不修改稳定控制面。
2. 固化当前 `run_id` 证据、差异和失败日志。
3. 回到最近通过双钥匙门禁的提交；已合入的问题用新的回退提交纠正，不重写历史。
4. 保留旧母体代码回退入口，直到 M6 获批。
5. 修复后从新的候选 worktree 重跑全闭环，不复用不可信运行态状态。

## 9. 推荐实施顺序

1. 先执行 WBS 0，停止把当前多项目变更继续叠加到同一工作树。
2. WBS 1 至 WBS 4 作为基础设施批次，优先修复 PM 技能与驾驶舱的真实契约，不做 UI 扩张。
3. WBS 5 先做只读 Pilot，证明闭环而不是证明功能数量。
4. WBS 6 选择低风险、可回退的小功能；当前未隔离的 Modbus 文案变更不直接充当首个 Dogfood 样本。
5. WBS 7 在 `DJ-2026-005` 自身恢复全绿后再执行，避免病态标杆污染模板。
6. 只有 WBS 5 至 WBS 8 连续稳定后，才讨论移除旧母体代码回退。

## 10. 本轮边界

- 本文只完成可行性评估、目标架构、回退设计和 WBS，不执行代码修改。
- 不迁移历史变更档案，不改写 Obsidian 全局规范，不触碰真实 PLC/HMI 硬件。
- 下一步若获批准，只启动 WBS 0；WBS 2 及以后需在基线清洁后重新报批具体受影响文件。
