# SW-2026-008 驾驶舱强制闭环后续 WBS 校准

## 1. 校准依据

- 用户确认的能力路线：C1R 控制面绑定与 Resume 编译；C1S 强制 PM 路由；C2/C3 handoff 生命周期与决策包连接；C5/C6 真实门禁证据与可恢复 PM Saga；C7 跨模型冷启动 Dogfood。
- 当前事实：`RESUME-C9C54866F5B61519`，控制面为 `SYS-2026-001`，无 active handoff，`CHG-SCPT-2026-156` 已关闭。
- 本文只校准后续路线，不重写 RC1 已验收的事实，也不将已实现的局部能力扩大解释为 C2 至 C7 全部完成。

## 2. 已交付与未交付边界

| 能力包 | 当前结论 | 证据与边界 |
|---|---|---|
| C1R | PASS | `pm-resume.v1` 输出 SYS 控制面、事实指纹、read_set、预算和下一合法动作。 |
| C1S | PASS | `pm-workflow` 已要求正式阶段 0/1 先消费有效 resume evidence。 |
| C2a 生命周期 | PASS | claim/start/heartbeat/result-submit/timeout/requeue/close 已实测；`manual` 不代表启动 IDE。 |
| C2/C3 决策包连接 | NOT_STARTED | 尚未把批准范围、决策包和 execution package 建成不可绕过的自动连接。 |
| C5 真实门禁证据 | PARTIAL | result preflight、伪 PASS/缺字段/过期 lease 拒绝已验证；尚未覆盖所有决策包与跨资产证据门禁。 |
| C6 可恢复 PM Saga | PARTIAL | 本次按 handoff、CHG、PM_SESSION、feedback、ledger 顺序完成真实收口；尚未实现跨资产事务日志、故障注入补偿和恢复编排。 |
| C7 跨模型 Dogfood | NOT_STARTED | JSON 契约是中立的，但尚未由 Codex、Antigravity、Cursor/Trae 等独立会话按同一 checkpoint 实测冷启动和下一动作一致性。 |

## 3. 后续 WBS

| 批次 | 工作包 | 目标 | 完成标准 | 当前批准状态 |
|---|---|---|---|---|
| W0 | C2/C3 决策包连接 | 将阶段 1 批准范围、结构化决策包和 execution handoff 绑定。 | 未获批准的 scope 无法进入 execution；批准事实和 request_id 可审计。 | NOT_APPROVED |
| W1 | C5 证据门禁扩展 | 将 result、变更范围、决策包、lease 和 domain evidence 统一预检。 | 伪 PASS、跨项目、过期事实、缺批准和缺领域证据全部拒绝。 | NOT_APPROVED |
| W2 | C6 可恢复 PM Saga | 为 handoff、CHG、PM_SESSION、feedback、ledger 建立顺序日志、失败注入、补偿和恢复。 | 中断后可从持久化 checkpoint 恢复；不产生双写或漏账。 | NOT_APPROVED |
| W3 | C7 跨模型冷启动 Dogfood | 由不同 IDE/模型独立消费同一 resume/checkpoint。 | 状态、read_set、预算、下一合法动作一致；差异可定位。 | NOT_APPROVED |

## 4. 执行约束

- 每个工作包必须重新执行阶段 0 和阶段 1 报批；RC1、closed CHG 和本文件均不构成自动开工授权。
- W0 是后续前置；W1/W2/W3 不并行抢跑。W2 只有在 W1 的真实证据门禁可用后才开始。
- C7 必须使用真实独立 Agent 会话；不能以同一会话模拟多个模型，也不能把格式化 JSON 当作 Dogfood 通过证据。
- 不得为了通过测试 Mock 核心路由、Hardcode 批准结论、扩大全局 ignore 或再次全仓格式化。

## 5. 下一合法动作

用户若决定继续驾驶舱能力迭代，下一步是对 W0/C2-C3 发起新的阶段 0：运行 fresh `pm resume SW-2026-008 --json`，读取其 read_set，并只读勘测现有 decision package、handoff create 和 stage gate 路径；随后提交阶段 1 实施方案等待批准。
