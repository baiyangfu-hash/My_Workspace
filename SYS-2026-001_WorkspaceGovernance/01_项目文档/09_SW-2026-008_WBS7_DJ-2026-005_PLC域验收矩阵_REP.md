# SW-2026-008 Dogfooding WBS7：DJ-2026-005 PLC 域验收矩阵

## 1. 文档控制

- 归属项目：`SYS-2026-001_WorkspaceGovernance`
- 被验收标杆：`DJ-2026-005`
- 驾驶舱运行位：`00_Infrastructure/auto_pm`
- 验收日期：2026-09-01
- 验收边界：PLC 静态规范、工程文档、变量表解析、HTML 原型完整性；不包含现场硬件编译、下装和真实 I/O 联调

## 2. 验收结论

**WBS7 静态域消费通过，可进入 WBS8。**

标杆项目已通过驾驶舱可执行的 PLC 结构门禁、变更台账对账和原型函数完整性检查。两类非阻塞观察项继续保留：实质文档中 `INT`/`TEC` 的字数阈值提示，以及 HTML 原型中 28 个寄存器尚未进入 `hmi_tag_mapping.json`。后者按用户决策暂不扩展为变量表同步任务。

## 3. 验收矩阵

| 域 | 驾驶舱验证 | 实测结果 | 状态 | 证据/说明 |
|---|---|---:|:---:|---|
| 项目配置与库 | `auto_pm plc check DJ-2026-005 --json` | 51 PASS / 0 WARN / 0 FAIL | 已验证 | `.plc.json`、SysLib 关键文件 5/5 |
| PLC 结构与 SCL | 同上 | 目录、12 个 SCL、LSP-905 全通过 | 已验证 | 未执行 Siemens/TIA Portal 实机编译 |
| 文档实质性 | `auto_pm plc check DJ-2026-005 --substance` | 14 PASS / 2 WARN / 0 FAIL | 已验证（带观察项） | `INT`/`TEC` 字数低于建议阈值，但无占位符、章节齐全 |
| 全局 Doc-as-Code | `auto_pm doc check` | 100% PASS | 已验证 | CLI 命令覆盖与版本锁一致 |
| 变量表解析 | `auto_pm vartable detect-format/parse io_points.csv` | 119 点，71 DI / 48 DO，无错误 | 已验证 | 解析能力通过；不等同于现场点表同步 |
| HMI HTML 原型 | `auto_pm prototype check --pid DJ-2026-005` | 函数完整性通过 | 已验证（带观察项） | 原型 51 个寄存器标记，28 个映射提示按决策暂缓 |
| 变更账一致性 | `auto_pm change verify DJ-2026-005 --ledger-check` | 0 缺失 / 0 孤儿 / 0 状态不一致 | 已验证 | `CHG-HMI-2026-001` 已纳入台账 |
| 真实硬件 | TIA Portal / GX Works / GP-Pro EX 外部验证 | 未执行 | 外部验证 | 由现场环境完成编译、下装、I/O 与触摸屏联调 |

## 4. 本批整改与边界

### 4.1 已完成

- 修正驾驶舱 PLC 检查器：在历史标准路径与 `11_监控` 并存时，优先选择包含真实变更台账的完整目录。
- 补齐 `DJ-2026-005` HTML 原型的前端仿真控制句柄；所有动作仅改变浏览器原型状态，不输出实际硬件信号。
- 将新建的 HMI 变更单保留并归并至 `11_监控/01_变更管理/01_变更单/CHG-HMI/`；确认原 `04_监控` 仅为无文件兼容壳后清理空目录，未删除变更资产。

### 4.2 保留事项

- `INT` 与 `TEC` 文档的字数观察项不阻塞本次域消费；后续由项目 PM 决定是否补强。
- `hmi_tag_mapping.json` 的 28 个覆盖提示不作为本批失败条件；当前目标是 HTML 可交互原型与工艺设计文档，不做硬件标签同步。
- `DJ-2026-005` 的实际源工程含三菱地址语义；本验收不把它宣称为 Siemens SCL 编译通过。它作为 PLC 域结构/文档/原型活体标杆，Siemens 适配仍由后续平台适配任务单独验证。

## 5. 后续入口

- WBS7：已完成，验收矩阵作为治理账证据保存。
- WBS8：选择一个只读、可回退的 handoff 工作队列能力，走候选 worktree、自检、稳定版外部复核和 PM 收口。
- WBS9：在连续稳定批次和用户最终决策后，评估是否移除根入口旧代码回退；旧母体文档与历史变更单仍保留。
