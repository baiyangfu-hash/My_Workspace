---
description: PM 工作流与变更管理（对齐 pm-workflow 技能）
globs: **/PM_SESSION*.md,**/CHG-*.md,**/.auto-pm/**,**/00_项目管理/**
alwaysApply: false
---

# PM Workflow（Cursor）

处理 PM_SESSION、变更单、迭代推进时，读取 `.trae/skills/pm-workflow/SKILL.md`。

## 落账模式（按用户指示）

| 模式 | 行为 |
|------|------|
| **执行者**（默认） | 只读 PM_SESSION；产出 handoff，由 Trae pm-workflow 落账 |
| **PM owner**（用户明确要求） | 可回写 PM_SESSION §6–§9 与 `ai_feedback.json`，仍遵循 skill_coordination |

## 强制流程

1. `python -m auto_pm -w "<工作空间根>" project show <项目ID>`
2. 变更闭环前校验 CHG 12 章节完整性
3. CHG closed 后：`ledger reconcile <项目ID>`
4. 版本升级前：`spec check --check-id SHC-011,SHC-012,SHC-013,SHC-014`

## 禁止

- 文件名追加版本号后缀（如 `PRD_V0.5.0.md`）
- 用 Python 脚本直接写项目文件绕过编辑器（见 skill_coordination §9）
- 默认 GUI offscreen 测试（须可见窗口，除非用户要求）
