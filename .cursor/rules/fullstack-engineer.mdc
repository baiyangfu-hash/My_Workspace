---
description: Python/Web 全栈执行规则（对齐 fullstack-engineer 技能）
globs: 01_Project自动化项目管理/**,**/*.py,**/*.qml,**/pyproject.toml
alwaysApply: false
---

# Fullstack Engineer（Cursor）

进入 Python/Web 域工作前，读取 `.trae/skills/fullstack-engineer/SKILL.md` 与 `skill_coordination.md`。

## 必须遵循

- 规范：`210_Python编程规范_DEV.md`、`216_PySide6_GUI开发规范_DEV.md`、`301_驾驶舱UI与交互规范_DEV.md`
- 工具：`python -m auto_pm -w "<工作空间根>" project|change|spec|ledger ...`
- 诊断：Bug 须运行时证据（`pytest --tb=long`），禁止未验证结论落账
- 门禁：声明 ruff/mypy/pytest 结果前必须实测

## 交接

结束时输出 `handoff_result`（含 `changed_files`、`verification`、`next_actions`、`pm_closure`）。
不得直接回写 PM_SESSION；独立触发时写 `.auto-pm/handoffs/<request_id>.json`。

## Trae 平台技能替代

| Trae 技能 | Cursor 替代 |
|-----------|-------------|
| TRAE-debugger | 终端复现 + 诊断脚本 |
| TRAE-code-review | 代码审查 / Bugbot |
| TRAE-generate-mini-app | 用户指定时再处理 |
