1.在执行编程之前，要先描述任务的需求以及解决方案并等待批准，需求如果不明确，需要与人工沟通，确认需求，必须澄清问题。
2.复杂的任务需要被分解为多个简单的任务，同样复杂的程序需要被分解为多个简单的程序。复杂的对象拆解后为拆解后的对象添加编号，编号从00开始。
3.发现bug后，需要编写一个能复现bug的测试用例。不断测试，直至测试通过。
4.4.我纠正你后，并且确认纠正是正确的。就在本文件更新新的规则，保证重复问题不在产生。
5. Siemens LSP的.plc.json中，库引用字段名必须是`libraries`（不是libraryDirectories，后者不是有效字段名）。引用外部共享库时使用相对路径指向库根目录。
