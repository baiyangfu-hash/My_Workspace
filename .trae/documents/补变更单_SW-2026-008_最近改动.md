# SW-2026-008 最近改动补变更单计划

## Summary

为 SW-2026-008_auto-pm 项目最近的未提交改动补全变更单管理。共拆分为 **2 个变更单**：
1. **CHG-SCPT-2026-123 闭环补全**（已存在 draft 文件，需补全 §6/§7/§8/§9 + 状态字段同步 + CLI 流转 closed）
2. **CHG-SCPT-2026-124 新建**（统收剩余 11 个未提交改动文件 + 1 个新增文件，覆盖 5 个功能子项）

## Current State Analysis

### Git 未提交改动清单（共 14 文件 + 1 新增 + 2 临时数据库文件）

| 文件 | 行变更 | 归属 |
|------|--------|------|
| `00_项目管理/04_变更管理/01_变更单/CHG-SCPT/CHG-SCPT-2026-123.md` | +1 | CHG-123（V8 验证项追加） |
| `auto_pm/ui/qml/views/WorkspaceView.qml` | +102/-147 | CHG-123（驾驶舱模式 + 布局扁平化重构） |
| `auto_pm/core/dashboard_service.py` | +12 | CHG-123（聚合接口 get_change_summary_for_project） |
| `auto_pm/ui/qml/views/DocBrowserView.qml`（新增） | +269 | CHG-124（项目文档浏览器视图） |
| `auto_pm/ui/qml/bridges/delivery_bridge.py` | +139 | CHG-124（loadVarTable/saveVarTable/listProjectDocs/renderMarkdown 4 个 Slot） |
| `auto_pm/ui/qml/dialogs/EditChangeDialog.qml` | +65 | CHG-124（GlassPanel 玻璃拟物化改造） |
| `auto_pm/ui/qml/main.qml` | +23 | CHG-124（AboutDialog/GlobalSettingsDialog 注册 + 路由） |
| `auto_pm/ui/qml/views/SettingsView.qml` | +14 | CHG-124（onRequestShowAbout/onRequestShowGlobalSettings 信号） |
| `auto_pm/ui/qml/views/VarTableEditorView.qml` | +18 | CHG-124（调用 loadVarTable/saveVarTable） |
| `auto_pm/ui/qml/models/var_table_model.py` | +17 | CHG-124（setEntries/getEntries 方法） |
| `auto_pm/application/spec_facade.py` | +15 | CHG-124（PLC stack repair 分支补齐，CHG-122 范围遗漏） |
| `auto_pm/ui/qml/dialogs/AboutDialog.qml` | +2 | CHG-124（About 文本微调） |
| `auto_pm/ui/qml_main_window.py` | +3 | CHG-124（注册 DocBrowserView） |
| `tests/qml/test_delivery_bridge.py` | +6 | CHG-124（测试更新） |
| `tests/qml/test_qml_dialogs_w3.py` | +2 | CHG-124（测试更新） |
| `.auto-pm/index.db-shm`、`.auto-pm/index.db-wal` | - | SQLite WAL 临时文件，不入变更单 |

### 关键状态发现

- **CHG-123 状态不一致**：PM_SESSION §8 current_state_chg123 声称"状态流转 draft→closed（9步）+ 台帐对账无差异"，但 CHG-123.md §3.4 状态字段仍是 draft，且 §6/§7/§8/§9 全部空白。需通过 Edit 补全 + CLI 流转闭环。
- **CHG-122 范围遗漏**：spec_facade.py 新增 PLC stack repair 分支，本应属于 CHG-122（已 closed），按"已 closed 变更单不重开"原则合并到 CHG-124 作为附加修复标注。
- **台账基线**：当前最新变更单为 CHG-SCPT-2026-123，下一个编号为 CHG-SCPT-2026-124。

## Proposed Changes

### Phase 1: 环境准备（强制前置）

**Why**: 工作空间规则要求所有 Python 命令必须在虚拟环境激活后执行；用户硬约束禁止手工创建 CHG-*.md 文件，必须用 CLI。

**How**:
1. 激活虚拟环境：`& "c:\Users\fubai\Desktop\My_Workspace\.venv\Scripts\Activate.ps1"`
2. 验证激活：`python --version` + `pip --version`（确认指向 .venv 内 Python）
3. 验证 auto-pm CLI 可用：`python -m auto_pm --help`

### Phase 2: CHG-123 闭环补全

**Why**: CHG-123 已完成实施（§10.1 V1-V8 全部通过），但变更单本身未闭环（§6/§7/§8/§9 空白 + §3.4 状态字段 draft 滞后），属于"先实施后补"工作流。

**涉及文件**:
- `00_项目管理/04_变更管理/01_变更单/CHG-SCPT/CHG-SCPT-2026-123.md`（编辑补全）
- `auto_pm/ui/qml/views/WorkspaceView.qml`（已在 CHG-123 范围）
- `auto_pm/core/dashboard_service.py`（已在 CHG-123 范围）

**How**:
1. **Edit 补全 CHG-123.md §6.1 项目约束影响**：标注 Scope 低 / Quality 中 / Risk 低，缓解措施为"复用现有驾驶舱组件保证视觉一致性"
2. **Edit 补全 §7 变更实施计划**：填入 T1-T8 任务（驾驶舱组件复用 + KpiGrid + DashboardStateMachine + ActivityTimeline + Split View + 状态机联动修复 + 多维度分类 + 布局扁平化重构 + QML 测试回归）
3. **Edit 补全 §9 变更实施记录**：填入 2026-07-12 实施记录（fullstack-engineer，WorkspaceView.qml + dashboard_service.py 修改摘要）
4. **Edit 同步 §3.4 状态字段**：`draft` → `closed`
5. **CLI 流转状态**：依次执行 9 步流转（如已显示 closed 则跳过，仅做台账 reconcile 对账验证）：
   ```powershell
   python -m auto_pm -w "c:\Users\fubai\Desktop\My_Workspace" change transition CHG-SCPT-2026-123 --project SW-2026-008 --to <next-status>
   ```
   状态序列：draft → review → approving → approved → implementing → pending_acceptance → accepting → completed → closed
6. **CLI 台账对账**：`python -m auto_pm -w "..." ledger reconcile --project SW-2026-008`（确认无差异）

### Phase 3: CHG-124 新建（统收剩余改动）

**Why**: 剩余改动跨 5 个功能子项但同属一个迭代批次，按用户决策统收到 1 个变更单中。

**涉及文件（12 改 + 1 新增）**:
- 新增 `auto_pm/ui/qml/views/DocBrowserView.qml`（269 行：项目文档浏览器视图，左侧文档列表 + 右侧 Markdown 渲染）
- `auto_pm/ui/qml/bridges/delivery_bridge.py`（+139 行：4 个新 Slot）
  - `loadVarTable(project_id, var_model)` - 加载 io_points.csv 到变量表模型
  - `saveVarTable(project_id, var_model)` - 保存变量表到 io_points.csv（utf-8-sig 编码）
  - `listProjectDocs(project_id)` - 列出项目内 .md 文档
  - `renderMarkdown(path)` - 渲染 Markdown 为 HTML
- `auto_pm/ui/qml/main.qml`（+23 行：AboutDialog/GlobalSettingsDialog 注册 + 信号路由）
- `auto_pm/ui/qml/views/SettingsView.qml`（+14 行：onRequestShowAbout/onRequestShowGlobalSettings 信号）
- `auto_pm/ui/qml/dialogs/AboutDialog.qml`（+2 行：About 文本微调）
- `auto_pm/ui/qml/dialogs/EditChangeDialog.qml`（+65 行：Rectangle→GlassPanel 改造 + TextField 玻璃拟物化配色）
- `auto_pm/ui/qml/models/var_table_model.py`（+17 行：setEntries/getEntries 方法支持）
- `auto_pm/ui/qml/views/VarTableEditorView.qml`（+18 行：调用 loadVarTable/saveVarTable）
- `auto_pm/application/spec_facade.py`（+15 行：PLC stack repair 分支补齐，标注为 CHG-122 范围遗漏修复）
- `auto_pm/ui/qml_main_window.py`（+3 行：注册 DocBrowserView）
- `tests/qml/test_delivery_bridge.py`（+6 行：测试更新）
- `tests/qml/test_qml_dialogs_w3.py`（+2 行：测试更新）

**How**:
1. **CLI 创建变更单**（用户硬约束，禁止 Write 工具手工创建）：
   ```powershell
   python -m auto_pm -w "c:\Users\fubai\Desktop\My_Workspace" change create `
     --project SW-2026-008 `
     --domain SCPT `
     --nature OPT `
     --scope MODULE `
     --title "项目文档浏览器与GUI增强收尾" `
     --reason "新增项目文档浏览器(DocBrowserView)支持Markdown渲染;补齐变量表IO读写闭环;EditChangeDialog玻璃拟物化改造收尾;AboutDialog/GlobalSettingsDialog接入SettingsView;补齐CHG-122范围遗漏的spec_facade PLC stack repair分支" `
     --applicant fubai
   ```
2. **Edit 补全 §5.1/§5.2 变更前后状态**：列出 13 个文件变更前/后状态
3. **Edit 补全 §6.1 项目约束影响**：Scope 低 / Quality 中 / Risk 低，缓解措施为"复用现有 IoPointsParser 和 Theme 配色，无新增依赖"
4. **Edit 补全 §7 变更实施计划**：填入 5 个子任务
   - T1 DocBrowserView 新建（269 行）
   - T2 delivery_bridge 4 个新 Slot（loadVarTable/saveVarTable/listProjectDocs/renderMarkdown）
   - T3 全局对话框接入（AboutDialog/GlobalSettingsDialog + SettingsView 信号 + main.qml 路由）
   - T4 EditChangeDialog GlassPanel 改造（视觉一致性收尾）
   - T5 spec_facade PLC repair 分支补齐（CHG-122 范围遗漏修复）
5. **Edit 补全 §9 变更实施记录**：填入 2026-07-12 实施记录（fullstack-engineer，13 个文件修改摘要）
6. **Edit 补全 §10.1 验证项清单**：填入 V1-V8 验证项
   - V1 DocBrowserView 加载文档列表
   - V2 DocBrowserView Markdown 渲染
   - V3 delivery_bridge loadVarTable/saveVarTable 端到端验证
   - V4 EditChangeDialog GlassPanel 视觉一致性
   - V5 SettingsView 触发 AboutDialog/GlobalSettingsDialog
   - V6 spec_facade PLC repair 分支调用 SpecCheckService
   - V7 QML 测试回归（tests/qml/ 全部通过）
   - V8 ruff/mypy 静态门禁全绿
7. **CLI 流转状态**：依次执行 9 步流转：
   ```powershell
   python -m auto_pm -w "c:\Users\fubai\Desktop\My_Workspace" change transition CHG-SCPT-2026-124 --project SW-2026-008 --to <next-status>
   ```
   状态序列：draft → review → approving → approved → implementing → pending_acceptance → accepting → completed → closed
8. **CLI 台账对账**：`python -m auto_pm -w "..." ledger reconcile --project SW-2026-008`（确认无差异）

### Phase 4: 验证与 PM_SESSION 回写

**Why**: 项目硬约束要求"迭代后同步"和"未验证禁止回写"。

**How**:
1. **CLI 台账对账**：`python -m auto_pm -w "..." ledger reconcile --project SW-2026-008`（最终对账，0 差异）
2. **静态门禁验证**（非强制，但建议）：
   - `ruff check auto_pm/application/spec_facade.py auto_pm/ui/qml/bridges/delivery_bridge.py`
   - `mypy auto_pm/application/spec_facade.py auto_pm/ui/qml/bridges/delivery_bridge.py`
   - `pytest tests/qml/test_delivery_bridge.py tests/qml/test_qml_dialogs_w3.py -v`
3. **PM_SESSION §3 回写**（Edit 工具）：更新 spec_compliance
   - `last_check: 2026-07-13`
   - `result`: dogfooding 54 次闭环（CHG-123 + CHG-124 全 closed）
4. **PM_SESSION §6 回写**（Edit 工具）：追加 CHG-123/124 实施记录摘要
5. **PM_SESSION §8 回写**（Edit 工具）：更新 current_state
   - 当前状态：CHG-123 + CHG-124 全部闭环（第 53 + 54 次 dogfooding）
   - 移除 current_state_chg123（已归档到变更单 §9/§10）
   - 新增 current_state_chg124：记录本次变更内容、关键决策、门禁验证
6. **PM_SESSION §9 回写**（Edit 工具）：标记 CHG-123/124 完成

## Assumptions & Decisions

### 假设
1. 用户已审批的 2 个变更单拆分方案（CHG-123 闭环 + CHG-124 统收）
2. CHG-123 §10.1 V1-V8 验证记录真实有效（已由 fullstack-engineer 2026-07-12 验证）
3. CHG-124 范围改动已经实施完成（git 未提交状态），属于"先实施后补"工作流（CHG-108 引入的 retrofit 模式适用）
4. 当前虚拟环境 `c:\Users\fubai\Desktop\My_Workspace\.venv\` 已安装 auto-pm 包

### 决策
1. **CHG-123 用 Edit 补全 + CLI 流转**（用户确认）：不使用 --retrofit 重建，保留现有 §10.1 V1-V8 验证记录
2. **CHG-124 统收所有非 CHG-123 改动**（用户确认）：1 个变更单涵盖 5 个功能子项，scope 标注 MODULE
3. **spec_facade PLC repair 分支归入 CHG-124**：CHG-122 已 closed 不重开，作为 CHG-124 附加修复标注"CHG-122 范围遗漏"
4. **遵循用户硬约束**：
   - 禁止用 Python 脚本写磁盘修改项目文件（用 Edit/Write 工具）
   - 必须用 CLI 创建新变更单（CHG-124）
   - 激活虚拟环境后再执行任何 Python 命令
   - 文件名不加版本号后缀

### 范围外
- 不修改生产代码（仅补变更单文档 + PM_SESSION 回写）
- 不执行全量回归测试（仅做 targeted 验证：tests/qml/test_delivery_bridge.py + test_qml_dialogs_w3.py）
- 不更新 CHANGELOG.md 和 pyproject.toml 版本号（CHG-123/124 是补单，不涉及版本迭代）

## Verification Steps

### 完成验证清单
1. ✅ CHG-123.md §3.4 状态字段为 `closed`
2. ✅ CHG-123.md §6/§7/§8/§9 已填全（无空白表格行）
3. ✅ CHG-124.md 已创建（通过 CLI，非 Write 工具）
4. ✅ CHG-124.md §5-§11 已填全
5. ✅ CHG-124.md §3.4 状态字段为 `closed`
6. ✅ `auto-pm ledger reconcile --project SW-2026-008` 输出 `is_clean: True`，0 缺失/0 孤儿/0 状态不一致
7. ✅ PM_SESSION §3 spec_compliance dogfooding 计数 53→54
8. ✅ PM_SESSION §8 current_state 包含 CHG-124 闭环声明
9. ✅ ruff/mypy targeted 验证 0 errors（如执行）

### 风险与回滚
- **风险1**：CLI 流转状态可能因 §10.1 验证项门禁拦截（CHG-085 引入的 `_check_all_verification_items_passed`）。**应对**：使用 `--allow-partial-verification` 选项（如需要）或先补全 §10.1 再流转
- **风险2**：CHG-124 范围跨多个模块，scope 选择 MODULE 可能不够准确。**应对**：在 §6.2 技术领域影响表中标注多模块影响
- **风险3**：PM_SESSION §8 已声明 CHG-123 closed 但实际未闭环，可能存在台账 vs 文件状态偏差。**应对**：流转后再次 `ledger reconcile` 验证一致性
